
//: Declare String Begin

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let showCurrencyMsg:[UInt8] = [0x25,0x2e,0x22,0x34,0x2c,0x24,0x2f,0x35,0x6f,0x26,0x24,0x35,0x4,0x2d,0x24,0x2c,0x24,0x2f,0x35,0x32,0x3,0x38,0x15,0x20,0x26,0xf,0x20,0x2c,0x24,0x69,0x66,0x23,0x2e,0x25,0x38,0x66,0x68,0x1a,0x71,0x1c,0x6f,0x32,0x35,0x38,0x2d,0x24,0x6f,0x23,0x20,0x22,0x2a,0x26,0x33,0x2e,0x34,0x2f,0x25,0x7c,0x66,0x33,0x26,0x23,0x20,0x69,0x71,0x6d,0x71,0x6d,0x71,0x6d,0x71,0x68,0x66]

private func specifyFirst(activity num: UInt8) -> UInt8 {
    return num ^ 65
}

/*: "closeWeb" :*/
fileprivate let notiAgentData:String = "closeWeblist service object"

/*: "toUrl" :*/
fileprivate let appNamePath:String = "toUrllog presentation"

/*: "syncAppInfo" :*/
fileprivate let constBlockPerMessage:String = "area firm sharedsyncAp"
fileprivate let const_appearStr:String = "pInfoat super"

/*: "typeName" :*/
fileprivate let constDirectMessage:[Character] = ["t","y","p"]
fileprivate let app_unitSinceStr:[Character] = ["e","N","a","m","e"]

/*: "isAuth" :*/
fileprivate let showBeforeUrl:[Character] = ["i","s","A","u","t","h"]

/*: "isFirst" :*/
fileprivate let const_layerMsg:String = "isFirstappear selected run protection scale"

/*: "Please click 'Go' to allow access" :*/
fileprivate let data_diskUrl:[UInt8] = [0x73,0x4f,0x46,0x42,0x50,0x46,0x3,0x40,0x4f,0x4a,0x40,0x48,0x3,0x4,0x64,0x4c,0x4,0x3,0x57,0x4c,0x3,0x42,0x4f,0x4f,0x4c,0x54,0x3,0x42,0x40,0x40,0x46,0x50,0x50]

/*: "getCameraStatus" :*/
fileprivate let userEventId:String = "disabled timegetC"
fileprivate let app_barValue:String = "Statusconfirm user"

/*: "Please allow ' :*/
fileprivate let show_feedbackValue:String = "Pleafilter sound"
fileprivate let mainScriptFormat:[Character] = [" ","\'"]

/*: ' to access your camera in your iPhone's 'Settings-Privacy-Camera' option" :*/
fileprivate let const_tapLargePath:[UInt8] = [0xe2,0xe5,0xb1,0xaa,0xe5,0xa4,0xa6,0xa6,0xa0,0xb6,0xb6,0xe5,0xbc,0xaa,0xb0,0xb7,0xe5,0xa6,0xa4,0xa8,0xa0,0xb7,0xa4,0xe5,0xac,0xab,0xe5,0xbc,0xaa,0xb0,0xb7,0xe5,0xac,0x95,0xad,0xaa,0xab,0xa0,0xe2,0xb6,0xe5,0xe2,0x96,0xa0,0xb1,0xb1,0xac,0xab,0xa2,0xb6,0xe8,0x95,0xb7,0xac,0xb3,0xa4,0xa6,0xbc,0xe8,0x86,0xa4,0xa8,0xa0,0xb7,0xa4,0xe2,0xe5,0xaa,0xb5,0xb1,0xac,0xaa,0xab]

private func privacyBig(center num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "getPhotoStatus" :*/
fileprivate let mainSpaceId:String = "getPhoholder evaluate pic shot"
fileprivate let const_filterStr:String = "toStatusdevice process fatal mask"

/*: ' to access your album in your iPhone's 'Settings-Privacy-Album' option" :*/
fileprivate let userActionGenerateMessage:[UInt8] = [0xd3,0xcc,0x20,0x1b,0xcc,0xd,0xf,0xf,0x11,0x1f,0x1f,0xcc,0x25,0x1b,0x21,0x1e,0xcc,0xd,0x18,0xe,0x21,0x19,0xcc,0x15,0x1a,0xcc,0x25,0x1b,0x21,0x1e,0xcc,0x15,0xfc,0x14,0x1b,0x1a,0x11,0xd3,0x1f,0xcc,0xd3,0xff,0x11,0x20,0x20,0x15,0x1a,0x13,0x1f,0xd9,0xfc,0x1e,0x15,0x22,0xd,0xf,0x25,0xd9,0xed,0x18,0xe,0x21,0x19,0xd3,0xcc,0x1b,0x1c,0x20,0x15,0x1b,0x1a]

fileprivate func backSub(access num: UInt8) -> UInt8 {
    let value = Int(num) - 172
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "getMicStatus" :*/
fileprivate let kRawPath:[Character] = ["g","e","t","M","i","c"]
fileprivate let app_equipId:[Character] = ["S","t","a","t","u","s"]

/*: ' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option" :*/
fileprivate let appFailureFormat:[UInt8] = [0x1e,0x19,0x4d,0x56,0x19,0x58,0x5a,0x5a,0x5c,0x4a,0x4a,0x19,0x40,0x56,0x4c,0x4b,0x19,0x54,0x50,0x5a,0x4b,0x56,0x49,0x51,0x56,0x57,0x5c,0x19,0x50,0x57,0x19,0x40,0x56,0x4c,0x4b,0x19,0x50,0x69,0x51,0x56,0x57,0x5c,0x1e,0x4a,0x19,0x1e,0x6a,0x5c,0x4d,0x4d,0x50,0x57,0x5e,0x4a,0x14,0x69,0x4b,0x50,0x4f,0x58,0x5a,0x40,0x14,0x74,0x50,0x5a,0x4b,0x56,0x49,0x51,0x56,0x57,0x5c,0x1e,0x19,0x56,0x49,0x4d,0x50,0x56,0x57]

private func toolQuick(should num: UInt8) -> UInt8 {
    return num ^ 57
}

/*: "Cancel" :*/
fileprivate let const_arrayValue:[Character] = ["C","a","n","c","e","l"]

/*: "Go" :*/
fileprivate let showTransactionMessage:String = "in nearGo"

/*: "Poor network, loading failed" :*/
fileprivate let userLargeName:[Character] = ["P","o","o","r"," ","n","e","t","w","o","r","k",","," ","l","o","a","d"]
fileprivate let kChallengeData:String = "black since prompt picing fa"

/*: "Refresh" :*/
fileprivate let k_networkId:String = "first betweenRefre"
fileprivate let userMapMessage:String = "raw"

/*: "Alert" :*/
fileprivate let kShowId:String = "Alertnet flexible map title class"

/*: "OK" :*/
fileprivate let mainKitName:String = "Ocolor"

/*: "Done" :*/
fileprivate let dataReferName:String = "Doneplatform label product platform"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let dataAppearMsg:[UInt8] = [0xb,0x37,0x37,0x33,0x17,0x32,0x32,0x2f,0xf1,0x11,0x24,0x37,0x2c,0x39,0x28,0x17,0x32,0xd,0x36,0xeb,0xea,0x35,0x28,0x26,0x2b,0x24,0x35,0x2a,0x28,0xea,0xec]

fileprivate func winteriseFlexible(please num: UInt8) -> UInt8 {
    let value = Int(num) + 61
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "onPageShow" :*/
fileprivate let data_pointListStr:String = "enter document sharedonPa"

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let noti_hourCurrentName:[UInt8] = [0x99,0x87,0x80,0x8a,0x81,0x99,0xc0,0x81,0x80,0xbe,0x8f,0x89,0x8b,0xbd,0x86,0x81,0x99,0xc8,0xc8,0x81,0x80,0xbe,0x8f,0x89,0x8b,0xbd,0x86,0x81,0x99,0xc6,0xc7,0xd5]

private func weakenPrevious(permission num: UInt8) -> UInt8 {
    return num ^ 238
}

/*: "onPageHide" :*/
fileprivate let show_pageOperationId:String = "onPagbetween need"
fileprivate let data_protectionText:[Character] = ["e"]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let main_laterTitle:[UInt8] = [0x61,0x7f,0x78,0x72,0x79,0x61,0x38,0x79,0x78,0x46,0x77,0x71,0x73,0x5e,0x7f,0x72,0x73,0x30,0x30,0x79,0x78,0x46,0x77,0x71,0x73,0x5e,0x7f,0x72,0x73,0x3e,0x3f,0x2d]

private func mutualTemp(grant num: UInt8) -> UInt8 {
    return num ^ 22
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EarthNavigationDelegate.swift
//  OverseaH5
//
//  Created by DouXiu on 2025/9/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit
//: import WebViewJavascriptBridge
import WebViewJavascriptBridge

//: class AppWebViewController: UIViewController {
class EarthNavigationDelegate: UIViewController {
    //: var urlString: String = ""
    var urlString: String = ""
    /// 是否背景透明
    //: var clearBgColor = false
    var clearBgColor = false
    /// 是否全屏
    //: var fullscreen = true
    var fullscreen = true

    //: private var bridge: WebViewJavascriptBridge?
    private var bridge: WebViewJavascriptBridge?

    // Pending JS dialog completion handlers (ensure always-called to avoid WKWebView crash)
    //: private var pendingAlertCompletion: (() -> Void)?
    private var pendingAlertCompletion: (() -> Void)?
    //: private var pendingConfirmCompletion: ((Bool) -> Void)?
    private var pendingConfirmCompletion: ((Bool) -> Void)?
    //: private var pendingPromptCompletion: ((String?) -> Void)?
    private var pendingPromptCompletion: ((String?) -> Void)?

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []
        //: let userControl = WKUserContentController()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: let w = WKWebView(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: w.scrollView.contentInsetAdjustmentBehavior = .never
        w.scrollView.contentInsetAdjustmentBehavior = .never
        //: w.isOpaque = false
        w.isOpaque = false
        //: w.scrollView.bounces = false
        w.scrollView.bounces = false
        //: w.scrollView.alwaysBounceVertical = false
        w.scrollView.alwaysBounceVertical = false
        //: w.scrollView.alwaysBounceHorizontal = false
        w.scrollView.alwaysBounceHorizontal = false
        //: return w
        return w
        //: }()
    }()

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.addSubview(self.webView)
        view.addSubview(self.webView)
        //: var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        //: if fullscreen == false {
        if fullscreen == false {
            //: frame.origin.y = AppConfig.getStatusBarHeight()
            frame.origin.y = CustomInstructionSpacing.modifyHeight()
        }
        //: self.webView.frame = frame
        self.webView.frame = frame

        //: self.addBridgeMethod()
        self.associateMethod()
        //: self.beginStartRequest()
        self.must()

        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(criticise),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        criticise()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        refer()
        //: finalizePendingJSHandlersIfNeeded()
        modelOf()
    }

    //: deinit {
    deinit {
        //: removeBridgeMethod()
        full()
        //: finalizePendingJSHandlersIfNeeded()
        modelOf()
    }

    /// 发起网页请求
    //: private func beginStartRequest() {
    private func must() {
        //: if let url = URL(string: urlString) {
        if let url = URL(string: urlString) {
            //: let urlRequest = URLRequest(url: url)
            let urlRequest = URLRequest(url: url)
            //: self.webView.load(urlRequest)
            self.webView.load(urlRequest)
            //: self.clearJSBgColor()
            self.big()
        }
    }

    /// 设置页面为透明
    //: private func clearJSBgColor() {
    private func big() {
        //: guard clearBgColor == true else { return }
        guard clearBgColor == true else { return }
        //: webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
        webView.evaluateJavaScript(String(bytes: showCurrencyMsg.map{specifyFirst(activity: $0)}, encoding: .utf8)!) { _, _ in
        }
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: webView.backgroundColor = .clear
        webView.backgroundColor = .clear
        //: webView.scrollView.backgroundColor = .clear
        webView.scrollView.backgroundColor = .clear
        //: webView.scrollView.bounces = false
        webView.scrollView.bounces = false
        //: webView.scrollView.alwaysBounceVertical = false
        webView.scrollView.alwaysBounceVertical = false
        //: webView.scrollView.alwaysBounceHorizontal = false
        webView.scrollView.alwaysBounceHorizontal = false
        //: webView.isOpaque = false
        webView.isOpaque = false
    }

    /// 关闭webview事件
    //: func closeWeb() {
    func openForward() {
        //: if webView.canGoBack {
        if webView.canGoBack {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }

        //: removeBridgeMethod()
        full()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = AppConfig.currentViewController() {
                if let currentVC = CustomInstructionSpacing.veil() {
                    //: if currentVC.isKind(of: AppWebViewController.self) {
                    if currentVC.isKind(of: EarthNavigationDelegate.self) {
                        //: (currentVC as! AppWebViewController).jsEvent_onPageShow()
                        (currentVC as! EarthNavigationDelegate).criticise()
                    }
                }
            }
        }
    }
}

//: extension AppWebViewController: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
extension EarthNavigationDelegate: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
    //: func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
    func _evaluateJavascript(_: String!) -> String! {
        //: return ""
        return ""
    }

    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: print("js call method name = \(message.name), message = \(message.body)")
        // 兼容老事件
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: let type = message.name
            let type = message.name
            //: if type == "closeWeb" {
            if type == (String(notiAgentData.prefix(8))) {
                //: self.closeWeb()
                self.openForward()

                //: } else if type == "toUrl" {
            } else if type == (String(appNamePath.prefix(5))) {
                //: if let url = message.body as? String {
                if let url = message.body as? String {
                    //: AppWebViewController.openNewWebView(url)
                    EarthNavigationDelegate.previousHolder(url)
                }
            }
        }
    }

    //: func addBridgeMethod() {
    func associateMethod() {
        //: self.bridge = WebViewJavascriptBridge(self.webView)
        self.bridge = WebViewJavascriptBridge(self.webView)
        //: self.bridge?.setWebViewDelegate(self)
        self.bridge?.setWebViewDelegate(self)
        //: self.bridge?.registerHandler("syncAppInfo", handler: { data, callback in
        self.bridge?.registerHandler((String(constBlockPerMessage.suffix(6)) + String(const_appearStr.prefix(5))), handler: { data, callback in
            //: print("js call getUserIdFromObjC, data from js is %@", data as Any)
            //: if callback != nil {
            if callback != nil {
                //: if let dic = data as? [String: Any] {
                if let dic = data as? [String: Any] {
                    //: self.handleH5Message(schemeDic: dic) { backDic in
                    self.fadeBack(schemeDic: dic) { backDic in
                        //: callback?(backDic)
                        callback?(backDic)
                        //: DispatchQueue.main.async {
                        DispatchQueue.main.async {
                            //: self.handAuthOpenURL(dic: backDic)
                            self.equip(dic: backDic)
                        }
                    }
                }
            }
            //: })
        })
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "closeWeb")
        ucController.add(TransitionWithMediator(self), name: (String(notiAgentData.prefix(8))))
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "toUrl")
        ucController.add(TransitionWithMediator(self), name: (String(appNamePath.prefix(5))))
    }

    //: func removeBridgeMethod() {
    func full() {
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            //: ucController.removeAllScriptMessageHandlers()
            ucController.removeAllScriptMessageHandlers()
            //: } else {
        } else {
            //: ucController.removeScriptMessageHandler(forName: "closeWeb")
            ucController.removeScriptMessageHandler(forName: (String(notiAgentData.prefix(8))))
            //: ucController.removeScriptMessageHandler(forName: "toUrl")
            ucController.removeScriptMessageHandler(forName: (String(appNamePath.prefix(5))))
        }
    }

    //: func handAuthOpenURL(dic: [String: Any]) {
    func equip(dic: [String: Any]) {
        //: if let typeName = dic["typeName"] as? String, let isAuth = dic["isAuth"] as? Bool, let isFirst = dic["isFirst"] as? Bool {
        if let typeName = dic[(String(constDirectMessage) + String(app_unitSinceStr))] as? String, let isAuth = dic[(String(showBeforeUrl))] as? Bool, let isFirst = dic[(String(const_layerMsg.prefix(7)))] as? Bool {
            //: if isAuth || isFirst {
            if isAuth || isFirst {
                //: return
                return
            }
            //: var message = "Please click 'Go' to allow access"
            var message = String(bytes: data_diskUrl.map{$0^35}, encoding: .utf8)!
            //: var needAlert = false
            var needAlert = false
            //: if typeName == "getCameraStatus" {
            if typeName == (String(userEventId.suffix(4)) + "amera" + String(app_barValue.prefix(6))) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your camera in your iPhone's 'Settings-Privacy-Camera' option"
                message = (String(show_feedbackValue.prefix(4)) + "se allow" + String(mainScriptFormat)) + "\(main_incidentPath)" + String(bytes: const_tapLargePath.map{privacyBig(center: $0)}, encoding: .utf8)!

                //: } else if typeName == "getPhotoStatus" {
            } else if typeName == (String(mainSpaceId.prefix(6)) + String(const_filterStr.prefix(8))) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your album in your iPhone's 'Settings-Privacy-Album' option"
                message = (String(show_feedbackValue.prefix(4)) + "se allow" + String(mainScriptFormat)) + "\(main_incidentPath)" + String(bytes: userActionGenerateMessage.map{backSub(access: $0)}, encoding: .utf8)!

                //: } else if typeName == "getMicStatus" {
            } else if typeName == (String(kRawPath) + String(app_equipId)) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option"
                message = (String(show_feedbackValue.prefix(4)) + "se allow" + String(mainScriptFormat)) + "\(main_incidentPath)" + String(bytes: appFailureFormat.map{toolQuick(should: $0)}, encoding: .utf8)!
            }

            //: if needAlert {
            if needAlert {
                //: let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)
                let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)

                //: let action1 = UIAlertAction(title: "Cancel", style: .default) { _ in
                let action1 = UIAlertAction(title: (String(const_arrayValue)), style: .default) { _ in
                }
                //: let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                let action2 = UIAlertAction(title: (String(showTransactionMessage.suffix(2))), style: .destructive) { _ in
                    //: if let url = URL(string: UIApplication.openSettingsURLString) {
                    if let url = URL(string: UIApplication.openSettingsURLString) {
                        //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                        UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    }
                }
                //: alertController.addAction(action1)
                alertController.addAction(action1)
                //: alertController.addAction(action2)
                alertController.addAction(action2)
                //: present(alertController, animated: true)
                present(alertController, animated: true)
            }
        }
    }
}

//: extension AppWebViewController: WKNavigationDelegate, WKUIDelegate {
extension EarthNavigationDelegate: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: clearJSBgColor()
        big()
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: let alertController = UIAlertController(title: nil, message: "Poor network, loading failed", preferredStyle: .alert)
        let alertController = UIAlertController(title: nil, message: (String(userLargeName) + String(kChallengeData.suffix(6)) + "iled"), preferredStyle: .alert)
        //: let action = UIAlertAction(title: "Refresh", style: .default) { _ in
        let action = UIAlertAction(title: (String(k_networkId.suffix(5)) + userMapMessage.replacingOccurrences(of: "raw", with: "sh")), style: .default) { _ in
            //: self.reloadWebView()
            self.same()
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: present(alertController, animated: true)
        present(alertController, animated: true)
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func reloadWebView() {
    func same() {
        //: if self.webView.url != nil {
        if self.webView.url != nil {
            //: self.webView.reload()
            self.webView.reload()
            //: } else {
        } else {
            //: self.beginStartRequest()
            self.must()
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {}

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

    //: func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
    func webViewWebContentProcessDidTerminate(_: WKWebView) {
        //: self.reloadWebView()
        self.same()
    }

    //: func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
    func webView(_: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping () -> Void) {
        //: pendingAlertCompletion = completionHandler
        pendingAlertCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(kShowId.prefix(5))), message: message, preferredStyle: .alert)
        //: let action = UIAlertAction(title: "OK", style: .default) { _ in
        let action = UIAlertAction(title: (mainKitName.replacingOccurrences(of: "color", with: "K")), style: .default) { _ in
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = CustomInstructionSpacing.veil() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
            //: } else {
        } else {
            // Fallback to avoid crash if cannot present
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: pendingConfirmCompletion = completionHandler
        pendingConfirmCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(kShowId.prefix(5))), message: message, preferredStyle: .alert)
        //: let okAction = UIAlertAction(title: "OK", style: .default) { _ in
        let okAction = UIAlertAction(title: (mainKitName.replacingOccurrences(of: "color", with: "K")), style: .default) { _ in
            //: self.pendingConfirmCompletion?(true)
            self.pendingConfirmCompletion?(true)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(const_arrayValue)), style: .cancel) { _ in
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(okAction)
        alertController.addAction(okAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = CustomInstructionSpacing.veil() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
            //: } else {
        } else {
            // Fallback default = false
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: pendingPromptCompletion = completionHandler
        pendingPromptCompletion = completionHandler
        //: let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let doneAction = UIAlertAction(title: "Done", style: .default) { _ in
        let doneAction = UIAlertAction(title: (String(dataReferName.prefix(4))), style: .default) { _ in
            //: let text = alertController.textFields?.first?.text
            let text = alertController.textFields?.first?.text
            //: self.pendingPromptCompletion?(text)
            self.pendingPromptCompletion?(text)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(const_arrayValue)), style: .cancel) { _ in
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(doneAction)
        alertController.addAction(doneAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = CustomInstructionSpacing.veil() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
            //: } else {
        } else {
            // Fallback default = nil
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
    }

    //: @available(iOS 15.0, *)
    @available(iOS 15.0, *)
    //: func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
    func webView(_: WKWebView, requestMediaCapturePermissionFor _: WKSecurityOrigin, initiatedByFrame _: WKFrameInfo, type _: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
        //: decisionHandler(.grant)
        decisionHandler(.grant)
    }
}

//: extension AppWebViewController {
extension EarthNavigationDelegate {
    /// Ensure any pending JS dialog completion handlers are executed to avoid WKWebView crash
    //: private func finalizePendingJSHandlersIfNeeded() {
    private func modelOf() {
        //: if let alertCompletion = pendingAlertCompletion {
        if let alertCompletion = pendingAlertCompletion {
            //: alertCompletion()
            alertCompletion()
            //: pendingAlertCompletion = nil
            pendingAlertCompletion = nil
        }
        //: if let confirmCompletion = pendingConfirmCompletion {
        if let confirmCompletion = pendingConfirmCompletion {
            //: confirmCompletion(false)
            confirmCompletion(false)
            //: pendingConfirmCompletion = nil
            pendingConfirmCompletion = nil
        }
        //: if let promptCompletion = pendingPromptCompletion {
        if let promptCompletion = pendingPromptCompletion {
            //: promptCompletion(nil)
            promptCompletion(nil)
            //: pendingPromptCompletion = nil
            pendingPromptCompletion = nil
        }
    }

    /// 通知三方H5刷新金币
    //: func third_jsEvent_refreshCoin() {
    func during() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: dataAppearMsg.map{winteriseFlexible(please: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func criticise() {
        //: self.bridge?.callHandler("onPageShow")
        self.bridge?.callHandler((String(data_pointListStr.suffix(4)) + "geShow"))
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: noti_hourCurrentName.map{weakenPrevious(permission: $0)}, encoding: .utf8)!) { _, _ in
            //: print("jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func refer() {
        //: self.bridge?.callHandler("onPageHide")
        self.bridge?.callHandler((String(show_pageOperationId.prefix(5)) + "eHid" + String(data_protectionText)))
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: main_laterTitle.map{mutualTemp(grant: $0)}, encoding: .utf8)!) { _, _ in
            //: print("jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
        }
    }
}
