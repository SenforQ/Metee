
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appMakePastFormat:[UInt8] = [0xa,0xd,0xa,0x17,0x4b,0x0,0xc,0x7,0x6,0x11,0x59,0x4a,0x43,0xb,0x2,0x10,0x43,0xd,0xc,0x17,0x43,0x1,0x6,0x6,0xd,0x43,0xa,0xe,0x13,0xf,0x6,0xe,0x6,0xd,0x17,0x6,0x7]

private func appNext(frame num: UInt8) -> UInt8 {
    return num ^ 99
}

/*: "uid" :*/
fileprivate let data_actionKey:[UInt8] = [0xcf,0xd3,0xde]

private func labIndexBottom(color num: UInt8) -> UInt8 {
    return num ^ 186
}

/*: "headPic" :*/
fileprivate let main_rawStr:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let appFrameValue:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let userFileName:String = "BIRTHDAY"

/*: "videoUrl" :*/
fileprivate let appNoseStr:String = "player"
fileprivate let notiLivePath:[Character] = ["i","d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let app_atHomeTitle:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let main_videoColorName:String = "locatwhiteon"

/*: "cmd" :*/
fileprivate let k_titlePath:[UInt8] = [0x4b,0x45,0x4c]

/*: "refuseCall" :*/
fileprivate let mainPicStr:[Character] = ["r","e","f","u","s"]
fileprivate let const_sizeName:[Character] = ["e","C","a","l","l"]

/*: "data" :*/
fileprivate let constFrontId:[UInt8] = [0xe3,0xe0,0xf3,0xe0]

fileprivate func listValue(old num: UInt8) -> UInt8 {
    let value = Int(num) - 127
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let const_sexName:String = "Call model center path transaction network"
fileprivate let show_exposeMakeMsg:String = "progresst"
fileprivate let appWithTitle:String = "ecurrentuption"

/*: "stopCall" :*/
fileprivate let show_itemViewStr:[Character] = ["s","t","o","p","C"]
fileprivate let k_draftMessage:String = "asizesize"

/*: "logId" :*/
fileprivate let app_clickUrl:[UInt8] = [0x8e,0x8d,0x85,0xab,0x86]

private func nameValue(moment num: UInt8) -> UInt8 {
    return num ^ 226
}

/*: "cancelCall" :*/
fileprivate let app_contentStr:String = "topn"
fileprivate let kNextPartyPath:String = "self true user top letcelCall"

/*: "No answer" :*/
fileprivate let noti_featureViewValue:String = "No ansize path transform to corner"
fileprivate let constSmallKey:[Character] = ["s","w","e","r"]

/*: "duration" :*/
fileprivate let user_labMsg:[Character] = ["d"]
fileprivate let showTargetFormat:String = "URATION"

/*: "She has hung up" :*/
fileprivate let app_addCenterPath:[Character] = ["S","h","e"," ","h","a","s"," ","h"]
fileprivate let const_toText:String = "ung upcontainer in"

/*: "cancelSex" :*/
fileprivate let dataPicRequestColorMsg:[Character] = ["c","a","n","c","e","l","S"]
fileprivate let appSectionFormat:String = "eequal"

/*: "He has hung up" :*/
fileprivate let const_imageValue:String = "random var varHe has"
fileprivate let noti_shareAppContent:[Character] = ["u","p"]

/*: "endType" :*/
fileprivate let showPlayerObjectAttachName:String = "EN"
fileprivate let appLabelMessage:[Character] = ["d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let userEqualData:String = "stopSexmodel extension in self view"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let dataDirectionMsg:[UInt8] = [0x14,0x33,0x2e,0x28,0x3b,0x3b,0x34,0x3e,0x34,0x38,0x33,0x29,0x7d,0x3a,0x32,0x31,0x39,0x7d,0x3e,0x32,0x34,0x33,0x2e,0x71,0x7d,0x3e,0x3c,0x31,0x31,0x7d,0x35,0x3c,0x2e,0x7d,0x38,0x33,0x39,0x38,0x39]

private func quantityeractionName(bar num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "The other hung up the phone" :*/
fileprivate let user_makeToUrl:String = "var color ofThe o"
fileprivate let main_indexLabUrl:String = "user regular super equalung "
fileprivate let kSourceData:String = "hwindowe"

/*: "type" :*/
fileprivate let dataManagerPathContent:String = "typm"

/*: "jumpUrl" :*/
fileprivate let constAddTitle:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let notiCenterTimeKitMessage:String = "Cancelmessage value text label"

/*: "Get gold coins" :*/
fileprivate let app_fillId:[Character] = ["G","e","t"," ","g","o","l","d"," "]
fileprivate let showModelTitle:[Character] = ["c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let noti_dataFormat:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let showPendingEqualCenterKey:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let app_yourName:[Character] = ["o","n","A","c","c"]
fileprivate let appListKey:[Character] = ["e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let show_shouldContent:String = "enable table let imageacceptCa"
fileprivate let showFileFormat:String = "centercenter"

/*: "Calls are cancelled" :*/
fileprivate let mainExtraMsg:String = "true guard background giftCall"
fileprivate let main_indexToId:[Character] = [" ","c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let appChoiceContent:String = "message amThe c"
fileprivate let constRegularKey:String = "text make self sizes ended"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PlayerCallerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let kHalfTableMessage = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class PlayerCallerDelegate: ScoreViewController {
	var stateMagnitude: Int = 6
	var recordCount: Double = -64.2
	var moreArray: [AnyHashable] = []
	var errorInsertDictionary: [AnyHashable: String] = [:]
	var frameCaptureMagnitude: Int = 43
	var colorTotal: Double = -58.7
	var timeArray: [AnyHashable] = []
	var statisticalTableDictionary: [AnyHashable: String] = [:]

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: SubChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: UserDataPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: VisualCommunicationReactiveCompatible? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: EffectScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ActionSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ActionSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ActionSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appMakePastFormat.map{appNext(frame: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! OriginalHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.someTotaleraction()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            stackAuth()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            calling(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ActionSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.materialisationList()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        sufficientFromSubviews()
        //: setupSubViewsConstraint()
        restrictionEnd()
        //: initNotificationObserver()
        big()
        //: req_callGetUserInfo()
        appBy()
        //: timeoutRules()
        gallery()
        //: setUpBGMPlayer()
        contentView()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.needText()
        }
    
		if var viewEqualValue = videoManager.infoModel?.logId { 
	            if (bgVideoView.renderView.restorationIdentifier != nil) && (bgVideoView.renderView.textInputContextIdentifier != nil) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let tabSave = ImageIndicatorView()
	            tabSave.duringCloseMagnitude = interactionView.giftView.contentHeight
	            
	            tabSave.postAtSum = { [self] dataManagerNumber in
	            self.frameCaptureMagnitude = dataManagerNumber
	            
	            var tabSave = viewEqualValue
	                tabSave -= 1
	                if Int(tabSave) > -17 {
	                    tabSave = tabSave + 1
	                }
	            if tabSave > self.frameCaptureMagnitude {
	                self.frameCaptureMagnitude = tabSave
	            }
	            
	            return self.frameCaptureMagnitude
	            }
	            tabSave.textCount = { [self] aggregationQuantity in
	            self.colorTotal = aggregationQuantity
	            
	            var tabSave = interactionView.giftView.contentHeight
	                tabSave += 1
	                if tabSave != 89 {
	                    tabSave = tabSave - 1
	                }
	            if tabSave < self.colorTotal {
	                self.colorTotal = tabSave
	            }
	            
	            self.colorTotal /= 6
	            return self.colorTotal
	            }
	            tabSave.recognizeViewArray = { [self] modelCameraArray in
	            self.timeArray = modelCameraArray
	            
	            guard var value = self.timeArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            tabSave.modelDictionary = { [self] midMeDictionary in
	            self.statisticalTableDictionary = midMeDictionary
	            
	            guard var value = self.statisticalTableDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                bgVideoView.renderView.addSubview(tabSave)
	            }
	
		}
	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RegularBeginDecisionMakerAppManager.share.appConfigMode.disableShootScreen {
            if RegularBeginDecisionMakerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ToShieldView(frame: CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isShared = false
        ActionSocketDelegate.shared.isShared = false
        //: self.stopVCAllActivties()
        self.underside()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: HeadSpeedReactiveCompatible = {
        //: let v = TalkingBGVideoView()
        let v = HeadSpeedReactiveCompatible()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.map(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: StreamPlayerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = StreamPlayerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: AddFrameWindowView = {
        //: let v = TalkingVideoWindowView()
        let v = AddFrameWindowView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = notiEnabledRecordUrl - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if AddBeautyThen.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: showPlainValue + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.plugSumoPath()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: PrepareReactiveCompatible = {
        //: let m = TalkingVideoChatManager()
        let m = PrepareReactiveCompatible()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension PlayerCallerDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func appBy() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        PrepareReactiveCompatible.spectralColourCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: data_actionKey.map{labIndexBottom(color: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(main_rawStr))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(appFrameValue))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(userFileName.lowercased())].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(appNoseStr.replacingOccurrences(of: "player", with: "v") + String(notiLivePath))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(app_atHomeTitle))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(main_videoColorName.replacingOccurrences(of: "white", with: "i"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.retrieveCorner()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.refreshStatus()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func refreshStatus() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = ClickCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = ClickCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.showViewShutterCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.refreshEqual(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension PlayerCallerDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func stackAuth() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        PenumbraReactiveCompatible.behindModel { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.statusTo(showMsg: const_infoKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ActionSocketDelegate.shared.tabularArray(info: [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (String(mainPicStr) + String(const_sizeName)), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: data_actionKey.map{labIndexBottom(color: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.messageAcross()
                //: return
                return
            }

            //: self.startPreview()
            self.needText()
            //: self.stopBGM()
            self.withImage()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = UserDataPlayListener.hollerithCard(type: .Response)
            //: self.startBGM()
            self.someTotaleraction()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func needText() {
        //: self.videoManager.startPreview()
        self.videoManager.formatBefore()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func underside() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.withImage()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        AtClickDanmuManager.hypothesis()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ActionSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ActionSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ActionSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func messageAcross(animated: Bool = true) {
        //: self.stopBGM()
        self.withImage()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func plugSumoPath() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.system()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.refreshStatus()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func conjureUpToEvent() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.barSinister()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.refreshStatus()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.colorAnimation()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func colorAnimation() {
        //: guard RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = EffectScreenView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.withSendAnimation()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension PlayerCallerDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func big() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(finishEnable(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(cypher(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: app_halfMaxName,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(root(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: main_keyFormat,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kHalfTableMessage.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func finishEnable(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            messageAcross()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func root(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ActionSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ActionSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ActionSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ActionSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.restartShould()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func cypher(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.refreshStatus()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension PlayerCallerDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.statusTo(showMsg: (String(const_sexName.prefix(5)) + show_exposeMakeMsg.replacingOccurrences(of: "progress", with: "in") + appWithTitle.replacingOccurrences(of: "current", with: "rr")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ActionSocketDelegate.shared.tabularArray(info: [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (String(show_itemViewStr) + k_draftMessage.replacingOccurrences(of: "size", with: "l")), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: app_clickUrl.map{nameValue(moment: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ActionSocketDelegate.shared.tabularArray(info: [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (app_contentStr.replacingOccurrences(of: "top", with: "ca") + String(kNextPartyPath.suffix(7))), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: app_clickUrl.map{nameValue(moment: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ActionSocketDelegate.shared.tabularArray(info: [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (String(mainPicStr) + String(const_sizeName)), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: data_actionKey.map{labIndexBottom(color: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.expose()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ActionSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            messageAcross()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension PlayerCallerDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func contentView() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? GenerateStrideable.Call : GenerateStrideable.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = UserDataPlayListener.hollerithCard(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func someTotaleraction() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.burnPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func withImage() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.coverInsideModel()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension PlayerCallerDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func gallery() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? user_netName : self.chatModel?.strokeVia()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            talkApp()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.talkApp()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func talkApp() {
        //: guard !TalkingSocketManager.shared.isShared else { return }
        guard !ActionSocketDelegate.shared.isShared else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.statusTo(showMsg: (String(noti_featureViewValue.prefix(5)) + String(constSmallKey)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (app_contentStr.replacingOccurrences(of: "top", with: "ca") + String(kNextPartyPath.suffix(7))), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: app_clickUrl.map{nameValue(moment: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (String(mainPicStr) + String(const_sizeName)), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: data_actionKey.map{labIndexBottom(color: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ActionSocketDelegate.shared.tabularArray(info: socketDict)
        //: popCurrentViewController()
        messageAcross()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension PlayerCallerDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func dataPhoto(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: app_clickUrl.map{nameValue(moment: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(user_labMsg) + showTargetFormat.lowercased())].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.limitTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.voiceConstraintStage()
            //: createTalkRoom()
            conjureUpToEvent()
            //: self.stopBGM()
            self.withImage()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func calling(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.system()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.map(show: false)
        //: self.stopBGM()
        self.withImage()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.voiceConstraintStage()
        //: self.createTalkRoom()
        self.conjureUpToEvent()
    }
}

// MARK: - EvaluationObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension PlayerCallerDelegate: EvaluationObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func endSchedule(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(app_addCenterPath) + String(const_toText.prefix(6))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(dataPicRequestColorMsg) + appSectionFormat.replacingOccurrences(of: "equal", with: "x"))].stringValue == ScoreScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_imageValue.suffix(6)) + " hung " + String(noti_shareAppContent)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.statusTo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.expose()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ActionSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        messageAcross()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func pullUpShort(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        calling(logId: json[String(bytes: app_clickUrl.map{nameValue(moment: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func running(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(showPlayerObjectAttachName.lowercased() + String(appLabelMessage))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(app_addCenterPath) + String(const_toText.prefix(6))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(userEqualData.prefix(7)))].stringValue == ScoreScalarLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(const_imageValue.suffix(6)) + " hung " + String(noti_shareAppContent)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: dataDirectionMsg.map{quantityeractionName(bar: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(user_makeToUrl.suffix(5)) + "ther h" + String(main_indexLabUrl.suffix(4)) + "up the p" + kSourceData.replacingOccurrences(of: "window", with: "on")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.statusTo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.expose()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ActionSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.buttonSizeDismiss()
        //: popCurrentViewController()
        messageAcross()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func dataText(msg: String, data: [String: Any]) {
        //: guard RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(dataManagerPathContent.replacingOccurrences(of: "m", with: "e"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(constAddTitle))] as? String else { return }
            //: let httpsUrl = RegularBeginDecisionMakerAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = RegularBeginDecisionMakerAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = FrameControlConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: ManageressThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            ManageressThen.share.barbLab(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ButtonTargetReactiveCompatible.doState(title: nil,
                                     //: message: msg,
                                     message: msg,
                                     //: leftBtnTitle: "Cancel".localized,
                                     leftBtnTitle: (String(notiCenterTimeKitMessage.prefix(6))).localized,
                                     //: rightBtnTitle: "Get gold coins".localized) {
                                     rightBtnTitle: (String(app_fillId) + String(showModelTitle)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ButtonTargetReactiveCompatible.obnubilateBlockAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: ManageressThen.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                ManageressThen.share.themeParams(clickEvent: dataClickScreenMsg, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func curriculum(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.nurseLogAction(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func christen(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(noti_dataFormat))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.appRemove(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.ingredientFull(point: addBean)
        }
    }
}

// MARK: - PathSuiteObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension PlayerCallerDelegate: PathSuiteObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func pointData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(app_addCenterPath) + String(const_toText.prefix(6))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(showPendingEqualCenterKey))].stringValue == ScoreScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_imageValue.suffix(6)) + " hung " + String(noti_shareAppContent)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.statusTo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.expose()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            ActionSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        messageAcross()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func placeToData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        calling(logId: json[String(bytes: app_clickUrl.map{nameValue(moment: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - ConnectAtErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension PlayerCallerDelegate: ConnectAtErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func heed(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(mainPicStr) + String(const_sizeName)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.statusTo(showMsg: (String(noti_featureViewValue.prefix(5)) + String(constSmallKey)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.expose()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                ActionSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            messageAcross()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(app_yourName) + String(appListKey)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.normal.rawValue else { return }
            //: ManageressThen.share.func__jumpToWebRecharge(sufficient: false)
            ManageressThen.share.themeParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(app_yourName) + String(appListKey)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.statusTo(showMsg: msg)
            //: popCurrentViewController()
            messageAcross()
        }
    }
}

// MARK: - ActionObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension PlayerCallerDelegate: ActionObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func upwardsMoment() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ActionSocketDelegate.shared.tabularArray(info: [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (String(show_shouldContent.suffix(8)) + showFileFormat.replacingOccurrences(of: "center", with: "l")), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: data_actionKey.map{labIndexBottom(color: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func nextShould() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.statusTo(showMsg: (String(mainExtraMsg.suffix(4)) + "s are" + String(main_indexToId)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ActionSocketDelegate.shared.tabularArray(info: [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (app_contentStr.replacingOccurrences(of: "top", with: "ca") + String(kNextPartyPath.suffix(7))), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: app_clickUrl.map{nameValue(moment: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.statusTo(showMsg: (String(mainExtraMsg.suffix(4)) + "s are" + String(main_indexToId)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ActionSocketDelegate.shared.tabularArray(info: [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (String(mainPicStr) + String(const_sizeName)), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: data_actionKey.map{labIndexBottom(color: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.statusTo(showMsg: (String(appChoiceContent.suffix(5)) + "all ha" + String(constRegularKey.suffix(7))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ActionSocketDelegate.shared.tabularArray(info: [String(bytes: k_titlePath.map{$0^40}, encoding: .utf8)!: (String(show_itemViewStr) + k_draftMessage.replacingOccurrences(of: "size", with: "l")), String(bytes: constFrontId.map{listValue(old: $0)}, encoding: .utf8)!: [String(bytes: app_clickUrl.map{nameValue(moment: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        messageAcross()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func lowReport(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.styleCity(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func recordRequest(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.map(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.onPhone(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.refreshStatus()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func skip() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        ActionSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = VisualCommunicationReactiveCompatible.notTotaleractionLocation()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.liveVoice()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.duringUser()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.firmness()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        messageAcross()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func colorSucceed() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension PlayerCallerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func sufficientFromSubviews() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.modelText(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func restrictionEnd() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
