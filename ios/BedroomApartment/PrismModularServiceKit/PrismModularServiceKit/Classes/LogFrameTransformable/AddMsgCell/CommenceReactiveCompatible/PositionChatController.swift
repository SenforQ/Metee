
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showColorTextValue:[UInt8] = [0x75,0x72,0x75,0x68,0x34,0x7f,0x73,0x78,0x79,0x6e,0x26,0x35,0x3c,0x74,0x7d,0x6f,0x3c,0x72,0x73,0x68,0x3c,0x7e,0x79,0x79,0x72,0x3c,0x75,0x71,0x6c,0x70,0x79,0x71,0x79,0x72,0x68,0x79,0x78]

private func tabularArray(render num: UInt8) -> UInt8 {
    return num ^ 28
}

/*: "roomId" :*/
fileprivate let notiTimeName:String = "model intimate self labroomId"

/*: "type" :*/
fileprivate let appTopText:[UInt8] = [0x7b,0x80,0x77,0x6c]

fileprivate func monthSize(status num: UInt8) -> UInt8 {
    let value = Int(num) - 7
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "extra" :*/
fileprivate let constCellStr:String = "exphotora"

/*: "jumpKey" :*/
fileprivate let notiLabelId:[UInt8] = [0x48,0x53,0x4b,0x4e,0x29,0x43,0x57]

fileprivate func equalActual(gift num: UInt8) -> UInt8 {
    let value = Int(num) + 34
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Send Gift" :*/
fileprivate let mainViewId:[Character] = ["S","e","n","d"," ","G","i"]
fileprivate let mainBetweenName:[Character] = ["f","t"]

/*: "View info" :*/
fileprivate let user_rawId:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Report" :*/
fileprivate let userManagerButtonMessage:[Character] = ["R","e","p","o","r","t"]

/*: "uid" :*/
fileprivate let user_sexPath:[UInt8] = [0x70,0x6c,0x61]

private func labelCell(view num: UInt8) -> UInt8 {
    return num ^ 5
}

/*: "name" :*/
fileprivate let appMarginData:[UInt8] = [0xab,0x9e,0xaa,0xa2]

fileprivate func equalAppFlushFee(edit num: UInt8) -> UInt8 {
    let value = Int(num) + 195
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't send blank message" :*/
fileprivate let userGoldData:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a"]
fileprivate let appModelTitle:String = "on path responsenk "
fileprivate let userStateUrl:String = "messtope"

/*: "user" :*/
fileprivate let userNameStr:[Character] = ["u","s","e","r"]

/*: "content" :*/
fileprivate let data_kitTitle:[UInt8] = [0x71,0x7d,0x7c,0x66,0x77,0x7c,0x66]

private func infoText(view num: UInt8) -> UInt8 {
    return num ^ 18
}

/*: "msgType" :*/
fileprivate let kGameUrl:String = "msgTyplabel photo view to equal"
fileprivate let dataPopMessage:String = "request"

/*: "txt" :*/
fileprivate let const_cellId:String = "systemxsystem"

/*: "isGroup" :*/
fileprivate let constWhiteData:String = "self wrap model equal makeisGro"
fileprivate let appWillShowId:String = "king"

/*: "jumps" :*/
fileprivate let dataRemoveWindowStr:String = "juindexps"

/*: "message" :*/
fileprivate let const_userPath:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let appCurrentKey:String = "limit"
fileprivate let appErrorIconMsg:String = "xtMsgelse to tool temp type"

/*: "toUid" :*/
fileprivate let constCornerData:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let k_arrayTitle:String = "gray self in transformmsgInfo"

/*: "params" :*/
fileprivate let k_appFormat:[UInt8] = [0x76,0x67,0x74,0x67,0x6b,0x75]

private func voiceModel(time num: UInt8) -> UInt8 {
    return num ^ 6
}

/*: "contentType" :*/
fileprivate let show_barData:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let constBackGestureValue:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let user_aspectKey:String = "pathmg"

/*: "imageUri" :*/
fileprivate let notiEqualId:[UInt8] = [0x32,0x36,0x2a,0x30,0x2e,0x1e,0x3b,0x32]

fileprivate func viewScaleBar(list num: UInt8) -> UInt8 {
    let value = Int(num) + 55
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let noti_finishTextPath:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let const_pathMessage:String = "create return removemsgId"

/*: "totalMfCoin" :*/
fileprivate let constAddMessage:String = "constraint"
fileprivate let appPhotoKey:String = "otalMfplace class"

/*: "giftId" :*/
fileprivate let k_modelValue:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let main_imageMessage:String = "catch momentgiftNum"

/*: "pkgItemsetId" :*/
fileprivate let main_contentUrl:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "msg" :*/
fileprivate let appAttachFormat:String = "msappear"

/*: "tips" :*/
fileprivate let show_photoBackgroundData:String = "tipinterval"

/*: "gift" :*/
fileprivate let data_sectionRenderMessage:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let dataFileKey:[Character] = ["n"]
fileprivate let main_likeVersionId:String = "icknamstatus"

/*: "headPic" :*/
fileprivate let const_sectionPath:[UInt8] = [0x88,0x85,0x81,0x84,0xb0,0x89,0x83]

private func startAdd(asset num: UInt8) -> UInt8 {
    return num ^ 224
}

/*: "All Numbers" :*/
fileprivate let appActionKey:[Character] = ["A","l","l"," ","N","u","m","b","e","r","s"]

/*: "sendNum" :*/
fileprivate let appAddFormat:[Character] = ["s","e","n","d","N","u","m"]

/*: "toUser" :*/
fileprivate let userErrorStr:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let mainTextUrl:String = "msgInormal first equal gift"

/*: "isConnection" :*/
fileprivate let dataColorMessage:String = "isConneuser called center make"
fileprivate let app_imageValue:String = "CTION"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PositionChatController.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class PositionChatController: ScoreViewController {
	var sectionBelowMagnitude: Double = -52.0
	var ageTitle: String = "style"
	var lifeStyleDictionary: [AnyHashable: String] = [:]
	var cowlTitle: String = "measurement"
	var finishArray: [AnyHashable] = []
	var sandbagTotal: Double = -92.8
	var toolName: String = "medium"
	var magnitudeDictionary: [AnyHashable: String] = [:]

    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = CommenceReactiveCompatible() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = RegularConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = CommenceReactiveCompatible.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showColorTextValue.map{tabularArray(render: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RegularBeginDecisionMakerAppManager.share.appConfigMode.disableShootScreen {
            if RegularBeginDecisionMakerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ToShieldView(frame: CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        controlEqual()
        //: createUIConstraints()
        constraintsFather()
        //: bindInteraction()
        objectCurrent()
        //: addTapGestureRecognizer()
        infoEqual()
        //: func__chatRoom_insertTipMsg()
        chatOn()
        //: chatSettings()
        nameSettings()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            taskAd()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    
            if (self.disablesAutomaticKeyboardDismissal) && (self.isBeingPresented && !self.isViewLoaded) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let keyCorner = ResumeController()
            keyCorner.futurismSwitch = msgInputView.showOtherInputView
            keyCorner.activityMagnitude = { [self] errorNumber in
            self.sectionBelowMagnitude = errorNumber
            
            var keyCorner = msgInputView.inputTextView.placeholderLeftOffset
            keyCorner = 0
            if keyCorner > self.sectionBelowMagnitude {
                self.sectionBelowMagnitude = keyCorner
            }
            
            return self.sectionBelowMagnitude
            }
            keyCorner.quoteUpTitle = { [self] handleIntervalerestTranslationName in
            self.ageTitle = handleIntervalerestTranslationName
            
            var keyCorner = msgInputView.chatRoomID

            if keyCorner.allSatisfy({ $0.isCurrencySymbol }) {
                keyCorner.removeLast()
            }
            if keyCorner.contains(self.ageTitle) {
                self.ageTitle = keyCorner
            }
            
            self.ageTitle.append(contentsOf: self.ageTitle.uppercased() + "collection")
            return self.ageTitle
            }
            keyCorner.editDictionary = { [self] willReportDictionary in
            self.lifeStyleDictionary = willReportDictionary
            
            guard var value = self.lifeStyleDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.navigationController?.present(keyCorner.self, animated: false) { [self] in
                }
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    
            if (giftTrackView.layer.contentsRect.size.height != 1) && (giftTrackView.layoutGuides.count == 35) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let changeLimit = ResumeView()
            changeLimit.connectEnable = animated
            changeLimit.menuCount = self.infoModel.identity
            changeLimit.cellQuantity = msgInputView.inputTextView.placeholderLeftOffset
            changeLimit.beautyName = conversationData.conversationID
            changeLimit.collectionContent = { [self] handleIntervalerestTranslationName in
            self.cowlTitle = handleIntervalerestTranslationName
            
            var changeLimit = infoModel.name
            changeLimit.insert("}", at: changeLimit.startIndex)
            if changeLimit.contains(self.cowlTitle) {
                self.cowlTitle = changeLimit
            }
            
            return self.cowlTitle
            }
            changeLimit.sectionArray = { [self] withClickArray in
            self.finishArray = withClickArray
            
            guard var value = self.finishArray as? [String] else {
                return nil
            }
            return value
            }
                giftTrackView.addSubview(changeLimit)
            }

	}

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.telecasting(join: false)
        //: self.destroyTipsTimer()
        self.active()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: ResumeTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ResumeTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(VoicePlayMsgCell.self, forCellReuseIdentifier: VoicePlayMsgCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(IncrementalCostView.self, forCellReuseIdentifier: IncrementalCostView.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(AddMsgCell.self, forCellReuseIdentifier: AddMsgCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(RegularMsgCell.self, forCellReuseIdentifier: RegularMsgCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: EqualGlobalView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(SubMessageCell.self, forCellReuseIdentifier: EqualGlobalView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: CleanViewDelegate = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = CleanViewDelegate(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: ReverberationThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ReverberationThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: ContactThen = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = ContactThen()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: WiseNameView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = WiseNameView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: ButtonMeView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = ButtonMeView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension PositionChatController {
    /// 群聊设置
    //: private func chatSettings() {
    private func nameSettings() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        TableConnectionThen.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        telecasting(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func telecasting(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(notiTimeName.suffix(6))): self.conversationData.groupID, String(bytes: appTopText.map{monthSize(status: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: PictureRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        PictureRequestTool.cellCompletion(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - AttachTotal

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension PositionChatController: AttachTotal {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> QuantityeractionReactiveCompatible! {
    func messageController(_: ResumeTableViewController!, onNewMessage data: V2TIMMessage!) -> QuantityeractionReactiveCompatible! {
        //: let dic = RoomsChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = RoomsChatManager.visualProperty(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = TableChatManager.tantamount(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: QuantityeractionReactiveCompatible!) -> EqualGlobalView! {
    func messageController(_: ResumeTableViewController!, onShowMessageData data: QuantityeractionReactiveCompatible!) -> EqualGlobalView! {
        //: if data.isKind(of: PathCellData.self) {
        if data.isKind(of: PathCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = VoicePlayMsgCell(style: .default, reuseIdentifier: VoicePlayMsgCell.className())
            //: let cellData = data as! PathCellData
            let cellData = data as! PathCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.kind(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.translateMsg(withCellData: cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: SuiteCellData.self) {
        if data.isKind(of: SuiteCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = IncrementalCostView(style: .default, reuseIdentifier: IncrementalCostView.className())
            //: let cellData = data as! SuiteCellData
            let cellData = data as! SuiteCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: PtolemaicSystemCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = AddMsgCell(style: .default, reuseIdentifier: AddMsgCell.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! PtolemaicSystemCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: TipsReactiveCompatible.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = RegularMsgCell(style: .default, reuseIdentifier: RegularMsgCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! TipsReactiveCompatible
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: EqualGlobalView!) {
    func messageController(_: ResumeTableViewController!, onSelectMessageAvatar cell: EqualGlobalView!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(RegularBeginDecisionMakerAppManager.share.loginUid) {
            if userModel.uid == Int(RegularBeginDecisionMakerAppManager.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                kind(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                compare(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: EqualGlobalView!) {
    func messageController(_: ResumeTableViewController!, onSelectMessageContent cell: EqualGlobalView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: IncrementalCostView.self) { // 图片
            //: let imageCellData = cell.messageData as! SuiteCellData
            let imageCellData = cell.messageData as! SuiteCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(show_barId)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = DotTransformable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = QueryEnableCardTypifySunModelType()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [DotTransformable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = PhotosRecognizerDelegate(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.firmness()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: VoicePlayMsgCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! VoicePlayMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = RoomsChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = RoomsChatManager.visualProperty(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(constCellStr.replacingOccurrences(of: "photo", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: notiLabelId.map{equalActual(gift: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PopMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ManageressThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ManageressThen.share.contextModel(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: RegularMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! RegularMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = RoomsChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = RoomsChatManager.visualProperty(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(constCellStr.replacingOccurrences(of: "photo", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: notiLabelId.map{equalActual(gift: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PopMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ManageressThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ManageressThen.share.contextModel(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: EqualGlobalView!) {
    func messageController(_: ResumeTableViewController!, onRetryMessageContent cell: EqualGlobalView!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        collectionAcrossSend(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension PositionChatController {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func active() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func compare(user: WrapModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = PlaySuiteReactiveCompatible(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.statuteName(cellTitleList: [(String(mainViewId) + String(mainBetweenName)).localized, "@", (String(user_rawId)).localized, (String(userManagerButtonMessage)).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.beyondLoad(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.resign(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.kind(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.statusUid(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func beyondLoad(user: WrapModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.overArcBtn()
        //: let model = TalkingChatRoomMemberModel()
        let model = ResistanceReactiveCompatible()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.honkyTonkModel(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func resign(user: WrapModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: user_sexPath.map{labelCell(view: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: appMarginData.map{equalAppFlushFee(edit: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.dict(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func kind(uid: Int) {
        //: ManageressThen.share.func__pushToUserDetailVC(uid: String(uid))
        ManageressThen.share.bigness(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func statusUid(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = FromShareVisualImageViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.chemicalElement(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension PositionChatController {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func stateStopModel(msgStr: String, quoteModel _: MinutiaModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            statusTo(showMsg: (String(userGoldData) + String(appModelTitle.suffix(3)) + userStateUrl.replacingOccurrences(of: "top", with: "ag")).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: notiLabelId.map{equalActual(gift: $0)}, encoding: .utf8)!: (String(userNameStr)), String(bytes: user_sexPath.map{labelCell(view: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: data_kitTitle.map{infoText(view: $0)}, encoding: .utf8)!: temStr, (String(kGameUrl.prefix(6)) + dataPopMessage.replacingOccurrences(of: "request", with: "e")): (const_cellId.replacingOccurrences(of: "system", with: "t")), (String(constWhiteData.suffix(5)) + appWillShowId.replacingOccurrences(of: "king", with: "up")): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(dataRemoveWindowStr.replacingOccurrences(of: "index", with: "m"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: const_userPath.reversed(), encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: appTopText.map{monthSize(status: $0)}, encoding: .utf8)!: (appCurrentKey.replacingOccurrences(of: "limit", with: "T") + String(appErrorIconMsg.prefix(5))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(notiTimeName.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(constCornerData))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(k_arrayTitle.suffix(7))): msgInfo, String(bytes: k_appFormat.map{voiceModel(time: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            collectionAcrossSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func enableBeginChange(img: UIImage) {
        //: let imagePath = SuiteCellData.getChatImagePath(image: img)
        let imagePath = SuiteCellData.addAcross(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: show_barData.reversed(), encoding: .utf8)!: (String(constBackGestureValue)), (String(kGameUrl.prefix(6)) + dataPopMessage.replacingOccurrences(of: "request", with: "e")): (user_aspectKey.replacingOccurrences(of: "path", with: "i")), String(bytes: notiEqualId.map{viewScaleBar(list: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(noti_finishTextPath)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: appTopText.map{monthSize(status: $0)}, encoding: .utf8)!: (String(constBackGestureValue)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(notiTimeName.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(k_arrayTitle.suffix(7))): msgInfo, String(bytes: k_appFormat.map{voiceModel(time: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            collectionAcrossSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: QuantityeractionReactiveCompatible, reSend: Bool) {
    private func collectionAcrossSend(msgData: QuantityeractionReactiveCompatible, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            statusTo(showMsg: constInputText)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.sendCustomMessage(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(noti_finishTextPath))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(noti_finishTextPath))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(noti_finishTextPath))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: PictureRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        PictureRequestTool.needAction(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.viewResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(k_arrayTitle.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(msgData, msgId: msgInfo[(String(const_pathMessage.suffix(5)))] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((constAddMessage.replacingOccurrences(of: "constraint", with: "t") + String(appPhotoKey.prefix(6)) + "Coin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(constAddMessage.replacingOccurrences(of: "constraint", with: "t") + String(appPhotoKey.prefix(6)) + "Coin")] as! NSNumber
                //: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func viewResend(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard RegularBeginDecisionMakerAppManager.share.loginUserMode.status != 1 else {
            guard RegularBeginDecisionMakerAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    statusTo(showMsg: errorStr)
                }
                //: return
                return
            }

            //: ManageressThen.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            ManageressThen.share.themeParams(clickEvent: kDismissSuccessUrl, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.quantityerval()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            countGroupView(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = VoiceReactiveCompatible(frame: CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr))
            //: view.show()
            view.fromTime()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                statusTo(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension PositionChatController: ReadoutViewDelegate {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func labelNum(giftModel _: ModelHeadTransformable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func waitress() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func giftFrom(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = LargeReactiveCompatible()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.dict(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func politeness(actionType: TypeNameConvertible) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            pleasure()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func popLegacy(giftModel _: ModelHeadTransformable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func playModel(giftModel: ModelHeadTransformable, giftNum: String, model: ResistanceReactiveCompatible) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            statusTo(showMsg: constInputText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(constCornerData))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(k_modelValue))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(main_imageMessage.suffix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(notiTimeName.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(main_contentUrl))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: PictureRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        PictureRequestTool.frame(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.viewResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.bringHomeWith(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.exerciseName(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func underDigitizer(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func smallContent(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func circumcise(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.circumcise(tap)
    }

    //: func func__starRecordBtnDown() {}
    func belowPop() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func monopolise(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension PositionChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func pleasure() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        PenumbraReactiveCompatible.toRequest(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = MomentPickTool.infoVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: ManageressThen.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                ManageressThen.share.afootVc()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.enableBeginChange(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func tillUpColor() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        PenumbraReactiveCompatible.sharedAgent(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.enableBeginChange(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension PositionChatController {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func jump(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(notiTimeName.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension PositionChatController {
    //: func func__TipsNotification() {
    func roundColor() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(addFor(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: showGiftUrl,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func addFor(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: TipsReactiveCompatible = userinfo[(appAttachFormat.replacingOccurrences(of: "appear", with: "g"))] as! TipsReactiveCompatible

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func countGroupView(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: data_kitTitle.map{infoText(view: $0)}, encoding: .utf8)!: tipStr, (String(kGameUrl.prefix(6)) + dataPopMessage.replacingOccurrences(of: "request", with: "e")): (show_photoBackgroundData.replacingOccurrences(of: "interval", with: "s"))]
        //: var message: QuantityeractionReactiveCompatible?
        var message: QuantityeractionReactiveCompatible?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = TableChatManager.isoclinic(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func chatOn() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard RegularBeginDecisionMakerAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            guard RegularBeginDecisionMakerAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: RegularBeginDecisionMakerAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.countGroupView(tipStr: RegularBeginDecisionMakerAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension PositionChatController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func exerciseName(extral: [String: Any?], model: ResistanceReactiveCompatible, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(k_arrayTitle.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(constWhiteData.suffix(5)) + appWillShowId.replacingOccurrences(of: "king", with: "up"))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(kGameUrl.prefix(6)) + dataPopMessage.replacingOccurrences(of: "request", with: "e"))] = (String(data_sectionRenderMessage))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(k_arrayTitle.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: user_sexPath.map{labelCell(view: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(dataFileKey) + main_likeVersionId.replacingOccurrences(of: "status", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: const_sectionPath.map{startAdd(asset: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(appActionKey)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(data_sectionRenderMessage))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(data_sectionRenderMessage))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(dataFileKey) + main_likeVersionId.replacingOccurrences(of: "status", with: "e"))] = "\(giftdic[(String(appAddFormat))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(userErrorStr))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TableChatManager.isoclinic(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(mainTextUrl.prefix(4)) + "ncome")) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(mainTextUrl.prefix(4)) + "ncome")] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(const_pathMessage.suffix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        iconModel(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((constAddMessage.replacingOccurrences(of: "constraint", with: "t") + String(appPhotoKey.prefix(6)) + "Coin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(constAddMessage.replacingOccurrences(of: "constraint", with: "t") + String(appPhotoKey.prefix(6)) + "Coin")] as! NSNumber
            //: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.makeDown(needReload: false, mf_coin: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.generate()
    }

    //: func initCachEffectData() {
    func taskAd() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.themeAllWaste(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func iconModel(giftDic: [String: Any], model: ResistanceReactiveCompatible) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.nameBy(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in ControlGiftManager.share.withView() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [BeautyGiftHandyJSON]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.tick(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.asEqual(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func material(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in ControlGiftManager.share.withView() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [BeautyGiftHandyJSON]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.tick(model: giftModel as! ViewAnimatModel)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.makeArr(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension PositionChatController {
    /// 创建UI
    //: private func createUI() {
    private func controlEqual() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func constraintsFather() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(const_changeFormat)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(const_changeFormat)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - constNameValue)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func objectCurrent() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = LargeReactiveCompatible()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! QuantityeractionReactiveCompatible).isKind(of: PathCellData.self) {
                    if (cellData as! QuantityeractionReactiveCompatible).isKind(of: PathCellData.self) {
                        //: let groupCellData = cellData as! PathCellData
                        let groupCellData = cellData as! PathCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(const_pathMessage.suffix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: DirectConversationListener.shared.rx
        DirectConversationListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(dataColorMessage.prefix(7)) + app_imageValue.lowercased()))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
