
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainSizeFormat:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "F5F5F5" :*/
fileprivate let userMonthModelName:String = "Fofof5"

/*: "#128CFF" :*/
fileprivate let dataMomentName:[Character] = ["#"]
fileprivate let notiSendKey:String = "128CFFcolor type time"

/*: "Feedback" :*/
fileprivate let noti_normalStr:String = "Feformat tag false"
fileprivate let appLastText:String = "edbacshow"

/*: "#8370FF" :*/
fileprivate let notiResultCookieTitle:String = "#8370FFfor self manager lock interaction"

/*: "Quick Greeting" :*/
fileprivate let user_instanceMsg:[Character] = ["Q","u","i","c","k"]
fileprivate let data_directionAppTitle:[Character] = [" ","G","r","e","e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let app_centerStr:String = "Beauresume error label add time"
fileprivate let k_packageStr:String = "tnormal"

/*: "url" :*/
fileprivate let showOfData:String = "addrl"

/*: "unlockGift" :*/
fileprivate let dataRequestText:String = "unlockGirender extension mode table size"
fileprivate let appToBackUrl:String = "name"

/*: "%d" :*/
fileprivate let show_viewName:String = "center button self view%d"

/*: "isBurn" :*/
fileprivate let user_youKey:[UInt8] = [0x42,0x58,0x69,0x5e,0x59,0x45]

private func textTalk(mic num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "giftId" :*/
fileprivate let show_cellMsg:String = "giftIdvar then true out name"

/*: "rYMsgType" :*/
fileprivate let appFrameCenterElseFormat:String = "any text else sharedrYMs"
fileprivate let k_addFormat:String = "me"

/*: "GJ:CallCustom" :*/
fileprivate let showLayerPhoneId:[Character] = ["G","J",":","C","a","l","l"]
fileprivate let appColorKey:[Character] = ["C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let mainTabValue:String = "kitang"
fileprivate let main_changePath:String = "eDirfinish model self model event"

/*: "extra" :*/
fileprivate let showSourceMsg:String = "EXTRA"

/*: "msgInfo" :*/
fileprivate let kLineData:String = "in let lab barmsgInfo"

/*: "toUid" :*/
fileprivate let k_giftKey:String = "toUidbeauty make in"

/*: "wantGift" :*/
fileprivate let show_mePath:String = "color viewwantGift"

/*: "jumpKey" :*/
fileprivate let appValueName:String = "jcell"
fileprivate let noti_mainAspectTitle:String = "false send text tompKey"

/*: "truePersonAuth" :*/
fileprivate let const_textName:[Character] = ["t","r","u","e"]
fileprivate let mainUpName:String = "Personimage data var time from"

/*: "logType" :*/
fileprivate let notiCollectionKey:String = "logTypehidden interaction view case"

/*: "msgType" :*/
fileprivate let noti_headKey:[UInt8] = [0x16,0x8,0x1c,0x2f,0x2,0xb,0x1e]

private func whiteUser(low num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "txt" :*/
fileprivate let constMainUrl:String = "TXT"

/*: "type" :*/
fileprivate let show_listKey:[Character] = ["t","y","p","e"]

/*: "QuickMsg" :*/
fileprivate let constViewData:String = "user"
fileprivate let app_titleUrl:String = "else view rawuickMsg"

/*: "params" :*/
fileprivate let noti_filePointTitle:[UInt8] = [0x74,0x65,0x76,0x65,0x69,0x77]

private func labPage(corner num: UInt8) -> UInt8 {
    return num ^ 4
}

/*: "content" :*/
fileprivate let k_quoteName:[UInt8] = [0xc3,0xcf,0xce,0xd4,0xc5,0xce,0xd4]

private func currentOrigin(manager num: UInt8) -> UInt8 {
    return num ^ 160
}

/*: "id" :*/
fileprivate let dataCountervalMsg:[Character] = ["i","d"]

/*: "txtId" :*/
fileprivate let main_logStr:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let appFaceMsg:[UInt8] = [0x34,0x2c,0x3a,0x3a,0x28,0x2e,0x2c]

fileprivate func toolTo(status num: UInt8) -> UInt8 {
    let value = Int(num) + 57
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let k_storagePath:[Character] = ["T"]
fileprivate let data_modelGiftMessage:String = "design viewxtMsg"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let show_indicatorName:[UInt8] = [0xfc,0xc5,0xc3,0xc9,0xcf,0x8a,0xc2,0xcf,0xc6,0xc6,0xc5,0x8a,0xce,0xc5,0xdd,0xc4,0xc6,0xc5,0xcb,0xce,0x8a,0xcc,0xcb,0xc3,0xc6,0xcf,0xce,0x84,0x8a,0xfa,0xd8,0xc5,0xc9,0xcf,0xce,0xdf,0xd8,0xcf]

/*: "length" :*/
fileprivate let showPathTitle:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let noti_timeKey:String = "adddio"

/*: "audioId" :*/
fileprivate let k_shadowPath:String = "table let crop dataaudioI"
fileprivate let appImageValue:[Character] = ["d"]

/*: "audioLength" :*/
fileprivate let kAppMessage:[UInt8] = [0xfe,0x12,0x1,0x6,0xc,0xe9,0x2,0xb,0x4,0x11,0x5]

fileprivate func errorLayer(moment num: UInt8) -> UInt8 {
    let value = Int(num) - 157
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let constDistanceMsg:String = "succeed var add showaudioS"

/*: "senderId" :*/
fileprivate let mainLastKey:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x6e,0x65,0x73]

/*: "AudioMsg" :*/
fileprivate let appFileValue:String = "type sex event falseAudioMsg"

/*: "Can't send blank message" :*/
fileprivate let dataNativeStr:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"]
fileprivate let user_sendData:String = "now let m to message"

/*: "quoteMsgId" :*/
fileprivate let user_inputUrl:[Character] = ["q","u","o","t","e","M","s"]
fileprivate let const_toTitle:String = "any view record guardgId"

/*: "quoteMsgInfo" :*/
fileprivate let show_headStr:[Character] = ["q","u","o","t","e"]
fileprivate let const_followDataValue:[Character] = ["M","s","g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let notiStarContent:[UInt8] = [0x13,0x1f,0x1e,0x4,0x15,0x1e,0x4,0x24,0x9,0x0,0x15]

private func borderMode(aspect num: UInt8) -> UInt8 {
    return num ^ 112
}

/*: "ImgMsg" :*/
fileprivate let notiCountValue:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let k_lowerModelStr:String = "handleg"

/*: "imageUri" :*/
fileprivate let notiNameToReturnFormat:[UInt8] = [0x97,0x9b,0x8f,0x95,0x93,0x83,0xa0,0x97]

fileprivate func leadingMoment(pop num: UInt8) -> UInt8 {
    let value = Int(num) + 210
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "customSend" :*/
fileprivate let notiManagerValue:[UInt8] = [0xda,0xec,0xea,0xeb,0xe6,0xe4,0xca,0xdc,0xe5,0xdb]

fileprivate func priceAspect(on num: UInt8) -> UInt8 {
    let value = Int(num) - 119
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let kModelShowForKey:[Character] = ["p","i","c"]

/*: "VideoMsg" :*/
fileprivate let user_labTableId:String = "error size raw modeVideoMs"
fileprivate let const_toFormat:String = "G"

/*: "video" :*/
fileprivate let constViewMsg:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let data_frameErrorKey:[UInt8] = [0xaa,0xb6,0xbd,0xac,0xb9,0x90,0xb4,0xae]

fileprivate func contentObject(block num: UInt8) -> UInt8 {
    let value = Int(num) + 185
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "videoUrl" :*/
fileprivate let notiMakeData:[UInt8] = [0xf3,0xec,0xe1,0xe0,0xea,0xd0,0xf7,0xe9]

/*: "duration" :*/
fileprivate let show_detailMessage:[UInt8] = [0x7b,0x8c,0x89,0x78,0x8b,0x80,0x86,0x85]

fileprivate func ptolemaicSystemName(index num: UInt8) -> UInt8 {
    let value = Int(num) - 23
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "len" :*/
fileprivate let data_equalId:[UInt8] = [0x6e,0x65,0x6c]

/*: "Documents/User/voice/" :*/
fileprivate let const_appearColorText:String = "re succeed backgroundDocume"
fileprivate let mainTableMessage:String = "var regularser/vo"

/*: "Documents/User/record/" :*/
fileprivate let noti_profileText:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r"]
fileprivate let app_makeData:[Character] = ["/","r","e","c","o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let showAppName:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "mfBean" :*/
fileprivate let mainCancelKey:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "messageType" :*/
fileprivate let showViewStr:String = "mattachssagattach"
fileprivate let constSharedUrl:String = "play class viewType"

/*: "WantGift" :*/
fileprivate let app_textSizeValue:[Character] = ["W","a","n","t","G"]
fileprivate let mainEquallyText:String = "yett"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let data_imageName:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T","i","p","s","T","i","t"]
fileprivate let show_ofSizePath:[Character] = ["l","e","_"]

/*: "msgId" :*/
fileprivate let kLabelName:String = "by frame selfmsgId"

/*: "totalMfCoin" :*/
fileprivate let noti_ageMsg:String = "view block data right managertotalMf"
fileprivate let data_rangeViewBelowContent:[Character] = ["C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let data_priceUrl:String = "voicetac"
fileprivate let k_contentUrl:[Character] = ["g"]

/*: "intimatePhoto" :*/
fileprivate let app_arrayMakeAtKey:String = "filentfile"
fileprivate let userAddStr:String = "tapp"

/*: _ :*/
fileprivate let main_selectCameraMsg:[Character] = ["_"]

/*: "totalIntimate" :*/
fileprivate let app_keyUrl:String = "totaby"
fileprivate let userNamePath:String = "value return letInti"

/*: "uid" :*/
fileprivate let appStartTitle:[UInt8] = [0xfc,0xe0,0xed]

private func andSo(index num: UInt8) -> UInt8 {
    return num ^ 137
}

/*: "intimate" :*/
fileprivate let showCloseKey:[UInt8] = [0xaa,0xad,0xb7,0xaa,0xae,0xa2,0xb7,0xa6]

private func photoMenu(button num: UInt8) -> UInt8 {
    return num ^ 195
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let show_norUrl:[UInt8] = [0xcc,0xea,0xf7,0xb0,0xfd,0xa9,0xfe,0xfc,0xee,0xa9,0xfd,0xf1,0xee,0xa9,0xef,0xfe,0xf7,0xec,0xfd,0xf2,0xf8,0xf7,0xa9,0xed,0xfe,0xee,0xa9,0xfd,0xf8,0xa9,0x2,0xf8,0xfe,0xfb,0xa9,0xf5,0xea,0xec,0xf4,0xa9,0xf8,0xef,0xa9,0xf2,0xf7,0xfd,0xf2,0xf6,0xea,0xec,0x2,0xa9,0xf5,0xee,0xff,0xee,0xf5,0xb7]

fileprivate func iconImage(title num: UInt8) -> UInt8 {
    let value = Int(num) + 119
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftNum" :*/
fileprivate let noti_shareKey:[Character] = ["g","i","f","t","N","u"]
fileprivate let constContentCustomKey:String = "M"

/*: "pkgItemsetId" :*/
fileprivate let k_imageTurnValue:String = "in raw theme textpkgIte"

/*: "from" :*/
fileprivate let k_keyFormat:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let mainOfFormat:String = "wantGifile intimate for price"
fileprivate let noti_colorMsg:[Character] = ["f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let const_titleFormat:String = "WantGilet add"

/*: "gift" :*/
fileprivate let show_constraintBlockText:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let notiObserverId:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Notes" :*/
fileprivate let kEndMessage:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let noti_objectTitle:String = "Reportleft pic"

/*: "View read receipts?" :*/
fileprivate let app_passData:[Character] = ["V","i","e","w"," ","r","e","a","d"," ","r","e","c","e","i"]
fileprivate let show_loadMakeContentId:[Character] = ["p","t","s","?"]

/*: "He" :*/
fileprivate let show_sexPath:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let kBiologyKey:String = "Sheself make cell"

/*: "%@ is using too low a version" :*/
fileprivate let app_rowName:String = "%@ iself view height"
fileprivate let kSceneStr:String = "female let path sun too "
fileprivate let appSourceKey:String = "versibind"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let user_handleId:String = "Shooequal make model make"
fileprivate let mainRestoreValue:[Character] = ["o"," ","a","t"," "]
fileprivate let dataGiftMessage:String = "countast"
fileprivate let constValueMessage:String = "pathonds"

/*: "Upload failed" :*/
fileprivate let noti_verticalPath:[Character] = ["U","p","l","o"]
fileprivate let mainBecomeId:[Character] = ["a","d"," ","f","a","i","l","e","d"]

/*: "播放状态: :*/
fileprivate let noti_greetData:String = "播放状态:"

/*: "c2c_" :*/
fileprivate let data_sizeEqualPath:String = "c2c_model time height message"

/*: "msg" :*/
fileprivate let constContentId:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let k_playerKey:String = "tuserps"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let kLabToValue:[UInt8] = [0xf4,0xa,0x10,0xbb,0xfe,0xfc,0x9,0xbb,0x10,0xe,0x0,0xbb,0xf,0x3,0x0,0xbb,0x11,0x4,0xff,0x0,0xa,0xbb,0xfe,0x3,0xfc,0xf,0xbb,0x1,0x0,0xfc,0xf,0x10,0xd,0x0,0xbb,0xf,0xa,0xbb,0x2,0x4,0x11,0x0,0xbb,0xc0,0xdb,0xbb,0xfc,0xbb,0xfe,0xfc,0x7,0x7]

fileprivate func theoreticalAccountBiology(table num: UInt8) -> UInt8 {
    let value = Int(num) + 101
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cellData" :*/
fileprivate let app_shareId:String = "view make value strength detailcellData"

/*: "System notification" :*/
fileprivate let const_loadData:[Character] = ["S","y","s","t","e","m"," ","n"]
fileprivate let show_constraintMakeMaleId:[Character] = ["o","t","i","f","i","c","a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ChatButtonViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum StyleFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ChatButtonViewController: ScoreViewController {
	var indoorsDoing: Bool = true
	var pathContent: String = "transport"
	var streetSmartDictionary: [AnyHashable: String] = [:]
	var coverOn: Bool = true
	var incrementalCostName: String = "will"
	var holismDictionary: [AnyHashable: String] = [:]
	var myEnable: Bool = false
	var selectedName: String = "element"
	var viewButtonDictionary: [AnyHashable: String] = [:]

    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = SpeedModelType()
    private var userWrap = SpeedModelType() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = StyleFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: SpeedModelType = SpeedModelType()) {
    init(chatID: String, isFrom: StyleFromEnum = .Normal, userWrap: SpeedModelType = SpeedModelType()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ChatButtonViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        MomentReactiveCompatible.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == ColorDelimitateMacroDefine.getXiaoMiID() {
        if targetId == ColorDelimitateMacroDefine.television() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == ColorDelimitateMacroDefine.getCustomerServiceID() {
        } else if targetId == ColorDelimitateMacroDefine.limpidityAppWork() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainSizeFormat.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RegularBeginDecisionMakerAppManager.share.appConfigMode.disableShootScreen {
            if RegularBeginDecisionMakerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ToShieldView(frame: CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        nearWithAnimation()
        //: createUI()
        constraintUi()
        //: createUIConstraints()
        large()
        //: bindInteraction()
        part()
        //: addTapGestureRecognizer()
        infoEqual()
        //: func__requestChatInfo()
        titleInfo()
        //: func__getFillDraftText()
        overfill()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        UserDataPlayManager.shared.equalStop()
        //: func__addVoiceNotification()
        theRecord()
        //: addReadPeceiptNotification()
        atomicQuantity85()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            taskBlock()
            //: func__TipsNotification()
            mini()
            //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.male.rawValue {
                //: func__setReplyTipsView()
                camera()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        giveAndTake(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        giveAndTake(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            signalingName()
        }
    
            if (bannerView.motionEffects.count == 17) && (bannerView.forFirstBaselineLayout.center.x == 2.07) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let tagLet = TelevisionAwayView(frame: bannerView.frame.offsetBy(dx: CGFloat(0), dy: CGFloat(62)))
            tagLet.topTotalClose = floatScreenView.isFirstChannelEmpty
            tagLet.beautyTitle = self.msgInputView.chatRoomID
            
            
            tagLet.labelOff = { [self] addEnable in
            self.myEnable = addEnable
            
            var tagLet = self.msgInputView.showOtherInputView
                tagLet = true
                tagLet = !tagLet
            if tagLet != self.myEnable {
                self.myEnable = tagLet
            }
            
                self.myEnable = true
                self.myEnable = !self.myEnable
            return self.myEnable
            }
            tagLet.statusTitle = { [self] atTitle in
            self.selectedName = atTitle
            
            var tagLet = userWrap.voiceVIPPrice
            do {
                tagLet = try String(contentsOfFile: tagLet.uppercased() + "party", encoding: .utf8)
            } catch {
                tagLet = error.localizedDescription
            }
            if tagLet.hasSuffix(self.selectedName) {
                self.selectedName = tagLet
            }
            
            self.selectedName += self.selectedName.capitalized + "system"
            return self.selectedName
            }
            tagLet.sumStopDictionary = { [self] toDictionary in
            self.viewButtonDictionary = toDictionary
            
            guard var value = self.viewButtonDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                bannerView.addSubview(tagLet)
            }

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    
		if var needValue = videoCallManager.uid { 
			if var sectionValue = videoCallManager.uid { 
		            if (navView.superview != nil && !navView.isDescendant(of: navView.superview!)) && (navView.backgroundColor != nil && navView.backgroundColor!.cgColor == UIColor.red.cgColor) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let stopTitle = TelevisionAwayView()
		            stopTitle.topTotalClose = animated
		            stopTitle.beautyTitle = sectionValue
		            
		            
		            stopTitle.labelOff = { [self] addEnable in
		            self.coverOn = addEnable
		            
		            var stopTitle = isShowIntimateView
		            stopTitle = false
		            if stopTitle != self.coverOn {
		                self.coverOn = stopTitle
		            }
		            
		            self.coverOn = !self.coverOn
		            return self.coverOn
		            }
		            stopTitle.statusTitle = { [self] atTitle in
		            self.incrementalCostName = atTitle
		            
		            var stopTitle = needValue
		            if #available(iOS 13, *) {
		                if stopTitle.difference(from: stopTitle.lowercased() + "bottom").count == stopTitle.dropLast().count {
		                    stopTitle = ""
		                }
		            }
		            if stopTitle.hasSuffix(self.incrementalCostName) {
		                self.incrementalCostName = stopTitle
		            }
		            
		            let tableString = self.incrementalCostName.uppercased() + "manager"
		            let tableData = tableString.data(using: String.Encoding.utf8)!
		            self.incrementalCostName = String(data: tableData, encoding: .utf8) ?? self.incrementalCostName.uppercased() + "tool"
		            return self.incrementalCostName
		            }
		            stopTitle.sumStopDictionary = { [self] toDictionary in
		            self.holismDictionary = toDictionary
		            
		            guard var value = self.holismDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                navView.addSubview(stopTitle)
		            }
		
			}
		}
	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: ResumeTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ResumeTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (userMonthModelName.replacingOccurrences(of: "of", with: "5F")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(QuantityeractionView.self, forCellReuseIdentifier: QuantityeractionView.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(IncrementalCostView.self, forCellReuseIdentifier: IncrementalCostView.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(MakeViewCell.self, forCellReuseIdentifier: MakeViewCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(PlungeView.self, forCellReuseIdentifier: PlungeView.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(ForTotalViewCell.self, forCellReuseIdentifier: ForTotalViewCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(RegularMsgCell.self, forCellReuseIdentifier: RegularMsgCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(EconomyReactiveCompatible.self, forCellReuseIdentifier: EconomyReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(FaunaThen.self, forCellReuseIdentifier: FaunaThen.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(ClickViewCell.self, forCellReuseIdentifier: ClickViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: EqualGlobalView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(SubMessageCell.self, forCellReuseIdentifier: EqualGlobalView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: RegularMatchDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = RegularMatchDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: CleanViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: NutsAndBoltsViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = CleanViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(dataMomentName) + String(notiSendKey.prefix(6))))!, .font: UIFont.pingfangCenter(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(noti_normalStr.prefix(2)) + appLastText.replacingOccurrences(of: "show", with: "k")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(feedbackPause), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: ReverberationThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ReverberationThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: WiseNameView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = WiseNameView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.pingfangCenter(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(notiResultCookieTitle.prefix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(user_instanceMsg) + String(data_directionAppTitle)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.outsideBecomeColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(moveClick), for: .touchUpInside)
        //: btn.isHidden = !RegularBeginDecisionMakerAppManager.share.appUserConfigMode.showDirectGreet || RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.male.rawValue || RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !RegularBeginDecisionMakerAppManager.share.appUserConfigMode.showDirectGreet || RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.male.rawValue || RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: LogReactiveCompatible? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = LogReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: SuiteControlReactiveCompatible = {
        //: let v = TalkingSelectTranslationView()
        let v = SuiteControlReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.serviceView(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: CheckLikeResultView = {
        //: let v = TalkingTranslationResultView()
        let v = CheckLikeResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.stateStopModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.pingfangCenter(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(notiResultCookieTitle.prefix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(app_centerStr.prefix(4)) + "ty Pho" + k_packageStr.replacingOccurrences(of: "normal", with: "os")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.outsideBecomeColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(assetEqual), for: .touchUpInside)
        //: btn.isHidden = RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.male.rawValue || RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.male.rawValue || RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: ConstrueThen = {
        //: let v = TalkingLiveRoomGamesView()
        let v = ConstrueThen()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: RegularBannerView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = RegularBannerView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: VoiceModelThen = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = VoiceModelThen()
        //: v.isHidden = (RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: CountReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = CountReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.contactBring()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ChatButtonViewController: FromModelReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func colorNow(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.sizeImage(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ChatButtonViewController: AttachTotal {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func moveClick() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        MomentReactiveCompatible.colorFile(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.statusTo(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.funcActionDict(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func assetEqual() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        MomentReactiveCompatible.messageDown(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = ConnectViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().equalFinish(urlStr: dict?[(showOfData.replacingOccurrences(of: "add", with: "u"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(dataRequestText.prefix(8)) + appToBackUrl.replacingOccurrences(of: "name", with: "ft"))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ReplyModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: user_youKey.map{textTalk(mic: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(show_cellMsg.prefix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.wrapUp(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> QuantityeractionReactiveCompatible! {
    func messageController(_: ResumeTableViewController!, onNewMessage data: V2TIMMessage!) -> QuantityeractionReactiveCompatible! {
        //: let dic = RoomsChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = RoomsChatManager.visualProperty(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TableChatManager.tantamount(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ShadowCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(appFrameCenterElseFormat.suffix(4)) + "gTyp" + k_addFormat.replacingOccurrences(of: "me", with: "e"))].stringValue == (String(showLayerPhoneId) + String(appColorKey)) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(mainTabValue.replacingOccurrences(of: "kit", with: "ch") + String(main_changePath.prefix(4)) + "ection")].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(showSourceMsg.lowercased())][(String(kLineData.suffix(7)))][(String(k_giftKey.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ButtonCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ButtonCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: AudioCellData.self) == true ||
        if cellData?.isKind(of: AudioCellData.self) == true ||
            //: cellData?.isKind(of: SuiteCellData.self) == true ||
            cellData?.isKind(of: SuiteCellData.self) == true ||
            //: cellData?.isKind(of: ButtonCellData.self) == true ||
            cellData?.isKind(of: ButtonCellData.self) == true ||
            //: cellData?.isKind(of: CardCellData.self) == true {
            cellData?.isKind(of: CardCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            MomentReactiveCompatible.sizeTime(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            MomentReactiveCompatible.tableOfTexts(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: QuantityeractionReactiveCompatible!) -> EqualGlobalView! {
    func messageController(_: ResumeTableViewController!, onShowMessageData data: QuantityeractionReactiveCompatible!) -> EqualGlobalView! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = filterUp(data: data)

        //: if data.isKind(of: ButtonCellData.self) {
        if data.isKind(of: ButtonCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = QuantityeractionView(style: .default, reuseIdentifier: QuantityeractionView.className())
            //: let cellData = data as! ButtonCellData
            let cellData = data as! ButtonCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
                //: if cellData.msgModel.quoteMsgInfo.uid == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: SuiteCellData.self) {
        if data.isKind(of: SuiteCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = IncrementalCostView(style: .default, reuseIdentifier: IncrementalCostView.className())
            //: let cellData = data as! SuiteCellData
            let cellData = data as! SuiteCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CardCellData.self) {
        if data.isKind(of: CardCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = MakeViewCell(style: .default, reuseIdentifier: MakeViewCell.className())
            //: let cellData = data as! CardCellData
            let cellData = data as! CardCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: DotCellData.self) {
        if data.isKind(of: DotCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = PlungeView(style: .default, reuseIdentifier: PlungeView.className())
            //: let cellData = data as! DotCellData
            let cellData = data as! DotCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: BuildCellData.self) {
        if data.isKind(of: BuildCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = ForTotalViewCell(style: .default, reuseIdentifier: ForTotalViewCell.className())
            //: let cellData = data as! BuildCellData
            let cellData = data as! BuildCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = ModelHeadTransformable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.sumimateBy(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(show_mePath.suffix(8))))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: AudioCellData.self) {
        if data.isKind(of: AudioCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = FaunaThen(style: .default, reuseIdentifier: FaunaThen.className())
            //: let cellData = data as! AudioCellData
            let cellData = data as! AudioCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: ShadowCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = EconomyReactiveCompatible(style: .default, reuseIdentifier: EconomyReactiveCompatible.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! ShadowCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: TipsReactiveCompatible.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = RegularMsgCell(style: .default, reuseIdentifier: RegularMsgCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! TipsReactiveCompatible
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: HypervelocityCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = ClickViewCell(style: .default, reuseIdentifier: ClickViewCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! HypervelocityCellData
            //: cell.fill(with: cellData)
            cell.revolution(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: EqualGlobalView!) {
    func messageController(_: ResumeTableViewController!, onSelectMessageAvatar cell: EqualGlobalView!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != ColorDelimitateMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != ColorDelimitateMacroDefine.limpidityAppWork() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: ManageressThen.share.func__pushToUserDetailVC(uid: String(uid))
            ManageressThen.share.bigness(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: EqualGlobalView!) {
    func messageController(_: ResumeTableViewController!, onSelectMessageContent cell: EqualGlobalView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: IncrementalCostView.self) { // 图片
            //: let imageCellData = cell.messageData as! SuiteCellData
            let imageCellData = cell.messageData as! SuiteCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = FromLikePreviewVc(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.directGiftHumanActivityEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_showData)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.host(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.ofView(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                pullIn()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(show_barId)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = DotTransformable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = QueryEnableCardTypifySunModelType()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [DotTransformable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = PhotosRecognizerDelegate(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                pullIn()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: MakeViewCell.self) { // 视频
            //: let videoCellData = cell.messageData as! CardCellData
            let videoCellData = cell.messageData as! CardCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = DetailRecognizerDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                pullIn()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = KnockDoingEffectViewDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.directGiftHumanActivityEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_showData)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.host(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.ofView(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                pullIn()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: FaunaThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! FaunaThen
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! AudioCellData, cellView: audioCell)
            upBirth(cellData: audioCell.cellData as! AudioCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: QuantityeractionView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! QuantityeractionView
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = RoomsChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = RoomsChatManager.visualProperty(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(showSourceMsg.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((appValueName.replacingOccurrences(of: "cell", with: "u") + String(noti_mainAspectTitle.suffix(5)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(appValueName.replacingOccurrences(of: "cell", with: "u") + String(noti_mainAspectTitle.suffix(5)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(const_textName) + String(mainUpName.prefix(6)) + "Auth") {
                    //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if RegularBeginDecisionMakerAppManager.share.loginUserMode.isTPAuth == SawLogSchemaConvertible.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if RegularBeginDecisionMakerAppManager.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if RegularBeginDecisionMakerAppManager.share.loginUserMode.isTPAuth == SawLogSchemaConvertible.unknown.rawValue {
                        //: ManageressThen.share.func__pushUserVerifyController(toast: nil)
                        ManageressThen.share.barDown(toast: nil)
                        //: } else if RegularBeginDecisionMakerAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if RegularBeginDecisionMakerAppManager.share.loginUserMode.isTPAuth == SawLogSchemaConvertible.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = BeautyRecognizerDelegate()
                        //: ManageressThen.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        ManageressThen.share.afootVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: ManageressThen.share.func__pushUserVerifyController(toast: nil)
                        ManageressThen.share.barDown(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PopMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ManageressThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ManageressThen.share.contextModel(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(appFrameCenterElseFormat.suffix(4)) + "gTyp" + k_addFormat.replacingOccurrences(of: "me", with: "e"))] as? String == (String(showLayerPhoneId) + String(appColorKey)) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(kLineData.suffix(7)))][(String(notiCollectionKey.prefix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    game()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    currentVideo()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: RegularMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! RegularMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = RoomsChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = RoomsChatManager.visualProperty(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(showSourceMsg.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((appValueName.replacingOccurrences(of: "cell", with: "u") + String(noti_mainAspectTitle.suffix(5)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<PopMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ManageressThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ManageressThen.share.contextModel(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: EconomyReactiveCompatible.self) {
            //: ManageressThen.share.func__pushToUserDetailVC(uid: targetId)
            ManageressThen.share.bigness(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: EqualGlobalView!) {
    func messageController(_: ResumeTableViewController!, onRetryMessageContent cell: EqualGlobalView!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: CompartmentCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: IncrementalCostView.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! IncrementalCostView
            //: cellData = imageCell.cellData as! SuiteCellData
            cellData = imageCell.cellData as! SuiteCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: MakeViewCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! MakeViewCell
            //: cellData = videoCell.cellData as! CardCellData
            cellData = videoCell.cellData as! CardCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: FaunaThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! FaunaThen
            //: cellData = audioCell.cellData as! AudioCellData
            cellData = audioCell.cellData as! AudioCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: QuantityeractionView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! QuantityeractionView
            //: cellData = textCell.cellData as! ButtonCellData
            cellData = textCell.cellData as! ButtonCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: QuantityeractionReactiveCompatible? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: QuantityeractionReactiveCompatible? = TableChatManager.quote(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: CardCellData.self) {
                if reCellData!.isKind(of: CardCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    sectionModel(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    nudge(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: QuantityeractionReactiveCompatible!) {
    func messageController(_: ResumeTableViewController!, onReplyMessage cellData: QuantityeractionReactiveCompatible!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = MomentReactiveCompatible.timeWith(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: QuantityeractionReactiveCompatible!) {
    func messageController(_: ResumeTableViewController!, onClickReplyMessage cellData: QuantityeractionReactiveCompatible!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        MomentReactiveCompatible.dataEqual(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: QuantityeractionReactiveCompatible) -> QuantityeractionReactiveCompatible {
    private func filterUp(data: QuantityeractionReactiveCompatible) -> QuantityeractionReactiveCompatible {
        //: let isSelf = data.msgModel.user.uid == Int(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if RegularBeginDecisionMakerAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = RegularBeginDecisionMakerAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = RegularBeginDecisionMakerAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = RegularBeginDecisionMakerAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = RegularBeginDecisionMakerAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func pan() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (constMainUrl.lowercased())]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(show_listKey)): (constViewData.replacingOccurrences(of: "user", with: "Q") + String(app_titleUrl.suffix(7)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kLineData.suffix(7))): msgInfo, String(bytes: noti_filePointTitle.map{labPage(corner: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.nudge(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func funcActionDict(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: k_quoteName.map{currentOrigin(manager: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: k_quoteName.map{currentOrigin(manager: $0)}, encoding: .utf8)!: content, (String(main_logStr)): greetId, String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (constMainUrl.lowercased())]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: appFaceMsg.map{toolTo(status: $0)}, encoding: .utf8)!: content, (String(main_logStr)): greetId, (String(show_listKey)): (String(k_storagePath) + String(data_modelGiftMessage.suffix(5)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(kLineData.suffix(7))): msgInfo, String(bytes: noti_filePointTitle.map{labPage(corner: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.nudge(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            ToSessionDelegate.shared.job(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.statusTo(showMsg: String(bytes: show_indicatorName.map{$0^170}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        MomentReactiveCompatible.viewFinder(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(showPathTitle))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (noti_timeKey.replacingOccurrences(of: "add", with: "au")),
                               //: "audioId": greetId,
                               (String(k_shadowPath.suffix(6)) + String(appImageValue)): greetId,
                               //: "audioLength": length,
                               String(bytes: kAppMessage.map{errorLayer(moment: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(constDistanceMsg.suffix(6)) + "andbox"): model.unZipFilePath,
                               //: "senderId": RegularBeginDecisionMakerAppManager.share.loginUserMode.userID]
                               String(bytes: mainLastKey.reversed(), encoding: .utf8)!: RegularBeginDecisionMakerAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(k_shadowPath.suffix(6)) + String(appImageValue)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: kAppMessage.map{errorLayer(moment: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(constDistanceMsg.suffix(6)) + "andbox"): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(show_listKey)): (String(appFileValue.suffix(8)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(kLineData.suffix(7))): msgInfo, String(bytes: noti_filePointTitle.map{labPage(corner: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.nudge(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func stateStopModel(msgStr: String, quoteModel: MinutiaModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            statusTo(showMsg: (String(dataNativeStr) + String(user_sendData.suffix(8))).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: k_quoteName.map{currentOrigin(manager: $0)}, encoding: .utf8)!: temStr, String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (constMainUrl.lowercased())]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: appFaceMsg.map{toolTo(status: $0)}, encoding: .utf8)!: temStr, (String(show_listKey)): (String(k_storagePath) + String(data_modelGiftMessage.suffix(5)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(kLineData.suffix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(user_inputUrl) + String(const_toTitle.suffix(3)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(show_headStr) + String(const_followDataValue))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: noti_filePointTitle.map{labPage(corner: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? MinutiaModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            nudge(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func wrapUp(img: UIImage, sendModel: ReplyModel) {
        //: let imagePath = SuiteCellData.getChatImagePath(image: img)
        let imagePath = SuiteCellData.addAcross(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: notiStarContent.map{borderMode(aspect: $0)}, encoding: .utf8)!: (String(notiCountValue)), String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (k_lowerModelStr.replacingOccurrences(of: "handle", with: "im")), String(bytes: notiNameToReturnFormat.map{leadingMoment(pop: $0)}, encoding: .utf8)!: imagePath, String(bytes: notiManagerValue.map{priceAspect(on: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(kModelShowForKey)): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(show_listKey)): (String(notiCountValue)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: user_youKey.map{textTalk(mic: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(show_cellMsg.prefix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kLineData.suffix(7))): msgInfo, String(bytes: noti_filePointTitle.map{labPage(corner: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            nudge(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: QuantityeractionReactiveCompatible?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func sectionModel(msgData: QuantityeractionReactiveCompatible?, sendModel: ReplyModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: notiStarContent.map{borderMode(aspect: $0)}, encoding: .utf8)!: (String(user_labTableId.suffix(7)) + const_toFormat.lowercased()), String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (String(constViewMsg)), String(bytes: notiManagerValue.map{priceAspect(on: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(show_listKey)): (String(user_labTableId.suffix(7)) + const_toFormat.lowercased()),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: data_frameErrorKey.map{contentObject(block: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: notiMakeData.map{$0^133}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: show_detailMessage.map{ptolemaicSystemName(index: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: user_youKey.map{textTalk(mic: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(show_cellMsg.prefix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(kLineData.suffix(7))): msgInfo, String(bytes: noti_filePointTitle.map{labPage(corner: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = TableChatManager.isoclinic(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.sendCustomMessage(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.run(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.run(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        nudge(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func monopolise(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (noti_timeKey.replacingOccurrences(of: "add", with: "au")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: kAppMessage.map{errorLayer(moment: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(constDistanceMsg.suffix(6)) + "andbox"): voicePath,
                       //: "senderId": RegularBeginDecisionMakerAppManager.share.loginUserMode.userID]
                       String(bytes: mainLastKey.reversed(), encoding: .utf8)!: RegularBeginDecisionMakerAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(constDistanceMsg.suffix(6)) + "andbox"): voicePath,
                         //: "type": "AudioMsg",
                         (String(show_listKey)): (String(appFileValue.suffix(8))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: data_equalId.reversed(), encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kLineData.suffix(7))): msgInfo, String(bytes: noti_filePointTitle.map{labPage(corner: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            nudge(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: QuantityeractionReactiveCompatible, reSend: Bool) {
    private func nudge(msgData: QuantityeractionReactiveCompatible, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            statusTo(showMsg: constInputText)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(k_giftKey.prefix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: CardCellData.self) == false && msgData.isKind(of: BuildCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: CardCellData.self) == false, msgData.isKind(of: BuildCellData.self) == false, params[(String(show_listKey))] as? String != (constViewData.replacingOccurrences(of: "user", with: "Q") + String(app_titleUrl.suffix(7))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = RoomsChatManager.getMessageInsertTime()
        let recordTime = RoomsChatManager.appVoice()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((String(kModelShowForKey))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(kModelShowForKey))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(kModelShowForKey))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(String(show_listKey))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(appFileValue.suffix(8))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(constDistanceMsg.suffix(6)) + "andbox")) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(constDistanceMsg.suffix(6)) + "andbox")] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(constDistanceMsg.suffix(6)) + "andbox"))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(const_appearColorText.suffix(6)) + "nts/U" + String(mainTableMessage.suffix(6)) + "ice/")) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = const_rejectMineMatchUrl + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(noti_profileText) + String(app_makeData))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = const_buttonMsg + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(k_shadowPath.suffix(6)) + String(appImageValue))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(noti_timeKey.replacingOccurrences(of: "add", with: "au"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: PictureRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        PictureRequestTool.nextContrive(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (constViewData.replacingOccurrences(of: "user", with: "Q") + String(app_titleUrl.suffix(7))) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.directGiftHumanActivityEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kTitleUrl)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(main_logStr))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    MomentReactiveCompatible.viewFinder(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(kLineData.suffix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(showAppName))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(showAppName))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if SearchPushListener.biologyLab().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: appFailText, object: nil, userInfo: [String(bytes: mainCancelKey.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.observerInfo(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(showViewStr.replacingOccurrences(of: "attach", with: "e") + String(constSharedUrl.suffix(4)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (constViewData.replacingOccurrences(of: "user", with: "Q") + String(app_titleUrl.suffix(7))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!] = (String(app_textSizeValue) + mainEquallyText.replacingOccurrences(of: "yet", with: "if"))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    app_serverName.successOrMeanSun(eventID: notiViewValue)
                    //: } else if type == "QuickMsg" {
                } else if type == (constViewData.replacingOccurrences(of: "user", with: "Q") + String(app_titleUrl.suffix(7))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!] = (constMainUrl.lowercased())
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(kLineData.suffix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.sendCustomMessage(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID)" + (String(data_imageName) + String(show_ofSizePath)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.male.rawValue, type != (constViewData.replacingOccurrences(of: "user", with: "Q") + String(app_titleUrl.suffix(7))) {
                //: self.removeReplyTipsView()
                self.takeDoing()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(kLabelName.suffix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            MomentReactiveCompatible.tableOfTexts(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(noti_ageMsg.suffix(7)) + String(data_rangeViewBelowContent))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(noti_ageMsg.suffix(7)) + String(data_rangeViewBelowContent))] as! NSNumber
                //: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((data_priceUrl.replacingOccurrences(of: "voice", with: "at") + "hSysMs" + String(k_contentUrl))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(data_priceUrl.replacingOccurrences(of: "voice", with: "at") + "hSysMs" + String(k_contentUrl))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.iconStr(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((app_arrayMakeAtKey.replacingOccurrences(of: "file", with: "i") + "matePho" + userAddStr.replacingOccurrences(of: "app", with: "o"))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(app_arrayMakeAtKey.replacingOccurrences(of: "file", with: "i") + "matePho" + userAddStr.replacingOccurrences(of: "app", with: "o"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? AlongModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(constViewMsg))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(constViewMsg))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? YankModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        follow()

        //: updateSession()
        currentEqual()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func currentEqual() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        InfoDetailThen.cacheWithinStreetwise(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func directGiftHumanActivityEvent(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard RegularBeginDecisionMakerAppManager.share.loginUserMode.status != 1 else {
            guard RegularBeginDecisionMakerAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    statusTo(showMsg: errorStr)
                }
                //: return
                return
            }

            //: ManageressThen.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            ManageressThen.share.themeParams(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.quantityerval()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = VoiceReactiveCompatible(frame: CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr))
            //: view.show()
            view.fromTime()
            //: } else if errorCode == 60003, !RegularBeginDecisionMakerAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !RegularBeginDecisionMakerAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            TargetThen.shared.creationGift()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                statusTo(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func ofView(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(kLineData.suffix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.observerInfo(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func exceptExhibit() {
        //: if self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.usufructuaryVoice(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (kBarUrl.array(forKey: notiBarTitle)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.cameraAnimation()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                kBarUrl.set(intimateCacheData, forKey: notiBarTitle)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func logOf(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = RoomsChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = RoomsChatManager.visualProperty(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(showSourceMsg.lowercased())].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(kLineData.suffix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(app_keyUrl.replacingOccurrences(of: "by", with: "l") + String(userNamePath.suffix(4)) + "mate")]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.perspective(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func observerInfo(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((app_keyUrl.replacingOccurrences(of: "by", with: "l") + String(userNamePath.suffix(4)) + "mate")), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(app_keyUrl.replacingOccurrences(of: "by", with: "l") + String(userNamePath.suffix(4)) + "mate")] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.perspective(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func perspective(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = InfoDetailThen.get(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.usufructuaryVoice(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.everyUp(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.needMagnitudeimate(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.needMagnitudeimate(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        InfoDetailThen.commentDismiss(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: appStartTitle.map{andSo(index: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: showCloseKey.map{photoMenu(button: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: showDeviceId,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ChatButtonViewController: ReadoutViewDelegate {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func waitress() {
        //: guard RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.album(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func giftFrom(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func politeness(actionType: TypeNameConvertible) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    punctuate()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    regardStr()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                punctuate()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus {
                if RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.cellPrompt(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                regardStr()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    noneShow()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    regardStr()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                noneShow()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.envelop(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func regardStr() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.statusTo(showMsg: String(bytes: show_norUrl.map{iconImage(title: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func playModel(giftModel _: ModelHeadTransformable, giftNum _: String, model _: ResistanceReactiveCompatible) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func popLegacy(giftModel: ModelHeadTransformable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        sumimateBy(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: BuildCellData?, fromStr: String?) {
    func sumimateBy(giftModel: ModelHeadTransformable, giftNum: String, cellData: BuildCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            statusTo(showMsg: constInputText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(k_giftKey.prefix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(show_cellMsg.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(noti_shareKey) + constContentCustomKey.lowercased())] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(k_imageTurnValue.suffix(6)) + "msetId")] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(k_keyFormat))] = fromStr
        }
        // 送礼接口
        //: PictureRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        PictureRequestTool.imageDrop(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.directGiftHumanActivityEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kDismissSuccessUrl)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.bringHomeWith(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.host(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.currentEqual()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(kLineData.suffix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((app_keyUrl.replacingOccurrences(of: "by", with: "l") + String(userNamePath.suffix(4)) + "mate")), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(app_keyUrl.replacingOccurrences(of: "by", with: "l") + String(userNamePath.suffix(4)) + "mate")] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.perspective(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                app_serverName.successOrMeanSun(eventID: main_screenId)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func labelNum(giftModel: ModelHeadTransformable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        app_serverName.successOrMeanSun(eventID: constStatusKey)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            statusTo(showMsg: constInputText)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= RegularBeginDecisionMakerAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            regardStr()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(mainOfFormat.prefix(6)) + String(noti_colorMsg))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(show_listKey))] = (String(const_titleFormat.prefix(6)) + "ftMsg")

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (String(app_textSizeValue) + mainEquallyText.replacingOccurrences(of: "yet", with: "if"))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(kLineData.suffix(7))): msgInfo, String(bytes: noti_filePointTitle.map{labPage(corner: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = TableChatManager.isoclinic(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            nudge(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func underDigitizer(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(showStatusKey - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func smallContent(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func circumcise(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.circumcise(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                vox(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func belowPop() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if UserDataPlayManager.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            UserDataPlayManager.shared.reinIn()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func host(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(kLineData.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!] = (String(show_constraintBlockText))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(kLineData.suffix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TableChatManager.isoclinic(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(showAppName))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(showAppName))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(kLabelName.suffix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        executeModel(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(noti_ageMsg.suffix(7)) + String(data_rangeViewBelowContent))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(noti_ageMsg.suffix(7)) + String(data_rangeViewBelowContent))] as! NSNumber
            //: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.makeDown(needReload: false, mf_coin: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func taskBlock() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.themeAllWaste(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func executeModel(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.asEqual(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func detailArr(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.makeArr(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func upId() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func game() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.mutual { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.currentEqual()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func currentVideo() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.swaddlingClothes { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.currentEqual()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func feedbackPause() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = TidewaterRiverFieldDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        pullIn()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func warpath() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = PlaySuiteReactiveCompatible(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.statuteName(cellTitleList: [(String(notiObserverId)).localized, (String(kEndMessage)).localized, (String(noti_objectTitle.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .census()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: ManageressThen.share.func__pushToUserDetailVC(uid: self.targetId)
                ManageressThen.share.bigness(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                contactBring()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = FromShareVisualImageViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.chemicalElement(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func contactBring() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = HeadViewDelegate()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.topState(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.tightReload(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func saddleMakeMove() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            vox(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            pullIn()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func burn(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = PathInfoScreenModel.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.atIndex(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func atomicQuantity85() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(receipt(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: main_timeValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(restore),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: notiDetailData,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func receipt(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus == true else { return }
        guard RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func restore() {
        //: RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus = true
        RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func targetShould() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? QuantityeractionReactiveCompatible else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? QuantityeractionReactiveCompatible else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if MomentReactiveCompatible.modelData(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.endLab(tipStr: (String(app_passData) + String(show_loadMakeContentId)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ChatButtonViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func punctuate() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        PenumbraReactiveCompatible.toRequest(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = MomentPickTool.infoVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.goForShowPhoto(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func funcUponPhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        PenumbraReactiveCompatible.sharedAgent(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.goForShowPhoto(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func goForShowPhoto(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ReplyModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.wrapUp(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = PhotosRecognizerDelegate(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ReplyModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.wrapUp(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        pullIn()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func noneShow() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard MomentReactiveCompatible.toDismiss(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == ScoreScalarLiteral.male.rawValue ? "He".localized : (String(kBiologyKey.prefix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.statusTo(showMsg: (String(app_rowName.prefix(4)) + "s using" + String(kSceneStr.suffix(5)) + "low a " + appSourceKey.replacingOccurrences(of: "bind", with: "on")).fast(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.sourceHead()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func sourceHead() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = MomentPickTool.infoVideo(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            MomentPickTool.tvCompletion(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.toAGreaterExtentTimeTv(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.statusTo(showMsg: (String(user_handleId.prefix(4)) + "t vide" + String(mainRestoreValue) + dataGiftMessage.replacingOccurrences(of: "count", with: "le") + " 5 s" + constValueMessage.replacingOccurrences(of: "path", with: "ec")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func toAGreaterExtentTimeTv(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: ReadTrackEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = StyleControlViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ReplyModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.sectionModel(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        DescriptionProgressHUD.actionOf()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        pullIn()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: QuantityeractionReactiveCompatible, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func run(msgData: QuantityeractionReactiveCompatible, sendModel: ReplyModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ViewSaveListener.shared.share(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.statusTo(showMsg: (String(noti_verticalPath) + String(mainBecomeId)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.sectionModel(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    //: func func__addVoiceNotification() {
    func theRecord() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(changeActive),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(maleModelBack),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(artefact(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(errorNotification(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: userListMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(logOf(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kQualityUserClickUrl,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func giveAndTake(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            UserDataPlayManager.shared.equalStop()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            UserDataPlayManager.shared.userBankManager()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func changeActive() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        giveAndTake(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func maleModelBack() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        giveAndTake(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func artefact(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            giveAndTake(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            giveAndTake(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: AudioCellData, cellView: TalkingChatAudioMsgCell) {
    func upBirth(cellData: AudioCellData, cellView: FaunaThen) {
        //: guard TalkingSocketManager.shared.isShared == false else {
        guard ActionSocketDelegate.shared.isShared == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.statusTo(showMsg: show_sessionName)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? QuantityeractionReactiveCompatible)!.isKind(of: AudioCellData.self) == false {
            if (indexModel as? QuantityeractionReactiveCompatible)!.isKind(of: AudioCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! AudioCellData
            let audioModel = indexModel as! AudioCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = InfoDetailThen.isochronalMessage(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        UserDataPlayManager.shared.reinIn()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if UserDataPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (noti_greetData.capitalized) + "\(UserDataPlayManager.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if UserDataPlayManager.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                UserDataPlayManager.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        UserDataPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        UserDataPlayManager.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: QuantityeractionReactiveCompatible = self.msgView.uiMsgs.object(at: index) as! QuantityeractionReactiveCompatible
                let subModel: QuantityeractionReactiveCompatible = self.msgView.uiMsgs.object(at: index) as! QuantityeractionReactiveCompatible
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: AudioCellData.self) {
                if subModel.isKind(of: AudioCellData.self) {
                    //: let audioModel = subModel as! AudioCellData
                    let audioModel = subModel as! AudioCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = InfoDetailThen.isochronalMessage(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    ControlThen.modifyPathMsg(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        UserDataPlayManager.shared.inputImagePath(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func titleInfo() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.draftCopy()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.funcActionDict(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? QuantityeractionReactiveCompatible
                let indexModel = model as? QuantityeractionReactiveCompatible
                //: if indexModel?.isKind(of: AudioCellData.self) == true || indexModel?.isKind(of: SuiteCellData.self) == true || indexModel?.isKind(of: ButtonCellData.self) == true || indexModel?.isKind(of: CardCellData.self) == true {
                if indexModel?.isKind(of: AudioCellData.self) == true || indexModel?.isKind(of: SuiteCellData.self) == true || indexModel?.isKind(of: ButtonCellData.self) == true || indexModel?.isKind(of: CardCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.follow()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.reader()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.targetShould()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func draftCopy() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.step(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.tabData(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.exceptExhibit()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == RegularBeginDecisionMakerAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == RegularBeginDecisionMakerAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == RegularBeginDecisionMakerAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == RegularBeginDecisionMakerAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.tightReload(userWrap)
    }

    //: func func__getFillDraftText() {
    func overfill() {
        //: let userID = "c2c_"+targetId
        let userID = (String(data_sizeEqualPath.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.waitress()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func signalingName() {
        //: let userID = "c2c_"+targetId
        let userID = (String(data_sizeEqualPath.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    //: func func__TipsNotification() {
    func mini() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(userNotification(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: showGiftUrl,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func userNotification(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: TipsReactiveCompatible = userinfo[(String(constContentId))] as! TipsReactiveCompatible

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func endLab(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: k_quoteName.map{currentOrigin(manager: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: noti_headKey.map{whiteUser(low: $0)}, encoding: .utf8)!: (k_playerKey.replacingOccurrences(of: "user", with: "i"))]
        //: var message: QuantityeractionReactiveCompatible?
        var message: QuantityeractionReactiveCompatible?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TableChatManager.terminal(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = TableChatManager.isoclinic(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func reader() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        MomentReactiveCompatible.nap { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.endLab(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func iconStr(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.endLab(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func follow() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        MomentReactiveCompatible.signatureIn(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.endLab(tipStr: String(bytes: kLabToValue.map{theoreticalAccountBiology(table: $0)}, encoding: .utf8)!.fast(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func errorNotification(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: QuantityeractionReactiveCompatible = dict?["cellData"] as! QuantityeractionReactiveCompatible
        let cellData: QuantityeractionReactiveCompatible = dict?[(String(app_shareId.suffix(8)))] as! QuantityeractionReactiveCompatible
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ChatButtonViewController: SuiteThen {
    //: func func__setReplyTipsView() {
    func camera() {
        //: let key =  "\(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID)" + (String(data_imageName) + String(show_ofSizePath)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.haulUp()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            colorNow(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            takeDoing()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func nudgeAll(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        stateStopModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        takeDoing()
    }

    //: func removeReplyTipsView() {
    func takeDoing() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            colorNow(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func nearWithAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: data_statusLimitBarName, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: main_versionEnterTimeStr - showStatusKey, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: notiEnabledRecordUrl,
                                     //: height: ScreenHeight)
                                     height: main_versionEnterTimeStr)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func vox(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.firmness() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if ParlourEventHandler.tarotCard().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: show_recordCornerId, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: AddManagerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: main_versionEnterTimeStr - showStatusKey,
                                     //: width: ScreenWidth,
                                     width: notiEnabledRecordUrl,
                                     //: height: ScreenHeight)
                                     height: main_versionEnterTimeStr)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if ParlourEventHandler.tarotCard().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: show_recordCornerId, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ChatButtonViewController {
    /// 创建UI
    //: private func createUI() {
    private func constraintUi() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(const_loadData) + String(show_constraintMakeMaleId)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func large() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? showStatusKey : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.giftRadii(width: contentView.width,
                                  //: height: contentView.height,
                                  height: contentView.height,
                                  //: corners: [.topLeft, .topRight],
                                  corners: [.topLeft, .topRight],
                                  //: cornerRadii: .init(width: 8, height: 8))
                                  cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? k_packageUserUrl : const_changeFormat)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + constNameValue)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? k_packageUserUrl : const_changeFormat)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + constNameValue)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + constNameValue)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func part() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.saddleMakeMove()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.warpath()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.politeness(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                app_serverName.successOrMeanSun(eventID: show_bottomBarScreenMessage, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
