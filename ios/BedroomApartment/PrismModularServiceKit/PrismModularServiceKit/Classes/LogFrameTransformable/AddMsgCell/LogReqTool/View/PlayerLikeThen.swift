
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_cellMsg:[UInt8] = [0xcc,0xcb,0xcc,0xd1,0x8d,0xc6,0xca,0xc1,0xc0,0xd7,0x9f,0x8c,0x85,0xcd,0xc4,0xd6,0x85,0xcb,0xca,0xd1,0x85,0xc7,0xc0,0xc0,0xcb,0x85,0xcc,0xc8,0xd5,0xc9,0xc0,0xc8,0xc0,0xcb,0xd1,0xc0,0xc1]

private func styleIconSure(render num: UInt8) -> UInt8 {
    return num ^ 165
}

/*: "Followers" :*/
fileprivate let userLabelMsg:[Character] = ["F","o","l"]
fileprivate let main_enterStr:String = "loweaction"

/*: "Following" :*/
fileprivate let noti_popId:[Character] = ["F","o"]
fileprivate let userFromText:[Character] = ["l","l","o","w","i","n","g"]

/*: "#E6E6E6" :*/
fileprivate let noti_modelMsg:String = "point path#E6E6E6"

/*: "btn_message_more" :*/
fileprivate let app_managerValue:String = "let successbtn_me"
fileprivate let constColorTitle:String = "as in event view ine_more"

/*: "UID:  :*/
fileprivate let data_makeId:[Character] = ["U","I","D",":"," "]

/*: "level_ :*/
fileprivate let app_sharedStr:String = "level_gift self medium bottom bottom"

/*: "Gift" :*/
fileprivate let appViewText:[UInt8] = [0xe4,0xca,0xc5,0xd7]

private func intervalimateLab(of num: UInt8) -> UInt8 {
    return num ^ 163
}

/*: "Mike" :*/
fileprivate let kYearName:[UInt8] = [0x23,0x7,0x5,0xb]

/*: "Follow" :*/
fileprivate let k_popMessage:[UInt8] = [0x8a,0xa3,0xa0,0xa0,0xa3,0xbb]

private func nameActualAdd(shared num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "gift_voice_card" :*/
fileprivate let constNorTitle:[Character] = ["g","i","f","t","_","v","o","i","c","e"]
fileprivate let appItemKey:[Character] = ["_","c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let showDataPath:[Character] = ["b","t","n","_","p","a","r"]
fileprivate let showToKey:[Character] = ["t","y","_","v","o","i","c","e","_","n","o","r"]

/*: "btn_party_voice_pre" :*/
fileprivate let appCenterStyleStr:String = "btn_psex else let add"
fileprivate let kBorderFormat:String = "oice_preminimize let key"

/*: "attentionUid" :*/
fileprivate let appFrameData:String = "attentwill"
fileprivate let dataRequestFirstStr:String = "onUidpath layer equal var"

/*: "source" :*/
fileprivate let data_commentMessage:[Character] = ["s","o","u","r","c","e"]

/*: "Kick out" :*/
fileprivate let mainCenterMsg:String = "self inputKick out"

/*: "Unmute" :*/
fileprivate let noti_makeKey:[Character] = ["U","n","m","u","t"]
fileprivate let main_imageKey:String = "back"

/*: "Mute" :*/
fileprivate let const_norAddStr:[Character] = ["M","u","t","e"]

/*: "Kick off" :*/
fileprivate let app_giftLabelTextMsg:String = "length nameKick off"

/*: "Block" :*/
fileprivate let showSectionMessage:String = "self forBlock"

/*: "Report" :*/
fileprivate let data_gestureText:String = "medium view succeedReport"

/*: "#F6F6F6" :*/
fileprivate let show_taskFormat:String = "#Ftable center add bottom"
fileprivate let userImageMessage:String = "let sex app to6F6F6"

/*: "Confirm to kick out the room?" :*/
fileprivate let kGestureKey:String = "Confirtalk shared top gift"
fileprivate let show_layerValue:String = " kick outype make height app"
fileprivate let showModelPath:String = "t thesuper path"

/*: "Cancel" :*/
fileprivate let app_toPath:[Character] = ["C","a","n","c","e","l"]

/*: "Sure" :*/
fileprivate let showMeFormat:String = "Surevar make index to"

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let constLayerValueName:[UInt8] = [0x3f,0x65,0x74,0x75,0x6d,0x20,0x65,0x68,0x74,0x20,0x6c,0x65,0x63,0x6e,0x61,0x63,0x20,0x6f,0x74,0x20,0x74,0x6e,0x61,0x77,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x75,0x73,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x41]

/*: "Confirm" :*/
fileprivate let constToFormat:String = "Confirmdelay video gift kind"

/*: "The mute has been lifted~" :*/
fileprivate let mainReloadKey:String = "The type do total"
fileprivate let constRawMsg:String = "view title has be"
fileprivate let show_appearName:String = "en color else data view to"
fileprivate let appUserMessage:String = "lifted~view line on"

/*: "Confirm to kick off the seat?" :*/
fileprivate let data_equalValue:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," "]
fileprivate let app_bottomValue:String = "thumbicthumb"
fileprivate let appNameKey:String = "he seat?name model view view item"

/*: "OK" :*/
fileprivate let user_tapStr:String = "ok"

/*: "Shielding Success" :*/
fileprivate let app_searchLiteUrl:[Character] = ["S","h","i","e","l","d","i","n","g"," ","S","u","c","c","e","s","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PlayerLikeThen.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol PerspectiveViewDelegate: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func tagTitle(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func resourceUp(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func contentPosition(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func ad(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class PlayerLikeThen: UIView {
    //: var popView: TalkingPopView?
    var popView: PathRecognizerDelegate?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: BullScalar?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(ParlourEventHandler.tarotCard().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = AddModelType()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: PerspectiveViewDelegate?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID {
        if uid == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        timeViewGift()
        //: setupSubViewsConstraint()
        stochasticProcessRelease()
        //: reqUserCardInfo()
        wearerMessageFormat()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_cellMsg.map{styleIconSure(render: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .pingfangCenter(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.outEqual()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(clickColor), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .pingfangCenter(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.census()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .pingfangCenter(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.outEqual()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .outEqual()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(userLabelMsg) + main_enterStr.replacingOccurrences(of: "action", with: "rs")).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.pingfangCenter(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .census()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.pingfangCenter(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .outEqual()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(noti_popId) + String(userFromText)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.pingfangCenter(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .census()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.pingfangCenter(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(noti_modelMsg.suffix(7))))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.addBar(name: (String(app_managerValue.suffix(6)) + "ssag" + String(constColorTitle.suffix(6)))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(exceptDisappear), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: SizeEffectThen = {
        //: let v = TalkingLiveRoomMuteView()
        let v = SizeEffectThen()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension PlayerLikeThen {
    //: func reqUserCardInfo() {
    func wearerMessageFormat() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        SearchPushListener.frameDownText(uid: self.uid, streamerUid: liveUserId, roomId: ParlourEventHandler.tarotCard().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<AddModelType>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.numberroduceExecute()
            }
        }
    }

    //: func setUserCardData() {
    func numberroduceExecute() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(data_makeId)) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.italianRegionViewFinish(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.BundleImageNamed(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.addBar(name: (String(app_sharedStr.prefix(6))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            viewToSex(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func viewToSex(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: appViewText.map{intervalimateLab(of: $0)}, encoding: .utf8)!)
            //: if liveUserId == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: kYearName.map{$0^110}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: k_popMessage.map{nameActualAdd(shared: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.outsideBecomeColor(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: appViewText.map{intervalimateLab(of: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.addBar(name: (String(constNorTitle) + String(appItemKey))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: kYearName.map{$0^110}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.addBar(name: (String(showDataPath) + String(showToKey))), for: .normal)
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.addBar(name: (String(appCenterStyleStr.prefix(5)) + "arty_v" + String(kBorderFormat.prefix(8)))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.pingfangMediumFont(fontSize: 15)
                btn.titleLabel?.font = UIFont.mediumPingfangStatusSizeFont(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(enablePlayer(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(noti_modelMsg.suffix(7))))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func enablePlayer(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: appViewText.map{intervalimateLab(of: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.resourceUp(uid: self.userModel.uid)
            //: self.dismiss()
            self.sachet()
        //: case "Mike":
        case String(bytes: kYearName.map{$0^110}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.contentPosition(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.sachet()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.tagTitle(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.sachet()
        //: case "Follow":
        case String(bytes: k_popMessage.map{nameActualAdd(shared: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            userState(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func userState(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        toShow(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.ad(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func toShow(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(appFrameData.replacingOccurrences(of: "will", with: "i") + String(dataRequestFirstStr.prefix(5)))] = uid
        //: dict["source"] = "2"
        dict[(String(data_commentMessage))] = "2"
        //: PictureRequestTool.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        PictureRequestTool.onEspecial(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func clickColor() {
        //: ManageressThen.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        ManageressThen.share.bigness(uid: self.userModel.uid)
        //: self.dismiss()
        self.sachet()
    }
}

//: extension TalkingVoiceRoomCardView {
extension PlayerLikeThen {
    //: @objc func moreBtnClick() {
    @objc func exceptDisappear() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(mainCenterMsg.suffix(8))).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(noti_makeKey) + main_imageKey.replacingOccurrences(of: "back", with: "e")).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(const_norAddStr)).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(app_giftLabelTextMsg.suffix(8))).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(showSectionMessage.suffix(5))).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(data_gestureText.suffix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.SpectaclesConfig.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = DumpViewDelegate.loadName(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(show_taskFormat.prefix(2)) + String(userImageMessage.suffix(5))))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func errorThird() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = PathRecognizerDelegate(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr)
        //: popView?.initWithView(view: self)
        popView?.addView(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? ColorDelimitateMacroDefine.getWindow())
        popView?.tableTitle(view: self.firmness()?.view ?? ColorDelimitateMacroDefine.beforeText())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func sachet() {
        //: popView?.dismissView()
        popView?.makeView()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension PlayerLikeThen: ClickViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func makeName(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(mainCenterMsg.suffix(8))).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            ButtonTargetReactiveCompatible.doState(title: nil, message: (String(kGestureKey.prefix(6)) + "m to" + String(show_layerValue.prefix(8)) + String(showModelPath.prefix(5)) + " room?").localized, leftBtnTitle: (String(app_toPath)).localized, rightBtnTitle: (String(showMeFormat.prefix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                DescriptionProgressHUD.toChange()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                LogReqTool.bullSEyeCompletion(targetUid: self.userModel.uid, roomId: ParlourEventHandler.tarotCard().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    DescriptionProgressHUD.actionOf()
                    //: self.dismiss()
                    self.sachet()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(noti_makeKey) + main_imageKey.replacingOccurrences(of: "back", with: "e")).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            ButtonTargetReactiveCompatible.doState(title: nil, message: String(bytes: constLayerValueName.reversed(), encoding: .utf8)!.localized, leftBtnTitle: (String(app_toPath)).localized, rightBtnTitle: (String(constToFormat.prefix(7))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                DescriptionProgressHUD.toChange()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                LogReqTool.createDoing(targetUid: self.userModel.uid, roomID: ParlourEventHandler.tarotCard().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    DescriptionProgressHUD.actionOf()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.clickFill(showMsg: (String(mainReloadKey.prefix(4)) + "mute" + String(constRawMsg.suffix(7)) + String(show_appearName.prefix(3)) + String(appUserMessage.prefix(7))).localized)
                    }
                    //: self.dismiss()
                    self.sachet()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(const_norAddStr)).localized: // 设置禁言
            //: muteView.showView()
            muteView.panoramicView()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.sachet()

        //: case "Kick off".localized: /// 踢下麦
        case (String(app_giftLabelTextMsg.suffix(8))).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            ButtonTargetReactiveCompatible.doState(title: nil, message: (String(data_equalValue) + app_bottomValue.replacingOccurrences(of: "thumb", with: "k") + " off t" + String(appNameKey.prefix(8))).localized, leftBtnTitle: (String(app_toPath)).localized, rightBtnTitle: (String(showMeFormat.prefix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                ParlourEventHandler.tarotCard().volleyballPlayerPosition(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.sachet()
            }

        //: case "Report".localized:
        case (String(data_gestureText.suffix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = FromShareVisualImageViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.chemicalElement(view: nil)
        //: case "Block".localized:
        case (String(showSectionMessage.suffix(5))).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            ButtonTargetReactiveCompatible.doState(title: nil, message: appTimeBarStr, leftBtnTitle: (String(app_toPath)).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                ButtonTargetReactiveCompatible.obnubilateBlockAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                ButtonTargetReactiveCompatible.obnubilateBlockAlert()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                SubUserReactiveCompatible.squareIn(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.clickFill(showMsg: (String(app_searchLiteUrl)).localized)
                        //: self.dismiss()
                        self.sachet()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension PlayerLikeThen {
    // 添加视图
    //: private func setupSubviews() {
    private func timeViewGift() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func stochasticProcessRelease() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(notiEnabledRecordUrl / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-notiEnabledRecordUrl / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(notiEnabledRecordUrl / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.firmness()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.firmness()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.sachet()
        }
    }
}
