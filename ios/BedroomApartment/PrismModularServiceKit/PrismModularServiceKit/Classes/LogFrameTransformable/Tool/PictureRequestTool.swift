
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let userModelTitleCaseUrl:String = "type left datamf/b"
fileprivate let show_leadingId:[Character] = ["s","s","/","g","i","f","t","L","i","s","t"]

/*: "type" :*/
fileprivate let show_yearName:[UInt8] = [0x1,0xc,0x5,0x10]

private func inputSignal(between num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let mainCellPath:[Character] = ["m","f","/","c","h"]
fileprivate let user_hiddenText:String = "atRoom/size by to name"
fileprivate let mainTingData:String = "giftListmode section index click"

/*: "live/giftList" :*/
fileprivate let show_gestureManagerPath:String = "statusve"
fileprivate let const_backgroundMsg:String = "image typetList"

/*: "party/giftList" :*/
fileprivate let k_colorName:String = "PART"
fileprivate let user_pathStr:[Character] = ["f","t","L","i","s","t"]

/*: "IM/getMessageList" :*/
fileprivate let const_viewTitle:String = "label nameIM/get"
fileprivate let show_hideKey:String = "self session returnMessa"

/*: "mf/business/sendMsg" :*/
fileprivate let constAllMessage:String = "text page female result nonemf/bus"
fileprivate let notiViewMsg:[Character] = ["n","d","M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let kTopStr:[Character] = ["m","f","/","c","h"]
fileprivate let appEqualMsg:String = "voice image to target toolatRo"
fileprivate let data_priceTitle:[Character] = ["o","m","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let dataAllName:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n"]
fileprivate let user_containerShowTitle:[Character] = ["d","G","i","f","t"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let user_firstValue:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/"]
fileprivate let show_shareMsg:[Character] = ["s","e","n","d","G","i","f","t"]

/*: "live/sendGift" :*/
fileprivate let userTopValue:String = "live/true gift legacy go self"
fileprivate let notiAddDataBackgroundUrl:String = "share view make type selfsendGift"

/*: "party/sendGift" :*/
fileprivate let main_viewValue:[Character] = ["p","a","r","t","y","/","s","e"]
fileprivate let appTapModelUrl:String = "age true equal estimated femalendGift"

/*: "mf/user/getChatInfo" :*/
fileprivate let data_infoPath:String = "style view titlemf/us"
fileprivate let main_imageContent:String = "name"
fileprivate let show_toValue:String = "atInfoview select"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let k_inputCenterMessage:String = "IM/getframe effect false"
fileprivate let kFinishAnswerPath:String = "cell left data collection aftereUse"
fileprivate let showMagnitudeervalKey:String = "let view viewoList"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let mainUserValue:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o","L","i","k","e","M","e","L","i"]
fileprivate let const_requestFormat:String = "frame"

/*: "mf/crush/ignore" :*/
fileprivate let appPopularMessage:String = "mf/crusmanager var if var count"
fileprivate let main_colorId:String = "self element let inserth/ig"

/*: "mf/crush/like" :*/
fileprivate let constCollectionName:[Character] = ["m","f","/","c","r","u","s","h"]
fileprivate let showModelMsg:String = "make in center/like"

/*: "mf/user/getRelation" :*/
fileprivate let appShowFormat:String = "share height skin headmf/use"
fileprivate let k_tableManagerStr:String = "latnameon"

/*: "user/attention" :*/
fileprivate let showAtSucceedTitle:[UInt8] = [0xaf,0xad,0x9f,0xac,0x69,0x9b,0xae,0xae,0x9f,0xa8,0xae,0xa3,0xa9,0xa8]

fileprivate func fromEqual(bring num: UInt8) -> UInt8 {
    let value = Int(num) + 198
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/removeAttention" :*/
fileprivate let noti_pairValue:String = "USER"
fileprivate let main_arrayStr:String = "ttentgesturen"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let k_objectStatusUrl:[UInt8] = [0xae,0x8c,0x83,0xca,0x99,0xcd,0x88,0x8c,0x9f,0x83,0xcd,0x9d,0x82,0x84,0x83,0x99,0x9e,0xcd,0x84,0x8b,0xcd,0x94,0x82,0x98,0xcd,0x8b,0x82,0x81,0x81,0x82,0x9a,0xcd,0x88,0x8c,0x8e,0x85,0xcd,0x82,0x99,0x85,0x88,0x9f,0x93]

/*: "follow Success" :*/
fileprivate let app_barUrl:[Character] = ["f","o","l","l"]
fileprivate let userGiftMsg:[Character] = ["o","w"," ","S","u","c","c","e","s","s"]

/*: "unfollow Success" :*/
fileprivate let userProgressFormat:String = "linenfo"
fileprivate let appVideoStr:[Character] = ["c","e","s","s"]

/*: "translate/msg" :*/
fileprivate let dataMakeMessage:String = "trcursl"
fileprivate let app_normalKey:[Character] = ["g"]

/*: "msgId" :*/
fileprivate let k_titleSharedMsg:[UInt8] = [0x53,0x59,0x4d,0x2f,0x4a]

fileprivate func modelMain(a num: UInt8) -> UInt8 {
    let value = Int(num) - 230
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "targetLng" :*/
fileprivate let k_shapeId:[UInt8] = [0x67,0x6e,0x4c,0x74,0x65,0x67,0x72,0x61,0x74]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let noti_capacityTitle:String = "transform info framemf/gre"
fileprivate let dataTextSuccessMsg:String = "button section blocktAut"
fileprivate let main_requestText:String = "view"
fileprivate let appGameSizeTrueFormat:String = "Greettrue center observer white"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let constFrameKey:[Character] = ["m","f","/","g","r","e","e","t"]
fileprivate let data_lowMsg:[Character] = ["/","g","e","t","I","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let showSizeMsg:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "mf/chatRoom/info" :*/
fileprivate let appObjectFormat:String = "cell bottom normal viewmf/cha"
fileprivate let kDomainTitle:String = "/inforegister app value"

/*: "mf/chatRoom/list" :*/
fileprivate let dataValueLabAppMsg:String = "mf/ctable color gift reply turn"
fileprivate let mainCellTitle:String = "oom/listrecover table tag model"

/*: "roomId" :*/
fileprivate let k_textData:[UInt8] = [0x76,0x6b,0x6b,0x69,0x4d,0x60]

private func counternalRepresentation(color num: UInt8) -> UInt8 {
    return num ^ 4
}

/*: "needExtraInfo" :*/
fileprivate let userToStr:[UInt8] = [0xd2,0xd9,0xd9,0xd8,0xf9,0xc4,0xc8,0xce,0xdd,0xf5,0xd2,0xda,0xd3]

private func timeBottom(data num: UInt8) -> UInt8 {
    return num ^ 188
}

/*: "mf/chatRoom/check" :*/
fileprivate let showFeedbackKey:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let constWhiteTitle:[Character] = ["o","o","m","/","c","h","e","c","k"]

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let k_actualName:[Character] = ["m","f","/","c","h","a","t","R","o","o"]
fileprivate let appColorName:String = "m/not path return"
fileprivate let dataSharedText:String = "joinOintimate self equal height"

/*: "mf/chatRoom/members" :*/
fileprivate let show_countRawMessage:[Character] = ["m","f","/","c","h","a","t","R","o","o","m"]
fileprivate let constManagerValue:String = "lab var map self bar/members"

/*: "translate/text" :*/
fileprivate let show_viewId:[Character] = ["t","r","a","n","s","l","a","t","e"]
fileprivate let k_meFormat:String = "/textadded view lab size"

/*: "text" :*/
fileprivate let const_voiceKey:[UInt8] = [0x65,0x74,0x69,0x65]

private func guardFollowingInfo(manager num: UInt8) -> UInt8 {
    return num ^ 17
}

/*: "mf/user/queryStatus" :*/
fileprivate let main_editKey:String = "activity to method reading viewmf/us"
fileprivate let kFeatureDraftMessage:String = "ryStatusapp if"

/*: "toUids" :*/
fileprivate let notiTableContent:[UInt8] = [0x73,0x64,0x69,0x55,0x6f,0x74]

/*: "call/getCallTab" :*/
fileprivate let constSizeValue:[Character] = ["c","a","l","l","/","g","e","t","C","a"]
fileprivate let show_barImageMessage:String = "llTabresult user view signature frame"

/*: "mf/user/remark" :*/
fileprivate let userMessageContent:[Character] = ["m","f","/","u","s","e","r","/","r","e","m","a"]
fileprivate let mainContentFromPath:String = "rgame"

/*: "content" :*/
fileprivate let main_iconNeedTitle:[UInt8] = [0xe6,0xf2,0xf1,0xf7,0xe8,0xf1,0xf7]

fileprivate func placeTarget(remove num: UInt8) -> UInt8 {
    let value = Int(num) - 131
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "top" :*/
fileprivate let userPutButtonStr:[UInt8] = [0x2e,0x29,0x2a]

fileprivate func messageMake(income num: UInt8) -> UInt8 {
    let value = Int(num) - 186
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PictureRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class PictureRequestTool: NSObject {
public class PictureRequestTool: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [SpeedModelType]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [SpeedModelType]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func feePressPull(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(userModelTitleCaseUrl.suffix(4)) + "usine" + String(show_leadingId))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: show_yearName.map{inputSignal(between: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func listInput(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(mainCellPath) + String(user_hiddenText.prefix(7)) + String(mainTingData.prefix(8)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func minutia(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (show_gestureManagerPath.replacingOccurrences(of: "status", with: "li") + "/gif" + String(const_backgroundMsg.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func half(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (k_colorName.lowercased() + "y/gi" + String(user_pathStr))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func keyText(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(const_viewTitle.suffix(6)) + String(show_hideKey.suffix(5)) + "geList")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func nextContrive(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(constAllMessage.suffix(6)) + "iness/se" + String(notiViewMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func needAction(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AddRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(kTopStr) + String(appEqualMsg.suffix(4)) + String(data_priceTitle))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func imageDrop(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(dataAllName) + String(user_containerShowTitle))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func frame(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(user_firstValue) + String(show_shareMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func serverItem(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(userTopValue.prefix(5)) + String(notiAddDataBackgroundUrl.suffix(8)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func param(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(main_viewValue) + String(appTapModelUrl.suffix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func starCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(data_infoPath.suffix(5)) + "er/getC" + main_imageContent.replacingOccurrences(of: "name", with: "h") + String(show_toValue.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func paramCompletion(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(k_inputCenterMessage.prefix(6)) + "Messag" + String(kFinishAnswerPath.suffix(4)) + "rInf" + String(showMagnitudeervalKey.suffix(5)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<SpeedModelType>.deserialize(from: result as? Array) {
            if let datas = Array<SpeedModelType>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [SpeedModelType])
                completion(succeed, datas as! [SpeedModelType])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func deviseBackgroundCompletion(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        app_sizeUrl.freebee(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func definite(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(mainUserValue) + const_requestFormat.replacingOccurrences(of: "frame", with: "st"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func superior(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(appPopularMessage.prefix(7)) + String(main_colorId.suffix(4)) + "nore")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func clickOff(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(constCollectionName) + String(showModelMsg.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func jumpList(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(appShowFormat.suffix(6)) + "r/getRe" + k_tableManagerStr.replacingOccurrences(of: "name", with: "i"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func onEspecial(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: showAtSucceedTitle.map{fromEqual(bring: $0)}, encoding: .utf8)! : (noti_pairValue.lowercased() + "/removeA" + main_arrayStr.replacingOccurrences(of: "gesture", with: "io"))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
                    if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        main_conversationId.clickFill(showMsg: String(bytes: k_objectStatusUrl.map{$0^237}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        main_conversationId.clickFill(showMsg: (String(app_barUrl) + String(userGiftMsg)).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    main_conversationId.clickFill(showMsg: (userProgressFormat.replacingOccurrences(of: "line", with: "u") + "llow Suc" + String(appVideoStr)).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                main_conversationId.statusTo(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func impressionLocation(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (dataMakeMessage.replacingOccurrences(of: "cur", with: "an") + "ate/ms" + String(app_normalKey))
        //: reqModel.params = ["msgId": msgId, "targetLng": ColorDelimitateMacroDefine.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: k_titleSharedMsg.map{modelMain(a: $0)}, encoding: .utf8)!: msgId, String(bytes: k_shapeId.reversed(), encoding: .utf8)!: ColorDelimitateMacroDefine.lang(), String(bytes: show_yearName.map{inputSignal(between: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func replacement(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(noti_capacityTitle.suffix(6)) + "et/ge" + String(dataTextSuccessMsg.suffix(4)) + main_requestText.replacingOccurrences(of: "view", with: "o") + String(appGameSizeTrueFormat.prefix(5)) + "Content")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func tone(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(constFrameKey) + String(data_lowMsg))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: showSizeMsg.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func chatCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(appObjectFormat.suffix(6)) + "tRoom" + String(kDomainTitle.prefix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func chatEqual(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(dataValueLabAppMsg.prefix(4)) + "hatR" + String(mainCellTitle.prefix(8)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func mpSet(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: k_textData.map{counternalRepresentation(color: $0)}, encoding: .utf8)!: roomId, String(bytes: userToStr.map{timeBottom(data: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(showFeedbackKey) + String(constWhiteTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func cellCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(k_actualName) + String(appColorName.prefix(2)) + String(dataSharedText.prefix(5)) + "rQuit")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func product(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(show_countRawMessage) + String(constManagerValue.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func completion(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AddRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (String(show_viewId) + String(k_meFormat.prefix(5)))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: const_voiceKey.map{guardFollowingInfo(manager: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func electricalDeviceCompletion(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AddRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(main_editKey.suffix(5)) + "er/que" + String(kFeatureDraftMessage.prefix(8)))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: notiTableContent.reversed(), encoding: .utf8)!: toUids, String(bytes: show_yearName.map{inputSignal(between: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func actionReply(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(constSizeValue) + String(show_barImageMessage.prefix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func tickOff(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(userMessageContent) + mainContentFromPath.replacingOccurrences(of: "game", with: "k"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: main_iconNeedTitle.map{placeTarget(remove: $0)}, encoding: .utf8)!: content, String(bytes: userPutButtonStr.map{messageMake(income: $0)}, encoding: .utf8)!: isTop, String(bytes: showSizeMsg.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
