
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let k_cardContent:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let constRangeTitle:[UInt8] = [0xbb,0xa7,0xab,0xa3,0xad,0xbc,0xb7,0xbe,0xa1,0xac,0xad,0xa7,0xab,0xa0,0xa9,0xbc,0xb7,0xba,0xad,0xb9,0xbd,0xad,0xbb,0xbc,0xab,0xa9,0xa4,0xa4,0xb7,0xa6,0xa7,0xbc,0xa1,0xae,0xa1,0xab,0xa9,0xbc,0xa1,0xa7,0xa6]

private func popularView(normal num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let app_nameId:[UInt8] = [0x2f,0x22,0x20,0x26,0x33,0x22,0x3c,0x23,0x29,0x2c,0x1e,0x31,0x30,0x20,0x2f,0x22,0x22,0x2b,0x3c,0x2b,0x2c,0x31,0x26,0x23,0x26,0x20,0x1e,0x31,0x26,0x2c,0x2b]

fileprivate func cookieAllow(to num: UInt8) -> UInt8 {
    let value = Int(num) + 35
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isCalling" :*/
fileprivate let dataManagerFormat:[Character] = ["i","s","C"]
fileprivate let data_monthTapMainValue:[Character] = ["a","l","l","i","n","g"]
 
/*: "SOCKET 连接成功." :*/
fileprivate let const_textWindowPath:String = "SaKE"

/*: "SOCKET 连接失败, error: :*/
fileprivate let k_appTitle:[Character] = ["S","O","C","K","E","T"," "]
fileprivate let notiPathValue:[Character] = ["连","接","失","败",","," ","e","r","r","o","r",":"]

/*: ." :*/
fileprivate let const_playName:String = "step"

/*: "SOCKET 失败 :*/
fileprivate let userInfoUrl:String = "so"
fileprivate let mainModelContent:[Character] = ["C","K","E","T"," ","失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let dataPlayName:String = "次, 切换\u{8fc7}"
fileprivate let data_kitInviteSignatureUrl:String = "lab to addet域名重新尝"
fileprivate let constInstanceCookieUrl:String = "试\u{ff0c}地址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let kCollectionTitle:[Character] = ["S","O","C","K","E","T"," ","已","关","闭",","," ","c"]
fileprivate let mainFilePath:[Character] = ["o","d","e",":"]

/*: , reason: :*/
fileprivate let main_pushWeightTextMessage:String = "file data, reas"
fileprivate let userLabelLabStr:String = "error view styleon:"

/*: "ws:// :*/
fileprivate let main_imageHomeMsg:String = "ws://data array"

/*: /?sid= :*/
fileprivate let appFrameTitle:[Character] = ["/","?","s","i","d"]
fileprivate let notiTodayMsg:[Character] = ["="]

/*: &uid= :*/
fileprivate let appEditStyleContent:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let showRequestFormat:String = "&platfview error block"
fileprivate let dataTaskUrl:String = "isocialo"
fileprivate let kUserText:String = "else attention shared varrsion="

/*: &packageId= :*/
fileprivate let app_recordFormat:[Character] = ["&","p","a","c","k","a","g","e","I"]
fileprivate let dataCellPath:[Character] = ["d","="]

/*: "1v1server. :*/
fileprivate let constPlayerStr:String = "appear manager else from gift1v1s"
fileprivate let main_touchDataText:String = "else container selectedr."

/*: .com:7766" :*/
fileprivate let constShowKey:String = "count share return.com"

/*: "43.129.185.194:7767" :*/
fileprivate let app_hideId:String = "43.announcement29.announcement"
fileprivate let kEqualData:String = "85.194"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let userMainKey:[UInt8] = [0x5c,0x40,0x4c,0x44,0x4a,0x5b,0x2f,0xe8,0xb2,0x9e,0xe8,0xb4,0x93,0x7d,0x6a,0x6e,0x6c,0x67,0x6e,0x6d,0x66,0x63,0x66,0x7b,0x76,0x4c,0x67,0x6e,0x61,0x68,0x6a,0x6b,0xe8,0x94,0x9e,0xea,0x9f,0xa3,0xe0,0xb3,0x83,0xe9,0x99,0xa2,0xe8,0xb2,0x9e,0xea,0x8a,0xbc,0xe6,0x98,0xa2,0x21]

private func colorType(at num: UInt8) -> UInt8 {
    return num ^ 15
}

/*: "cmd" :*/
fileprivate let notiArrayPath:String = "methodmd"

/*: "ping" :*/
fileprivate let showSourceMsg:String = "piimageg"

/*: "callHeartbeat" :*/
fileprivate let showResourceTimeStr:[Character] = ["c","a","l","l","H","e"]
fileprivate let dataSocialArrowMsg:String = "labelrtbelabelt"

/*: "recievedDate" :*/
fileprivate let data_makeToKey:[Character] = ["r","e","c","i","e","v","e","d"]
fileprivate let show_messageDotTitle:String = "back player intervalDate"

/*: "liveHeartBeat" :*/
fileprivate let notiFinishKey:String = "active self modeliveHea"
fileprivate let showRequestKey:String = "equal"
fileprivate let const_postPath:String = "page complaint else self colortBeat"

/*: "🔗 关闭 socket" :*/
fileprivate let user_itemTitle:String = "🔗 关闭 sview in var"

/*: "The user logs out" :*/
fileprivate let userViewFormat:[Character] = ["T","h","e"," ","u","s","e","r"," ","l","o","g","s"," ","o","u","t"]

/*: "SOCKET  :*/
fileprivate let k_modelId:[Character] = ["S"]
fileprivate let k_userMsg:[Character] = ["O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let data_loadUrl:String = "重连. 重"

/*: , 地址： :*/
fileprivate let dataSharedUrl:String = ", \u{5730}\u{5740}："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let show_viewBeautyItemMsg:[Character] = ["S"]
fileprivate let kSumRequestFormat:String = "OCKET return return class collection"
fileprivate let kAtText:String = "需\u{8981}重新连接."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let const_afterMsg:String = "SOfaceE"
fileprivate let dataSourceValue:String = "连接 不需要"
fileprivate let appAdMessage:String = "重新连fill."

/*: "&resume=1" :*/
fileprivate let notiDataModelMsg:String = "&resume=detail show remove"
fileprivate let dataPostFormat:[Character] = ["1"]

/*: "&lang= :*/
fileprivate let const_labPathName:String = "equal in&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let k_sharePath:[Character] = ["S","O","C","K","E","T"," "]
fileprivate let notiValueMsg:String = "开始连\u{63a5}"

/*: "onNetDisabled" :*/
fileprivate let constPathText:String = "value icon model else targetonNetDi"
fileprivate let app_pointTitle:String = "ssizeled"

/*: "errno" :*/
fileprivate let dataManagerId:String = "evideovideono"

/*: "data" :*/
fileprivate let userQuantityCirclePath:String = "dgesturea"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let main_titleLabValue:String = "SOCKET finish self value up"
fileprivate let k_makeStr:String = "\u{8fde}接保\u{6d3b}cm"
fileprivate let noti_topMessage:String = "d: ping.up image let make value"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let data_namePath:[UInt8] = [0x68,0x74,0x78,0x70,0x7e,0x6f,0x1b,0x4f,0x49,0x4f,0x58,0xdf,0x84,0xa6,0xdd,0x8f,0x80,0x58,0x56,0x5f,0x1,0x1b,0x58,0x5a,0x57,0x57,0x73,0x5e,0x5a,0x49,0x4f,0x59,0x5e,0x5a,0x4f,0x15]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let show_withUserStr:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "onP" :*/
fileprivate let constFrameToUrl:String = "onPmain series make data"

/*: "msg" :*/
fileprivate let noti_labPath:String = "msadd"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let user_arrayText:String = "🔗 处\u{7406}"
fileprivate let user_equalName:String = "Name:send not"

/*: , errNo: :*/
fileprivate let user_finishSumMsg:[Character] = [","," ","e","r","r"]
fileprivate let noti_hockeyId:[Character] = ["N","o",":"]

/*: , msg: :*/
fileprivate let constMakeTimeId:[Character] = [","," ","m","s","g"]
fileprivate let noti_toObjectMsg:String = ":"

/*: , data: :*/
fileprivate let show_partyUrl:String = "system "
fileprivate let show_cancelName:String = "data:medium share add actual"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let show_appMakeKey:String = "SOCKET 接if text date text var"
fileprivate let appPairValue:[Character] = ["\u{6536}","异","常","\u{6d88}","息","：","c","m","d","N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let kLastTitle:String = "onReceiintimate face path data"
fileprivate let appColorContent:String = "to name else letvedGift"

/*: "onVideoMatch" :*/
fileprivate let const_centerFormat:String = "onVistring cell"
fileprivate let data_managerSharedId:[Character] = ["a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let notiQuantityimateMessage:[Character] = ["o","n","R","e","q","u","e","s","t"]
fileprivate let mainModeUserName:String = "Callname bottom to"

/*: "onCancelCall" :*/
fileprivate let k_cameraLabMessage:String = "domain container mode return stackonCan"

/*: "onRefuseCall" :*/
fileprivate let userToData:[Character] = ["o","n","R","e","f","u","s","e","C","a"]
fileprivate let user_layerStr:[Character] = ["l","l"]

/*: "onAcceptCall" :*/
fileprivate let main_imageStr:String = "onAcctext string size"
fileprivate let showViewLaunchUrl:String = "icon"

/*: "onStopCall" :*/
fileprivate let mainTransformFormat:[Character] = ["o","n","S","t"]
fileprivate let dataPlayUrl:String = "opCallheight corner if message color"

/*: "freeCallTimes" :*/
fileprivate let main_needValue:[Character] = ["f","r","e","e","C","a"]
fileprivate let userOfFormat:[Character] = ["l","l","T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let dataSearchStr:String = "number successmfCoi"
fileprivate let app_timePauseId:String = "succeed"

/*: "onMissCoin" :*/
fileprivate let data_playerLiveFormat:String = "onMiskit lab live attention"
fileprivate let user_extraId:String = "post read image varsCoin"

/*: "acceptCall" :*/
fileprivate let dataCenterTitle:[Character] = ["a","c","c","e","p"]
fileprivate let showActualAddValue:String = "tCallorigin content self value"

/*: "stopCall" :*/
fileprivate let appTitleMessage:String = "S"
fileprivate let appListToolKey:String = "topCallleft view float title"

/*: "requestCall" :*/
fileprivate let k_thenPath:String = "rempty"
fileprivate let show_makeKey:String = "querequest"

/*: "[Invite you to a video call...]" :*/
fileprivate let showAppViewEnterValue:[UInt8] = [0x6c,0x7e,0x59,0x41,0x5e,0x43,0x52,0x17,0x4e,0x58,0x42,0x17,0x43,0x58,0x17,0x56,0x17,0x41,0x5e,0x53,0x52,0x58,0x17,0x54,0x56,0x5b,0x5b,0x19,0x19,0x19,0x6a]

private func lowClass(up num: UInt8) -> UInt8 {
    return num ^ 55
}

/*: "type" :*/
fileprivate let app_originName:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let noti_pathPlaceData:[UInt8] = [0x86,0x94,0xb3,0xab,0xb4,0xa9,0xb8,0xfd,0xa4,0xb2,0xa8,0xfd,0xa9,0xb2,0xfd,0xbc,0xfd,0xab,0xb2,0xb4,0xbe,0xb8,0xfd,0xbe,0xbc,0xb1,0xb1,0xf3,0xf3,0xf3,0x80]

private func imagePasse(button num: UInt8) -> UInt8 {
    return num ^ 221
}

/*: "uid" :*/
fileprivate let userUpgradeKey:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let notiUserUrl:[Character] = ["n","i"]
fileprivate let user_videoStr:String = "ckmakeame"

/*: "headPic" :*/
fileprivate let const_pleaseFormat:[Character] = ["h","e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let const_layerEqualPath:String = "onCayear make effect view name"

/*: "cancelCall" :*/
fileprivate let dataSumPath:String = "cancemethod"
fileprivate let app_popId:String = "Callvalue value true"

/*: "refuseCall" :*/
fileprivate let userLabContent:String = "refuindicator"
fileprivate let constContentFormat:[Character] = ["C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let userListTitle:String = "image giftonCa"
fileprivate let notiComponentUrl:[Character] = ["i","v","a","t","i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let user_modeFirstMessage:String = "accept true phone style targetonLive"
fileprivate let userImageText:String = "bottom in selfWarning"

/*: "reason" :*/
fileprivate let kViewContent:[Character] = ["r","e","a","s","o","n"]

/*: "time" :*/
fileprivate let show_touchPath:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let data_equalPath:[Character] = ["o","n","S","t","o","p"]
fileprivate let user_tagStr:String = "self caseLive"

/*: "onSendProgramGift" :*/
fileprivate let user_recommendFormat:String = "frame error error view makeonSe"
fileprivate let mainBagStr:String = "shell"
fileprivate let const_recordValue:[Character] = ["m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let user_colorActualMsg:String = "post model viewonGa"

/*: "broadStatus" :*/
fileprivate let data_tableAddOnMsg:[Character] = ["b","r","o","a","d","S","t"]
fileprivate let dataCellValue:[Character] = ["a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let user_centerUrl:String = "onCallmain save"

/*: "onNotificationPopupWin" :*/
fileprivate let mainImageValue:String = "name view first user lastonNotific"
fileprivate let main_equalMsg:[Character] = ["a","t","i","o","n","P","o","p","u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let k_statusText:String = "onUsadd arena return color"
fileprivate let userActionId:[Character] = ["k"]

/*: "freeCallWin" :*/
fileprivate let constLiveMessage:[Character] = ["f","r","e","e","C","a","l"]
fileprivate let dataBackgroundMessage:String = "name head aslWin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ActionSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let app_devicePath = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let app_recordValue = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let k_clickData = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let show_messageMsg = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let show_playUrl = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let main_closeClickPath = (String(k_cardContent))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let constCollectionKey = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let app_successTitle = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let mainSkipTitle = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let show_pingKey = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let constTimeKey = NSNotification.Name(rawValue: String(bytes: constRangeTitle.map{popularView(normal: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let k_noRequestFormat = NSNotification.Name(rawValue: String(bytes: app_nameId.map{cookieAllow(to: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol ConnectAtErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func heed(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol ViewObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func cleanData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func startLocation(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol EvaluationObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func pullUpShort(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func running(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func endSchedule(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func dataText(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol PathSuiteObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func pointData(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func placeToData(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func christen(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class ActionSocketDelegate: NSObject {
	var constraintName: String = "show"
	var colorAtArray: [AnyHashable] = []

    //: enum SocketRetryType: String {
    enum PointOfReferencePlayer: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: AddBeautyThen.shared.direction == .rightToLeft ? 0 : notiEnabledRecordUrl,
        //: y: StatusBarHeight+190
        y: showPlainValue + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: AddBeautyThen.shared.direction == .rightToLeft ? 0 : notiEnabledRecordUrl,
        //: y: StatusBarHeight+190
        y: showPlainValue + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: AddBeautyThen.shared.direction == .rightToLeft ? 0 : notiEnabledRecordUrl,
        //: y: StatusBarHeight+500
        y: showPlainValue + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: ConnectAtErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: ViewObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: EvaluationObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: PathSuiteObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    @objc dynamic var isShared = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = ActionSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { 
		if var addFinishValue = connectUrlStr { 
			if var frameValue = connectUrlStr { 
			if let containerView = containerView {
		
		            if (containerView.subviews.count == 173) && (containerView.backgroundColor != nil && containerView.backgroundColor!.cgColor == UIColor.systemRed.cgColor) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let takeValue = ResumeView(frame: containerView.bounds.intersection(CGRect(x: CGFloat(0), y: CGFloat(0), width: CGFloat(66), height: CGFloat(296.96))))
		            takeValue.connectEnable = cancelCall
		            takeValue.menuCount = linkIndex
		
		            takeValue.beautyName = frameValue
		            takeValue.collectionContent = { [self] handleIntervalerestTranslationName in
		            self.constraintName = handleIntervalerestTranslationName
		            
		            var takeValue = addFinishValue
		            if let with = takeValue.last(where: { $0.isUppercase }) {
		                takeValue.insert(with, at: takeValue.startIndex) // mutating
		            }
		            if takeValue.contains(self.constraintName) {
		                self.constraintName = takeValue
		            }
		            
		            return self.constraintName
		            }
		            takeValue.sectionArray = { [self] withClickArray in
		            self.colorAtArray = withClickArray
		            
		            guard var value = self.colorAtArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		                containerView.addSubview(takeValue)
		            }
		
			}
		
			}
		}
		return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(framework(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(periodicTable),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(dataManagerFormat) + String(data_monthTapMainValue)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (FirstApplicationThen.shared as! FirstApplicationThen).createAdd()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isShared")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (FirstApplicationThen.shared as! FirstApplicationThen).createAdd()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension ActionSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        CleanTargetLogTool.yearShow(msg: (const_textWindowPath.replacingOccurrences(of: "a", with: "OC") + "T 连接成功."))
        //: sendPing()
        direct()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: main_keyFormat,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: main_closeClickPath)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        afterSub()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        CleanTargetLogTool.yearShow(msg: (String(k_appTitle) + String(notiPathValue)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = RegularBeginDecisionMakerAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = RegularBeginDecisionMakerAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == show_messageMsg {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = plumeUse(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            CleanTargetLogTool.yearShow(msg: (userInfoUrl.uppercased() + String(mainModelContent)) + "\(show_messageMsg)" + (dataPlayName + "Sock" + String(data_kitInviteSignatureUrl.suffix(7)) + constInstanceCookieUrl) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.leadTimeReconnectClipVersionFeatherSocket(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.organiseToCommunicating(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        CleanTargetLogTool.yearShow(msg: (String(kCollectionTitle) + String(mainFilePath)) + "\(code)" + (String(main_pushWeightTextMessage.suffix(6)) + String(userLabelLabStr.suffix(3))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.socket(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == show_pingKey {
            //: return
            return
        }
        //: if let status = RegularBeginDecisionMakerAppManager.share.reachability?.connection, status != .unavailable {
        if let status = RegularBeginDecisionMakerAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.leadTimeReconnectClipVersionFeatherSocket(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func plumeUse(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = RegularBeginDecisionMakerAppManager.share.appConfigMode.ws
        self.linkAddrArr = RegularBeginDecisionMakerAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = clickArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: main_closeClickPath)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = RegularBeginDecisionMakerAppManager.share.loginUid
        let uid = RegularBeginDecisionMakerAppManager.share.loginUid
        //: let sid = RegularBeginDecisionMakerAppManager.share.loginSessionId
        let sid = RegularBeginDecisionMakerAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(appFrameTitle) + String(notiTodayMsg)) + "\(sid)" + (String(appEditStyleContent)) + "\(uid)" + (String(showRequestFormat.prefix(6)) + "orm=" + dataTaskUrl.replacingOccurrences(of: "social", with: "ph") + "ne&ve" + String(kUserText.suffix(6))) + "\(kKeyPath)" + (String(app_recordFormat) + String(dataCellPath)) + "\(user_environmentMsg)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func clickArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return notiBackData ? [(String(constPlayerStr.suffix(4)) + "erve" + String(main_touchDataText.suffix(2))) + "\(kErrId)" + (String(constShowKey.suffix(4)) + ":7766")] : [(app_hideId.replacingOccurrences(of: "announcement", with: "1") + kEqualData.capitalized + ":7767")]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension ActionSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func framework(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            CleanTargetLogTool.yearShow(msg: String(bytes: userMainKey.map{colorType(at: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.socket(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.leadTimeReconnectClipVersionFeatherSocket(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func periodicTable() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.leadTimeReconnectClipVersionFeatherSocket(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.writeShow()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension ActionSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func direct() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: app_devicePath, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.tabularArray(info: [(notiArrayPath.replacingOccurrences(of: "method", with: "c")): (showSourceMsg.replacingOccurrences(of: "image", with: "n"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func subKey() {
        //: endTalkingPing()
        beforeEach()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: app_recordValue, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isShared || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isShared || (self.firmness()?.isKind(of: PlayerCallerDelegate.self) == false && self.firmness()?.isKind(of: CalendarErrorDelegate.self) == false) && (self.currTalkingVC?.isKind(of: PlayerCallerDelegate.self) == false && self.currTalkingVC?.isKind(of: CalendarErrorDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.tabularArray(info: [(notiArrayPath.replacingOccurrences(of: "method", with: "c")): (String(showResourceTimeStr) + dataSocialArrowMsg.replacingOccurrences(of: "label", with: "a"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func beforeEach() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func writeShow() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(data_makeToKey) + String(show_messageDotTitle.suffix(4)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < main_nameViewStr {
                //: RegularBeginDecisionMakerAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                RegularBeginDecisionMakerAppManager.share.imageDownInfo(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension ActionSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func stubPing() {
        //: endLivePing()
        instrumentation()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: k_clickData, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.tabularArray(info: [(notiArrayPath.replacingOccurrences(of: "method", with: "c")): (String(notiFinishKey.suffix(7)) + showRequestKey.replacingOccurrences(of: "equal", with: "r") + String(const_postPath.suffix(5)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func instrumentation() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension ActionSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func afterSub() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = GlobalThen()
        //: let keyWindow = ColorDelimitateMacroDefine.getWindow()
        let keyWindow = ColorDelimitateMacroDefine.beforeText()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isShared
        self.containerView?.isHidden = !self.isShared
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func eventIn() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard SearchPushListener.biologyLab().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard ParlourEventHandler.tarotCard().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension ActionSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func socket(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(user_itemTitle.prefix(6)) + "ocket"))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: show_pingKey, reason: (String(userViewFormat)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        beforeEach()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            eventIn()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func mixUp() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = ActionSocketDelegate.shared.plumeUse(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.leadTimeReconnectClipVersionFeatherSocket(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func leadTimeReconnectClipVersionFeatherSocket(timeInterval: TimeInterval, type: PointOfReferencePlayer) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            self.writeData(type: type.rawValue)
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.connect(urlStr: self.connectUrlStr)
        }
    }
    
    func writeData(type: String) {
        CleanTargetLogTool.yearShow(msg: (String(k_modelId) + String(k_userMsg)) + "\(type)" + (data_loadUrl.capitalized + "\u{8bd5}次数\u{ff1a}") + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func connect(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                CleanTargetLogTool.yearShow(msg: (String(show_viewBeautyItemMsg) + String(kSumRequestFormat.prefix(6)) + "连接正常 \u{4e0d}" + kAtText))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                CleanTargetLogTool.yearShow(msg: (const_afterMsg.replacingOccurrences(of: "face", with: "CK") + "T 正在" + dataSourceValue.capitalized + appAdMessage.replacingOccurrences(of: "fill", with: "接")))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isShared {
        if self.isShared {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(notiDataModelMsg.prefix(8)) + String(dataPostFormat)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(const_labPathName.suffix(6))) + "\(PathAtAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        socket(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = show_playUrl
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        CleanTargetLogTool.yearShow(msg: (String(k_sharePath) + notiValueMsg + ", 地\u{5740}：") + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func tabularArray(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if RegularBeginDecisionMakerAppManager.share.reachability?.connection == nil || RegularBeginDecisionMakerAppManager.share.reachability?.connection == .unavailable {
        if RegularBeginDecisionMakerAppManager.share.reachability?.connection == nil || RegularBeginDecisionMakerAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(notiArrayPath.replacingOccurrences(of: "method", with: "c")): (String(constPathText.suffix(7)) + app_pointTitle.replacingOccurrences(of: "size", with: "ab")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (dataManagerId.replacingOccurrences(of: "video", with: "r")): "\(mainSkipTitle)",
                                       //: "data": [:]]
                                       (userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            organiseToCommunicating(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(notiArrayPath.replacingOccurrences(of: "method", with: "c"))] as? String == (showSourceMsg.replacingOccurrences(of: "image", with: "n")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                CleanTargetLogTool.yearShow(msg: (String(main_titleLabValue.prefix(7)) + k_makeStr + String(noti_topMessage.prefix(8))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(notiArrayPath.replacingOccurrences(of: "method", with: "c"))] as? String == (String(showResourceTimeStr) + dataSocialArrowMsg.replacingOccurrences(of: "label", with: "a")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                CleanTargetLogTool.yearShow(msg: String(bytes: data_namePath.map{$0^59}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(notiArrayPath.replacingOccurrences(of: "method", with: "c"))] as? String == (String(notiFinishKey.suffix(7)) + showRequestKey.replacingOccurrences(of: "equal", with: "r") + String(const_postPath.suffix(5))) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                CleanTargetLogTool.yearShow(msg: String(bytes: show_withUserStr.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(notiArrayPath.replacingOccurrences(of: "method", with: "c"))] as? String == (showSourceMsg.replacingOccurrences(of: "image", with: "n")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(notiArrayPath.replacingOccurrences(of: "method", with: "c")): (String(constFrameToUrl.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (dataManagerId.replacingOccurrences(of: "video", with: "r")): "\(constCollectionKey)",
                                           //: "data": [:]]
                                           (userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                organiseToCommunicating(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.leadTimeReconnectClipVersionFeatherSocket(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func organiseToCommunicating(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(notiArrayPath.replacingOccurrences(of: "method", with: "c"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(dataManagerId.replacingOccurrences(of: "video", with: "r"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(noti_labPath.replacingOccurrences(of: "add", with: "g"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (user_arrayText + "\u{6d88}息：cmd" + String(user_equalName.prefix(5))) + "\(cmdName)" + (String(user_finishSumMsg) + String(noti_hockeyId)) + "\(errorNo)" + (String(constMakeTimeId) + noti_toObjectMsg.capitalized) + "\(msg)" + (show_partyUrl.replacingOccurrences(of: "system", with: ",") + String(show_cancelName.prefix(5))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        eugene(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            CleanTargetLogTool.yearShow(msg: (String(show_appMakeKey.prefix(8)) + String(appPairValue)) + "\(cmdName)" + (String(user_finishSumMsg) + String(noti_hockeyId)) + "\(errorNo)" + (String(constMakeTimeId) + noti_toObjectMsg.capitalized) + "\(msg)" + (show_partyUrl.replacingOccurrences(of: "system", with: ",") + String(show_cancelName.prefix(5))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes = 0
                RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !RegularBeginDecisionMakerAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !RegularBeginDecisionMakerAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                TargetThen.shared.creationGift()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.heed(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(kLastTitle.prefix(7)) + String(appColorContent.suffix(7))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: show_topName, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(const_centerFormat.prefix(4)) + "deoM" + String(data_managerSharedId)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.cleanData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(notiQuantityimateMessage) + String(mainModeUserName.prefix(4))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.startLocation(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(k_cameraLabMessage.suffix(5)) + "celCall") { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(userToData) + String(user_layerStr)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(main_imageStr.prefix(5)) + "eptCal" + showViewLaunchUrl.replacingOccurrences(of: "icon", with: "l")) { // 接收方发送acceptCall回执
            //: self.isShared = true
            self.isShared = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            subKey()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.placeToData(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(mainTransformFormat) + String(dataPlayUrl.prefix(6))) { // 接收方发送stopCall回执
            //: self.isShared = false
            self.isShared = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            beforeEach()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(main_needValue) + String(userOfFormat))] as? Int {
                //: RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = json[(userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at"))][(String(dataSearchStr.suffix(5)) + app_timePauseId.replacingOccurrences(of: "succeed", with: "n"))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: appListMessage, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (FirstApplicationThen.shared as! FirstApplicationThen).overbusyFor()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(data_playerLiveFormat.prefix(5)) + String(user_extraId.suffix(5))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.dataText(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(dataCenterTitle) + String(showActualAddValue.prefix(5))) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isShared = true
            self.isShared = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            subKey()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.pullUpShort(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (appTitleMessage.lowercased() + String(appListToolKey.prefix(7))) { // 接收方/拨打方 挂断了通话
            //: self.isShared = false
            self.isShared = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            beforeEach()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.running(data: data)
            // 更新余额
            //: RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            RegularBeginDecisionMakerAppManager.share.loginUserMode.mf_coin = json[(userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at"))][(String(dataSearchStr.suffix(5)) + app_timePauseId.replacingOccurrences(of: "succeed", with: "n"))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            withImageData(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: appListMessage, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (FirstApplicationThen.shared as! FirstApplicationThen).overbusyFor()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (k_thenPath.replacingOccurrences(of: "empty", with: "e") + show_makeKey.replacingOccurrences(of: "request", with: "s") + "tCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: constTimeKey, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: RegularBeginDecisionMakerAppManager.share.recieve1v1TalkRequest(info: data)
                RegularBeginDecisionMakerAppManager.share.imageDownInfo(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(data_makeToKey) + String(show_messageDotTitle.suffix(4)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: showAppViewEnterValue.map{lowClass(up: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at"))][(String(app_originName))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: noti_pathPlaceData.map{imagePasse(button: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                BeginBaseDelegate.removeName(uid: String(json[(userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at"))][(String(userUpgradeKey))].intValue),
                                             //: title: data["nickname"] as? String,
                                             title: data[(String(notiUserUrl) + user_videoStr.replacingOccurrences(of: "make", with: "n"))] as? String,
                                             //: body: body,
                                             body: body,
                                             //: imageUrl: data["headPic"] as? String)
                                             imageUrl: data[(String(const_pleaseFormat))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                BeginBaseDelegate.shared.outFailure()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(const_layerEqualPath.prefix(4)) + "llResume") { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isShared = true
            self.isShared = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            subKey()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(const_layerEqualPath.prefix(4)) + "llResume")] = "1"
            //: RegularBeginDecisionMakerAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            RegularBeginDecisionMakerAppManager.share.recieve(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (FirstApplicationThen.shared as! FirstApplicationThen).createAdd()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (dataSumPath.replacingOccurrences(of: "method", with: "l") + String(app_popId.prefix(4))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isShared = false
            self.isShared = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            beforeEach()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.endSchedule(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                BeginBaseDelegate.writeCopy(identifier: String(json[(userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at"))][(String(userUpgradeKey))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                BeginBaseDelegate.shared.keyBgm()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (userLabContent.replacingOccurrences(of: "indicator", with: "se") + String(constContentFormat)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isShared = false
            self.isShared = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            beforeEach()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.pointData(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(userListTitle.suffix(4)) + "llMot" + String(notiComponentUrl)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.christen?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(user_modeFirstMessage.suffix(6)) + String(userImageText.suffix(7))) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            SearchPushListener.tenantColor(content: msg, reason: json[(userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at"))][(String(kViewContent))].stringValue, time: json[(userQuantityCirclePath.replacingOccurrences(of: "gesture", with: "at"))][(String(show_touchPath))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(data_equalPath) + String(user_tagStr.suffix(4))) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SearchPushListener.biologyLab().numbereraction()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(user_recommendFormat.suffix(4)) + "ndProgr" + mainBagStr.replacingOccurrences(of: "shell", with: "a") + String(const_recordValue)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.clickFill(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(user_colorActualMsg.suffix(4)) + "meFs") { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(data_tableAddOnMsg) + String(dataCellValue))] as? Int ?? -1
            //: if RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: k_noRequestFormat, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: show_topName, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: k_noRequestFormat, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(user_centerUrl.prefix(6)) + "ChatMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            AtClickDanmuManager.dipMagnitudeo().frenchTelephone(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(mainImageValue.suffix(9)) + String(main_equalMsg)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            TargetThen.shared.coatButton(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(k_statusText.prefix(4)) + "erBloc" + String(userActionId)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SearchPushListener.biologyLab().numbereraction()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ParlourEventHandler.tarotCard().attribute()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: kSoundStatusValue, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension ActionSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func eugene(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (k_thenPath.replacingOccurrences(of: "empty", with: "e") + show_makeKey.replacingOccurrences(of: "request", with: "s") + "tCall") || cmdName == (String(notiQuantityimateMessage) + String(mainModeUserName.prefix(4))) {
            //: if TalkingLiveManager.shared().isLive == true {
            if SearchPushListener.biologyLab().isLive == true {
                //: NEED_RESTORE_LIVE = true
                show_errorName = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                SearchPushListener.queryed(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                SearchPushListener.biologyLab().numbereraction()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard show_errorName == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (dataSumPath.replacingOccurrences(of: "method", with: "l") + String(app_popId.prefix(4))) || cmdName == (String(k_cameraLabMessage.suffix(5)) + "celCall") ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (userLabContent.replacingOccurrences(of: "indicator", with: "se") + String(constContentFormat)) || cmdName == (String(userToData) + String(user_layerStr)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (appTitleMessage.lowercased() + String(appListToolKey.prefix(7))) || cmdName == (String(mainTransformFormat) + String(dataPlayUrl.prefix(6)))
        {
            //: NEED_RESTORE_LIVE = false
            show_errorName = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = SizeDataReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                SearchPushListener.biologyLab().liveMake()
            }
            //: alert.show()
            alert.titleFromSelect()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func withImageData(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(main_needValue) + String(userOfFormat))] as? Int {
            //: RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(constLiveMessage) + String(dataBackgroundMessage.suffix(4)))] as? Bool
        //: if freeCallWin == true && RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = EffectUpView()
            //: view.show()
            view.deleteMake()
            //: view.setShowData(data: data)
            view.makeAcross(data: data)
        }
    }
}
