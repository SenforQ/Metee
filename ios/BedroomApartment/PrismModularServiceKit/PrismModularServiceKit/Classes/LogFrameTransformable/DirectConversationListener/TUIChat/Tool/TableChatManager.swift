
//: Declare String Begin

/*: "msgType" :*/
fileprivate let constTableFeeKey:String = "guard namemsgTyp"
fileprivate let kIndexKey:[Character] = ["e"]

/*: "tips" :*/
fileprivate let notiFailKey:String = "tipprogress"

/*: "extra" :*/
fileprivate let notiModelUrl:String = "EXTRA"

/*: "msgInfo" :*/
fileprivate let show_messageUrl:String = "error"
fileprivate let kViewDataTrueId:[Character] = ["g","I","n","f","o"]

/*: "user" :*/
fileprivate let userCenterContent:String = "ufilterer"

/*: "uid" :*/
fileprivate let main_willData:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let app_topKey:[UInt8] = [0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e]

/*: "headPic" :*/
fileprivate let showCountText:[UInt8] = [0x63,0x69,0x50,0x64,0x61,0x65,0x68]

/*: "age" :*/
fileprivate let constTransformData:[UInt8] = [0xc,0xa,0x8]

private func nameDismiss(cell num: UInt8) -> UInt8 {
    return num ^ 109
}

/*: "gift" :*/
fileprivate let show_logId:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let noti_leadingId:[Character] = ["i","s","G","r","o","u","p"]

/*: "WantGift" :*/
fileprivate let user_modelMessage:[Character] = ["W","a","n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let userKeyStr:[Character] = ["t","x","t"]

/*: "img" :*/
fileprivate let const_requestResultText:String = "ihidden"

/*: "video" :*/
fileprivate let noti_availableUrl:String = "vimakeo"

/*: "audio" :*/
fileprivate let kInfoText:String = "bodyudio"

/*: "messageType" :*/
fileprivate let k_ofGiftContent:String = "messaadde"
fileprivate let showToMessage:[Character] = ["T","y","p","e"]

/*: "contentType" :*/
fileprivate let notiTitleData:String = "cgo"
fileprivate let k_cellRequestMsg:[Character] = ["n","t","e","n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let userBlockMessage:String = "error viewImgMsg"

/*: "imageUri" :*/
fileprivate let appGreenId:String = "imwith"

/*: "type" :*/
fileprivate let user_beginPushId:String = "typfile"

/*: "pic" :*/
fileprivate let appCenterData:[Character] = ["p","i","c"]

/*: "isBurn" :*/
fileprivate let kDataValue:String = "message range top countisBurn"

/*: "giftId" :*/
fileprivate let notiOfStr:String = "item"
fileprivate let main_alwaysModePath:String = "iftIdclick all class range"

/*: "params" :*/
fileprivate let user_strengthTouchListPath:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let k_equalMsg:[Character] = ["a","u","d","i","o","L","e","n","g","t"]
fileprivate let appMaleTitle:String = "loop"

/*: "len" :*/
fileprivate let appContainerText:String = "ltimen"

/*: "audioSandbox" :*/
fileprivate let appHandleFormat:[Character] = ["a","u","d","i","o","S","a"]
fileprivate let constLeadingLoadUrl:[Character] = ["n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let dataModelPath:String = "bottom to let close selfsenderId"

/*: "AudioMsg" :*/
fileprivate let app_fileTitle:String = "cell exhibit count change beautyAudioMsg"

/*: "content" :*/
fileprivate let show_shareName:String = "CONTENT"

/*: "TxtMsg" :*/
fileprivate let mainModelValue:String = "at class viewTxtMsg"

/*: "message" :*/
fileprivate let noti_cellFormat:String = "messimageg"
fileprivate let kWithMagnitudePath:String = "E"

/*: "quoteMsgId" :*/
fileprivate let data_cameraDateStr:String = "quoteMheight if equal player color"

/*: "quoteMsgInfo" :*/
fileprivate let appMakeData:[Character] = ["q","u","o","t","e","M","s","g"]
fileprivate let notiMessageFormat:String = "count make selfInfo"

/*: "rYMsgType" :*/
fileprivate let const_frameMenuFormat:String = "hidden var key blockrYMsg"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let kErrorName:[Character] = ["G","J",":","S","e"]
fileprivate let kBottomValue:String = "make near import i mainss:Au"
fileprivate let userExceptionTitle:String = "eet:view suspend"
fileprivate let showSucceedValue:[Character] = ["T","x","t","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let userOutputMsg:String = "centerhan"
fileprivate let const_layerKey:String = "geDiresuper view block length"

/*: "system" :*/
fileprivate let noti_contentId:String = "systeuser"

/*: "title" :*/
fileprivate let const_rawPath:String = "titvalue"

/*: "id" :*/
fileprivate let app_videoMessage:[Character] = ["i","d"]

/*: "icon" :*/
fileprivate let k_equalPath:String = "icostyle"

/*: "all" :*/
fileprivate let noti_contentFormat:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let const_toPath:String = "icon_chatrow in field extension"
fileprivate let app_extendedStr:String = "s_pcrlet make intimate"

/*: "status" :*/
fileprivate let show_giftStr:String = "coloratus"

/*: "msgIncome" :*/
fileprivate let notiModelFromValue:String = "status"
fileprivate let k_blockInfoContent:String = "sgIncomeyear for behavior"

/*: "msgId" :*/
fileprivate let showWithTaskInfoKey:[Character] = ["m","s","g","I","d"]

/*: "DispensationNumeric" :*/
fileprivate let data_userId:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","S","t"]
fileprivate let data_dismissMessage:String = "anameus"

/*: "TMsgTranslatedContent" :*/
fileprivate let mainQueryionText:String = "TMsgTruser if"
fileprivate let k_topMsg:String = "view delay view data maxedCon"
fileprivate let app_textColorUrl:String = "tebar"

/*: "formatType" :*/
fileprivate let const_photoStr:[Character] = ["f","o","r","m","a"]
fileprivate let show_frameMsg:String = "tTypecount scale me"

/*: "plain" :*/
fileprivate let kAttributeId:String = "plbarin"

/*: "imageData" :*/
fileprivate let notiCenterMessage:[Character] = ["i"]
fileprivate let main_titleId:String = "current to transformation return imagemageData"

/*: "VideoMsg" :*/
fileprivate let notiTitleStr:String = "click min status fileVide"

/*: "match" :*/
fileprivate let kSeatKey:[Character] = ["m","a","t","c","h"]

/*: "mf" :*/
fileprivate let data_userName:String = "lab"

/*: "userIntro" :*/
fileprivate let notiPlayData:[Character] = ["u","s","e","r","I","n","t","r","o"]

/*: "targetId" :*/
fileprivate let show_makeKey:[Character] = ["t","a","r","g","e","t"]
fileprivate let mainWarningData:[Character] = ["I","d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let main_onUrl:String = "yyyy-MMmonth view to bottom"
fileprivate let kMakeData:[Character] = ["-","d"]
fileprivate let kIndexId:String = "error eventd HH:m"

/*: "sendTime" :*/
fileprivate let dataUserLikePath:[Character] = ["s","e","n","d","T","i","m","e"]

/*: "isRead" :*/
fileprivate let const_effectMessage:String = "isReadadd false view less"

/*: "audioData" :*/
fileprivate let dataModelId:String = "aintimatei"

/*: "audioUri" :*/
fileprivate let notiIncomeName:[Character] = ["a","u","d","i","o","U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TableChatManager.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum PaperQuickLookable: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - TableChatManager

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class TableChatManager: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func fictitiousName() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> QuantityeractionReactiveCompatible? {
    public class func terminal(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> QuantityeractionReactiveCompatible? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = isoclinic(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: DirectConversationListener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            DirectConversationListener.shared.anFunc(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> QuantityeractionReactiveCompatible? {
    public class func isoclinic(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> QuantityeractionReactiveCompatible? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] = (notiFailKey.replacingOccurrences(of: "progress", with: "s"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(notiModelUrl.lowercased()): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = dataQuantityeraction(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TipsReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ClickBottomNameTheoryModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? ByTipsModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(show_messageUrl.replacingOccurrences(of: "error", with: "ms") + String(kViewDataTrueId))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(userCenterContent.replacingOccurrences(of: "filter", with: "s"))] = [
            //: "uid": RegularBeginDecisionMakerAppManager.share.loginUserMode.userID,
            (String(main_willData)): RegularBeginDecisionMakerAppManager.share.loginUserMode.userID,
            //: "nickname": RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname,
            String(bytes: app_topKey.reversed(), encoding: .utf8)!: RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname,
            //: "headPic": RegularBeginDecisionMakerAppManager.share.loginUserMode.headPic,
            String(bytes: showCountText.reversed(), encoding: .utf8)!: RegularBeginDecisionMakerAppManager.share.loginUserMode.headPic,
            //: "age": RegularBeginDecisionMakerAppManager.share.loginUserMode.age,
            String(bytes: constTransformData.map{nameDismiss(cell: $0)}, encoding: .utf8)!: RegularBeginDecisionMakerAppManager.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(notiModelUrl.lowercased()): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = dataQuantityeraction(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (String(show_logId)) {
            //: var data: QuantityeractionReactiveCompatible
            var data: QuantityeractionReactiveCompatible
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(noti_leadingId))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = PtolemaicSystemCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = DotCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = DotCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ClickBottomNameTheoryModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (String(user_modelMessage)) {
            //: let data = BuildCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = BuildCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ClickBottomNameTheoryModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (String(userKeyStr)) {
            //: var data: QuantityeractionReactiveCompatible
            var data: QuantityeractionReactiveCompatible
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(noti_leadingId))] as? String == "1" { // 群聊
                //: data = PathCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = PathCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ButtonCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ButtonCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ClickBottomNameTheoryModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (const_requestResultText.replacingOccurrences(of: "hidden", with: "mg")) {
            //: let data = SuiteCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SuiteCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ClickBottomNameTheoryModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (noti_availableUrl.replacingOccurrences(of: "make", with: "de")) {
            //: let data = CardCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CardCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ClickBottomNameTheoryModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (kInfoText.replacingOccurrences(of: "body", with: "a")) {
            //: let data = AudioCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = AudioCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ClickBottomNameTheoryModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(show_logId))] != nil && (msgInfo[(k_ofGiftContent.replacingOccurrences(of: "add", with: "g") + String(showToMessage))] as? Int) == 4 {
            //: let data = ButtonCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ButtonCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ClickBottomNameTheoryModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: QuantityeractionReactiveCompatible) -> QuantityeractionReactiveCompatible? {
    public class func quote(withData cellData: QuantityeractionReactiveCompatible) -> QuantityeractionReactiveCompatible? {
        // 图片
        //: if let reCellData = cellData as? SuiteCellData {
        if let reCellData = cellData as? SuiteCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(ColorDelimitateMacroDefine.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(ColorDelimitateMacroDefine.toponymTo())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(notiTitleData.replacingOccurrences(of: "go", with: "o") + String(k_cellRequestMsg))] = (String(userBlockMessage.suffix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] = (const_requestResultText.replacingOccurrences(of: "hidden", with: "mg"))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(appGreenId.replacingOccurrences(of: "with", with: "ag") + "eUri")] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(show_messageUrl.replacingOccurrences(of: "error", with: "ms") + String(kViewDataTrueId))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(user_beginPushId.replacingOccurrences(of: "file", with: "e"))] = (String(userBlockMessage.suffix(6)))
            //: params["pic"] = filePath
            params[(String(appCenterData))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(kDataValue.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(kDataValue.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(notiOfStr.replacingOccurrences(of: "item", with: "g") + String(main_alwaysModePath.prefix(5)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(notiOfStr.replacingOccurrences(of: "item", with: "g") + String(main_alwaysModePath.prefix(5)))] = "0"
            }

            //: dic["params"] = params
            dic[(String(user_strengthTouchListPath))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = isoclinic(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? CardCellData {
        if let reCellData = cellData as? CardCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? AudioCellData {
        if let reCellData = cellData as? AudioCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = InfoDetailThen.isochronalMessage(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(k_equalMsg) + appMaleTitle.replacingOccurrences(of: "loop", with: "h"))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(appContainerText.replacingOccurrences(of: "time", with: "e"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(appHandleFormat) + String(constLeadingLoadUrl))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(appHandleFormat) + String(constLeadingLoadUrl))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] = (kInfoText.replacingOccurrences(of: "body", with: "a"))
            //: msgInfo["senderId"] = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
            msgInfo[(String(dataModelPath.suffix(8)))] = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(show_messageUrl.replacingOccurrences(of: "error", with: "ms") + String(kViewDataTrueId))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(user_beginPushId.replacingOccurrences(of: "file", with: "e"))] = (String(app_fileTitle.suffix(8)))
            //: dic["params"] = params
            dic[(String(user_strengthTouchListPath))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = isoclinic(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ButtonCellData {
        if let reCellData = cellData as? ButtonCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(show_shareName.lowercased())] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] = (String(userKeyStr))
            //: dic["msgInfo"] = msgInfo
            dic[(show_messageUrl.replacingOccurrences(of: "error", with: "ms") + String(kViewDataTrueId))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(user_beginPushId.replacingOccurrences(of: "file", with: "e"))] = (String(mainModelValue.suffix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(noti_cellFormat.replacingOccurrences(of: "image", with: "a") + kWithMagnitudePath.lowercased())] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(data_cameraDateStr.prefix(6)) + "sgId")] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(appMakeData) + String(notiMessageFormat.suffix(4)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(user_strengthTouchListPath))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = isoclinic(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> QuantityeractionReactiveCompatible? {
    public class func tantamount(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> QuantityeractionReactiveCompatible? {
        //: var cellData: QuantityeractionReactiveCompatible?
        var cellData: QuantityeractionReactiveCompatible?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(notiModelUrl.lowercased())] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(show_messageUrl.replacingOccurrences(of: "error", with: "ms") + String(kViewDataTrueId))] as? [String: Any]

        //: let user = extraDict?["user"] as? [String: Any] ?? [:]
        let user = dic[(userCenterContent.replacingOccurrences(of: "filter", with: "s"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ClickBottomNameTheoryModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(const_frameMenuFormat.suffix(5)) + "Type")] as? String == (String(kErrorName) + String(kBottomValue.suffix(5)) + "toGr" + String(userExceptionTitle.prefix(4)) + String(showSucceedValue)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(userOutputMsg.replacingOccurrences(of: "center", with: "c") + String(const_layerKey.prefix(6)) + "ction")] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = sinceTo(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (noti_contentId.replacingOccurrences(of: "user", with: "m")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ClickBottomNameTheoryModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? ByTipsModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TipsReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(show_shareName.lowercased())] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = SuiteCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SuiteCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = CardCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CardCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PtolemaicSystemCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = DotCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = DotCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = developmentPush(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(notiModelUrl.lowercased())] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(show_messageUrl.replacingOccurrences(of: "error", with: "ms") + String(kViewDataTrueId))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? NumberModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = BuildCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BuildCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = AudioCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = AudioCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? AudioCellData {
            if let audioCellData = cellData as? AudioCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = reachResult(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = ControlThen.makeWith(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = ControlThen.overItemDb(packet(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ShadowCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(const_rawPath.replacingOccurrences(of: "value", with: "le"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(show_shareName.lowercased())] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(const_requestResultText.replacingOccurrences(of: "hidden", with: "mg"))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = HypervelocityCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = PathCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PathCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ButtonCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ButtonCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? WrapModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(k_equalPath.replacingOccurrences(of: "style", with: "n"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: RegularBeginDecisionMakerAppManager.share.loginUserMode.headPic ?? ""
                        RegularBeginDecisionMakerAppManager.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = NumberModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(show_shareName.lowercased())] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(notiModelUrl.lowercased())] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = DigitizerModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(noti_contentFormat))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(const_toPath.prefix(9)) + String(app_extendedStr.prefix(5)))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.lie(withJsonString: customStr) ?? [:]
        //: let status = TMsgStatus(rawValue: UInt(customDic["status"] as? Int ?? 0)) ?? .Msg_Status_Init
        let statusString = customDic["status"] as? String ?? "0"
        let status = TMsgStatus(rawValue: UInt(statusString) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(notiModelFromValue.replacingOccurrences(of: "status", with: "m") + String(k_blockInfoContent.prefix(8)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(k_ofGiftContent.replacingOccurrences(of: "add", with: "g") + String(showToMessage))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(showWithTaskInfoKey))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["DispensationNumeric"] as? Int {
        if let translateStatus = customDic[(String(data_userId) + data_dismissMessage.replacingOccurrences(of: "name", with: "t"))] as? Int {
            //: cellData?.translateStatus = DispensationNumeric(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = DispensationNumeric(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(mainQueryionText.prefix(6)) + "anslat" + String(k_topMsg.suffix(5)) + app_textColorUrl.replacingOccurrences(of: "bar", with: "nt"))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = AlongModel.loadLocalIntimatePhotoDict()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = AlongModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = YankModel.loadLocalIntimateVideoDict()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = YankModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = CurrentModel.loadAskForGiftArr()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func sinceTo(_ extraDic: [String: Any]) -> PaperQuickLookable {
        //: guard let dic = extraDic["extra"] as? [String: Any],
        let dic = extraDic[(notiModelUrl.lowercased())] as? [String: Any]
        //: let msgInfo = dic["msgInfo"] as? [String: Any],
        let msgInfo = dic?[(show_messageUrl.replacingOccurrences(of: "error", with: "ms") + String(kViewDataTrueId))] as? [String: Any]
              //: let model = try? AbTalkingChatMsgBaseModel(dictionary: dic)
        guard let model = try? ClickBottomNameTheoryModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic["formatType"] as? String == "plain" {
        if dic?[(String(const_photoStr) + String(show_frameMsg.prefix(5)))] as? String == (kAttributeId.replacingOccurrences(of: "bar", with: "a")) {
            //: let imageUri = dic["imageUri"] as? String ?? ""
            let imageUri = dic?[(appGreenId.replacingOccurrences(of: "with", with: "ag") + "eUri")] as? String ?? ""
            //: let imageData = dic["imageData"] as? String ?? ""
            let imageData = dic?[(String(notiCenterMessage) + String(main_titleId.suffix(8)))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(notiTitleStr.suffix(4)) + "oMsg") {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo)
        let isVoiceStyle = can(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic["match"] as? Int) == 1 || (msgInfo["match"] as? Int) == 1 {
        if (dic?[(String(kSeatKey))] as? Int) == 1 || (msgInfo?[(String(kSeatKey))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic["msgType"] as? String == "tips" || (dic["messageType"] as? Int) == 17 {
        if dic?[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (notiFailKey.replacingOccurrences(of: "progress", with: "s")) || (dic?[(k_ofGiftContent.replacingOccurrences(of: "add", with: "g") + String(showToMessage))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic["formatType"] as? String == "mf") && ((dic["messageType"] as? Int) == 15 || (dic["messageType"] as? Int) == 16) {
        if (dic?[(String(const_photoStr) + String(show_frameMsg.prefix(5)))] as? String == "mf") && ((dic?[(k_ofGiftContent.replacingOccurrences(of: "add", with: "g") + String(showToMessage))] as? Int) == 15 || (dic?[(k_ofGiftContent.replacingOccurrences(of: "add", with: "g") + String(showToMessage))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic["msgType"] as? String == "userIntro" {
        if dic?[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (String(notiPlayData)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo["msgType"] as? String == "WantGift" || (msgInfo["messageType"] as? Int) == 25 {
        if msgInfo?[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String == (String(user_modelMessage)) || (msgInfo?[(k_ofGiftContent.replacingOccurrences(of: "add", with: "g") + String(showToMessage))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func packet(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.lie(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(notiModelUrl.lowercased())] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(show_messageUrl.replacingOccurrences(of: "error", with: "ms") + String(kViewDataTrueId))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(userCenterContent.replacingOccurrences(of: "filter", with: "s"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(dataModelPath.suffix(8)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID {
        if senderId == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(dataModelPath.suffix(8)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(show_makeKey) + String(mainWarningData))] = message.userID ?? (user?[(String(show_makeKey) + String(mainWarningData))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(dataModelPath.suffix(8)))] = "\(msgInfo?[(String(main_willData))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(show_makeKey) + String(mainWarningData))] = "\(msgInfo?[(String(main_willData))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(showWithTaskInfoKey))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(showWithTaskInfoKey))] = "\(msgInfo?[(String(showWithTaskInfoKey))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.lie(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(showWithTaskInfoKey))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(showWithTaskInfoKey))] = "\(customDic[(String(showWithTaskInfoKey))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(showWithTaskInfoKey))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(appHandleFormat) + String(constLeadingLoadUrl))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(appHandleFormat) + String(constLeadingLoadUrl))] = "\(msgInfo?[(String(appHandleFormat) + String(constLeadingLoadUrl))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(k_equalMsg) + appMaleTitle.replacingOccurrences(of: "loop", with: "h"))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(k_equalMsg) + appMaleTitle.replacingOccurrences(of: "loop", with: "h"))] = "\(msgInfo?[(String(k_equalMsg) + appMaleTitle.replacingOccurrences(of: "loop", with: "h"))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(main_onUrl.prefix(7)) + String(kMakeData) + String(kIndexId.suffix(6)) + "m:ss")
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(dataUserLikePath))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID ? "1" : "0"
        info?[(String(const_effectMessage.prefix(6)))] = senderId == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func reachResult(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = packet(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(showWithTaskInfoKey))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func developmentPush(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func dataQuantityeraction(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func can(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(constTableFeeKey.suffix(6)) + String(kIndexKey))] as? String, msgType == (kInfoText.replacingOccurrences(of: "body", with: "a")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(notiTitleData.replacingOccurrences(of: "go", with: "o") + String(k_cellRequestMsg))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(app_fileTitle.suffix(8)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(dataModelId.replacingOccurrences(of: "intimate", with: "ud") + "oData")] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(notiIncomeName))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
