
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let constFitTitle:String = "kind view self objectIM退出登"
fileprivate let show_userRankStr:String = "code:target date make"

/*: , desc: :*/
fileprivate let userCancelData:String = ", desuser equal self"
fileprivate let user_blockData:[Character] = ["c",":"]

/*: ." :*/
fileprivate let user_afterName:String = "tool"

/*: "IM登录失败，code: :*/
fileprivate let notiEqualCreateFormat:[Character] = ["I","M","登","录"]
fileprivate let app_labelManagerKey:[Character] = ["\u{5931}","败","，","c","o","d","e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let notiEffectTitle:String = "IM获取view top bar status fill"
fileprivate let app_normalRangeMessage:String = "失败\u{ff0c}code"
fileprivate let kContainerViewStr:[Character] = [":"]

/*: "crushBadge" :*/
fileprivate let constColorExceptionName:String = "ccellsh"
fileprivate let const_lipTargetId:String = "turn selfBadge"

/*: "badNumber" :*/
fileprivate let const_imageStr:[UInt8] = [0x2c,0x2b,0x2e,0x18,0x3f,0x37,0x2c,0x2f,0x3c]

fileprivate func computerFilename(game num: UInt8) -> UInt8 {
    let value = Int(num) - 202
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let k_makeKeyUrl:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let k_textLineEqualFormat:String = "roomNumtype model fill to"

/*: "num" :*/
fileprivate let main_toMsg:String = "nushared"

/*: "onlyOneRoomId" :*/
fileprivate let noti_imageValue:String = "odownly"
fileprivate let app_loadEqualContent:String = "oomIdlayer menu as share"

/*: "LIVE-CHAT" :*/
fileprivate let show_modeRecordMessage:String = "LIVmake"

/*: "PARTY-CHAT" :*/
fileprivate let user_dataTitle:String = "PARTY-CHAsource table"
fileprivate let mainBiologyPath:String = "position"

/*: "extra" :*/
fileprivate let appMakeUrl:String = "einfora"

/*: "msgType" :*/
fileprivate let app_viewResultFormat:String = "error manager crush youmsgType"

/*: "newPkgGift" :*/
fileprivate let const_imageId:[Character] = ["n","e","w","P","k"]
fileprivate let user_lengthPreviousUrl:[Character] = ["g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let notiTitleStr:String = "add atMF:Ch"
fileprivate let noti_statusTitle:String = "nMsgfor push path top"

/*: "uid" :*/
fileprivate let app_managerMsg:[UInt8] = [0x64,0x69,0x75]

/*: "onlineStatus" :*/
fileprivate let userLineUrl:[UInt8] = [0x1b,0x1a,0x18,0x1d,0x1a,0x11,0x27,0x0,0x15,0x0,0x1,0x7]

private func mostValuablePlayer(last num: UInt8) -> UInt8 {
    return num ^ 116
}

/*: "isNewUser" :*/
fileprivate let userOfTitle:[UInt8] = [0x3c,0x46,0x21,0x38,0x4a,0x28,0x46,0x38,0x45]

fileprivate func borderRequest(role num: UInt8) -> UInt8 {
    let value = Int(num) - 211
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "c2c_ :*/
fileprivate let showModelName:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let main_rankName:String = "IM获取title in title path"
fileprivate let show_emptyRowStr:String = "parent if target white replacementnvId:"

/*: , code: :*/
fileprivate let notiKitSuiteMakeKey:String = ", code:color user name"

/*: "IM插入消息失败，targetId: :*/
fileprivate let const_statusId:String = "IM\u{63d2}入消息"
fileprivate let notiHiddenModelText:[Character] = ["g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let data_locationMsg:String = "livehange"
fileprivate let const_bankKey:String = "cgifton"

/*: "rYMsgType" :*/
fileprivate let user_installMsg:String = "rYMsformat right gift if equal"

/*: "GJ:CallCustom" :*/
fileprivate let kVideoFormat:String = "GJ:Callcolor session device size present"
fileprivate let user_nameViewPath:[Character] = ["C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let show_officialMsg:[Character] = ["I","M","\u{83b7}","取","会","话","列","表","\u{5931}"]
fileprivate let mainUserPath:String = "败"
fileprivate let appTermsId:String = "，code:full in color photo"

/*: "msgInfo" :*/
fileprivate let kMainId:String = "string item row withmsgInfo"

/*: "needFold" :*/
fileprivate let constRawFirstPath:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let showViewContent:String = "kind level namegroup_"

/*: "IM删除会话失败，convID: :*/
fileprivate let main_userPathName:String = "IM\u{5220}除会"
fileprivate let showColorId:String = "nvID:leading self in"

/*: "IM置顶会话失败，convId: :*/
fileprivate let appAddTextFormat:String = "than frameIM置顶"
fileprivate let const_makeData:String = "key"
fileprivate let appSegmentShowKey:[Character] = ["n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let appPlainMsg:[Character] = ["S"]
fileprivate let constItemValue:[Character] = ["D","K"," ","正","在","连","接","到","服","务","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let data_elementMsg:[Character] = ["S","D","K"," ","已","经","成","功","连"]
fileprivate let user_errorTitle:String = "接到服务器"

/*: "IM连接服务器失败，code: :*/
fileprivate let data_labelStr:[Character] = ["I","M","连","接","服","\u{52a1}","器","失","\u{8d25}","，","c","o","d","e",":"]

/*: , error: :*/
fileprivate let showMakeUrl:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let kMainPhotoName:String = "IM账号被挤下线data let center"
fileprivate let mainViewTitle:String = "to"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let appPicMsg:[UInt8] = []

fileprivate func fillModel(time num: UInt8) -> UInt8 {
    let value = Int(num) - 57
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Offline Notification" :*/
fileprivate let kMediumTitle:[Character] = ["O","f","f","l","i","n","e"," ","N","o","t","i","f","i","c","a","t"]
fileprivate let notiEqualBlockMakeId:String = "itargetn"

/*: "Cancel" :*/
fileprivate let main_viewUrl:String = "self enterCancel"

/*: "Re Login" :*/
fileprivate let data_arrayText:String = "self type make background itemRe"
fileprivate let constWorkValue:String = " Logindata equal regular"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DirectConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol PeculiarVelocityManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func playerModelData(data: [RegularConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func searchMagnitude(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func sizeMsg(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum VoiceOverDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class DirectConversationListener: NSObject {
@objcMembers public class DirectConversationListener: NSObject {
    //: public static let shared = DirectConversationListener()
    public static let shared = DirectConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [RegularConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [RegularConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [RegularConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [RegularConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [RegularConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: RegularConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.showTransformInitialization()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func outFormatInit() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(k_giftFormat, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func showTransformInitialization() {
        //: func__removeAllDelegate()
        bag()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.titleAcrossTo()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func titleAcrossTo() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = RegularConversationModel.kindButton(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = RegularConversationModel.kindButton(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func dower() -> [RegularConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == ColorDelimitateMacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == ColorDelimitateMacroDefine.limpidityAppWork() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension DirectConversationListener {
extension DirectConversationListener {
    //: func func__removeAllDelegate() {
    func bag() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func upView(_ delegate: PeculiarVelocityManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func possibility(_ delegate: PeculiarVelocityManagerDelegate) {
        //: self.func__invokeDelegate {
        self.dataSendDelegate {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func dataSendDelegate(_ invocation: (PeculiarVelocityManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! PeculiarVelocityManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension DirectConversationListener {
extension DirectConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func sessionDownLine() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            CleanTargetLogTool.yearShow(msg: (String(constFitTitle.suffix(5)) + "录\u{5931}\u{8d25}，" + String(show_userRankStr.prefix(5))) + "\(code)" + (String(userCancelData.prefix(5)) + String(user_blockData)) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.showTransformInitialization()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func inCompletion(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = RegularBeginDecisionMakerAppManager.share.appUserConfigMode.uSig
        let uSigStr = RegularBeginDecisionMakerAppManager.share.appUserConfigMode.uSig
        //: let uidStr = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
        let uidStr = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.onCollection()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            CleanTargetLogTool.yearShow(msg: (String(notiEqualCreateFormat) + String(app_labelManagerKey)) + "\(code)" + (String(userCancelData.prefix(5)) + String(user_blockData)) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func listUp() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || RegularBeginDecisionMakerAppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || RegularBeginDecisionMakerAppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.statusTo(showMsg: constInputText)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension DirectConversationListener {
extension DirectConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func onCollection() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            CleanTargetLogTool.yearShow(msg: (String(notiEffectTitle.prefix(4)) + "未读消息总\u{6570}" + app_normalRangeMessage + String(kContainerViewStr)) + "\(code)" + (String(userCancelData.prefix(5)) + String(user_blockData)) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func refresh() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: PictureRequestTool.req_sysMessageList { _, result in
        PictureRequestTool.keyText { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(constColorExceptionName.replacingOccurrences(of: "cell", with: "ru") + String(const_lipTargetId.suffix(5)))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: main_barMessage, object: self, userInfo: [String(bytes: const_imageStr.map{computerFilename(game: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func dataConverter(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: PictureRequestTool.req_getChatRoomInfo { succeed, result, _ in
        PictureRequestTool.chatCompletion { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(k_makeKeyUrl))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(k_textLineEqualFormat.prefix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = RegularConversationModel.kindButton(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(main_toMsg.replacingOccurrences(of: "shared", with: "m"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(noti_imageValue.replacingOccurrences(of: "down", with: "n") + "OneR" + String(app_loadEqualContent.prefix(5)))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension DirectConversationListener: V2TIMAdvancedMsgListener {
extension DirectConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((show_modeRecordMessage.replacingOccurrences(of: "make", with: "E") + "-CHAT")) || msg.groupID.contains((String(user_dataTitle.prefix(9)) + mainBiologyPath.replacingOccurrences(of: "position", with: "T")))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            SelectPopThen.afterWithShared().systemText(msg: msg)
            //: return
            return
        }
        //: if msg.sender == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID { return }
        if msg.sender == RegularBeginDecisionMakerAppManager.share.loginUserMode.userID { return }

        //: let dic = RoomsChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = RoomsChatManager.visualProperty(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(appMakeUrl.replacingOccurrences(of: "info", with: "xt"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? ClickBottomNameTheoryModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: kQualityUserClickUrl, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(app_viewResultFormat.suffix(7)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(const_imageId) + String(user_lengthPreviousUrl)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: appLiveName, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if RoomsChatManager.checkVoiceMessage(message: msg) {
            if RoomsChatManager.exceptApp(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TableChatManager.packet(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                ControlThen.overItemDb(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.judgeisCurrentProximate(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            ContentAnimatTool.shared.impressDic(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            ContentAnimatTool.shared.isWrite(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(notiTitleStr.suffix(5)) + "atMentio" + String(noti_statusTitle.prefix(4))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            TableConnectionThen.share.random(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: MomentReactiveCompatible.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: MomentReactiveCompatible.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != ColorDelimitateMacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != ColorDelimitateMacroDefine.television() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.dataSendDelegate { $0.sizeMsg?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            SpeedManagerReactiveCompatible.shared.withDict(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: app_managerMsg.reversed(), encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: userLineUrl.map{mostValuablePlayer(last: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: userOfTitle.map{borderRequest(role: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: noti_noUrl,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            InfoDetailThen.tillShow(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            MomentReactiveCompatible.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: main_timeValue, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: QuantityeractionReactiveCompatible, isPrivete: Bool) {
    @objc public func anFunc(targetId: String, msgCellData: QuantityeractionReactiveCompatible, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.primaFacie(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(showModelName)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.observerInList(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.toTopSliceTitles(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                CleanTargetLogTool.yearShow(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func primaFacie(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
        let senderStr = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                CleanTargetLogTool.yearShow(msg: (const_statusId + "失败，tar" + String(notiHiddenModelText)) + "\(targetId)" + (String(notiKitSuiteMakeKey.prefix(7))) + "\(code)" + (String(userCancelData.prefix(5)) + String(user_blockData)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func judgeisCurrentProximate(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(data_locationMsg.replacingOccurrences(of: "live", with: "c") + "Dire" + const_bankKey.replacingOccurrences(of: "gift", with: "ti"))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && ManageressThen.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(user_installMsg.prefix(4)) + "gType")].stringValue == (String(kVideoFormat.prefix(7)) + String(user_nameViewPath)) && isSelf && ManageressThen.share.afootVc()!.isKind(of: ChatButtonViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: show_limitMessage) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: show_limitMessage)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension DirectConversationListener: V2TIMConversationListener {
extension DirectConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.observerInList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.toTopSliceTitles(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.observerInList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.toTopSliceTitles(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: RegularBeginDecisionMakerAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        RegularBeginDecisionMakerAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.dataSendDelegate {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.searchMagnitude?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func toTopSliceTitles(conVersationList: [RegularConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if InfoDetailThen.saveView().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                InfoDetailThen.saveView().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.dataSendDelegate {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.playerModelData?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func include(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [RegularConversationModel] = []

        // V2TIMConversation->RegularConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = RegularConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.utilizer(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [RegularConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = SetUpInfoTable.pressOut(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.createName(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension DirectConversationListener {
extension DirectConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func more(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.include(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.toTopSliceTitles(conVersationList: arr as? [RegularConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            CleanTargetLogTool.yearShow(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func purchaseFirst(targetId: String) -> RegularConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func observerInList(convList: [V2TIMConversation]) -> [RegularConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [RegularConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = RegularConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.utilizer(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.createName(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func createName(convModel: RegularConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.titleAcrossTo()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: VoiceOverDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - RegularBeginDecisionMakerAppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - RegularBeginDecisionMakerAppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = InfoDetailThen.get(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = RoomsChatManager.parseTXMessageData(data: elemData)
            let dict = RoomsChatManager.visualProperty(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(appMakeUrl.replacingOccurrences(of: "info", with: "xt"))][(String(kMainId.suffix(7)))][(String(constRawFirstPath))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.toneTo(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.toneTo(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func toneTo(convList: [RegularConversationModel], dataType: VoiceOverDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func imageIncome(targetId: String, dataType: VoiceOverDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(showModelName)) + "\(targetId)" : (String(showViewContent.suffix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            InfoDetailThen.sizeBlock(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID, toUid: targetId)
            ControlThen.placeUid(RegularBeginDecisionMakerAppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.onCollection()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            CleanTargetLogTool.yearShow(msg: (main_userPathName + "话失\u{8d25}，co" + String(showColorId.prefix(5))) + "\(convID)" + (String(notiKitSuiteMakeKey.prefix(7))) + "\(code)" + (String(userCancelData.prefix(5)) + String(user_blockData)) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func equalBy(convModel: RegularConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            CleanTargetLogTool.yearShow(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.toneTo(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.toneTo(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension DirectConversationListener: V2TIMSDKListener {
extension DirectConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(appPlainMsg) + String(constItemValue)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(data_elementMsg) + user_errorTitle.capitalized))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        CleanTargetLogTool.yearShow(msg: (String(data_labelStr)) + "\(code)" + (String(showMakeUrl)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        CleanTargetLogTool.yearShow(msg: (String(kMainPhotoName.prefix(8)) + mainViewTitle.replacingOccurrences(of: "to", with: "。")))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        ActionSocketDelegate.shared.socket(userLoginOut: true)
        //: DirectConversationListener.shared.isConnection = false
        DirectConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (FirstApplicationThen.shared as! FirstApplicationThen).bubble()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: appPicMsg.map{fillModel(time: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = PlayColorAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if PathAtAddrTool.share.interfaceLang == ClickConvertible.ar.rawValue || PathAtAddrTool.share.interfaceLang == ClickConvertible.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            PathAtAddrTool.share.interfaceLang == ClickConvertible.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.mediumPingfangStatusSizeFont(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        ButtonTargetReactiveCompatible.doState()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        ButtonTargetReactiveCompatible.doState(title: (String(kMediumTitle) + notiEqualBlockMakeId.replacingOccurrences(of: "target", with: "o")).localized, message: msg, leftBtnTitle: (String(main_viewUrl.suffix(6))).localized, rightBtnTitle: (String(data_arrayText.suffix(2)) + String(constWorkValue.prefix(6))).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SearchPushListener.biologyLab().numbereraction()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ParlourEventHandler.tarotCard().attribute()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            SuiteReactiveCompatible.agnomen { t in
                //: ProgressHUD.dismiss()
                DescriptionProgressHUD.actionOf()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kSoundStatusValue, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            SuiteReactiveCompatible.requestHandleList { t in
                //: if t {
                if t {
                    //: DirectConversationListener.shared.func__LogingIn { _ in
                    DirectConversationListener.shared.inCompletion { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if SearchPushListener.biologyLab().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(SearchPushListener.biologyLab().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    ActionSocketDelegate.shared.mixUp()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
