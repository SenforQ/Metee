
//: Declare String Begin

/*: "nil" :*/
fileprivate let data_topText:[Character] = ["n","i","l"]

/*: "Edit profiles" :*/
fileprivate let appResponseKey:String = "Edit pkit var"
fileprivate let userSendData:String = "S"

/*: "Done" :*/
fileprivate let show_toContent:[Character] = ["D","o","n","e"]

/*: "#999999" :*/
fileprivate let showFemaleFormat:String = "#9999"
fileprivate let kUseName:String = "99"

/*: "uid" :*/
fileprivate let k_loadId:[Character] = ["u","i","d"]

/*: "ActionReactiveCompatible" :*/
fileprivate let data_liveKey:String = "choice temporary progress number nameTalk"
fileprivate let userMoreId:String = "itNammodel no"
fileprivate let const_blockValue:String = "image domaineCell"

/*: "SignViewDelegate" :*/
fileprivate let show_toData:[Character] = ["T","a","l","k","i","n","g","E","d","i"]
fileprivate let k_sizeBeginPath:String = "tSigself input"

/*: "Save the change?" :*/
fileprivate let showMakeWhoData:[Character] = ["S","a","v","e"," "]
fileprivate let noti_colorMessage:[Character] = ["t","h","e"," ","c","h"]
fileprivate let mainIconMakeName:String = "let container string noange?"

/*: "Cancel" :*/
fileprivate let kBackgroundId:[Character] = ["C","a","n","c","e","l"]

/*: "Save" :*/
fileprivate let constPhotoMsg:String = "top you iSave"

/*: "nickname" :*/
fileprivate let data_topMessage:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let appViewMsg:[Character] = ["b","i","r","t","h"]
fileprivate let dataGreenEqualChangeStr:String = "dlocationy"

/*: "desc" :*/
fileprivate let app_homeFormat:String = "DESC"

/*: "Modify the success" :*/
fileprivate let app_responseMessage:[Character] = ["M","o","d","i","f","y"," ","t","h","e"," ","s","u","c","c","e","s"]
fileprivate let kPointUrl:[Character] = ["s"]

/*: "pic" :*/
fileprivate let showCenterVersionPath:String = "picorner"

/*: "[ :*/
fileprivate let user_textFormat:String = "success"

/*: ]" :*/
fileprivate let userDoingValueUrl:[Character] = ["]"]

/*: "pids" :*/
fileprivate let main_tableMsg:String = "PIDS"

/*: "aboutMe" :*/
fileprivate let main_modeKey:String = "count"
fileprivate let user_ofTitle:String = "outMepath self view class out"

/*: "interest" :*/
fileprivate let user_smallIndexUrl:[Character] = ["i","n","t","e","r"]
fileprivate let noti_userFormat:[Character] = ["e","s","t"]

/*: "tagIds" :*/
fileprivate let showFrameValue:[Character] = ["t","a","g","I","d","s"]

/*: "category" :*/
fileprivate let app_namePriceMagnitudeMessage:[Character] = ["c","a","t","e","g","o","r"]
fileprivate let showEndValue:String = "corner"

/*: "An error occurred, please try again" :*/
fileprivate let data_modelText:[UInt8] = [0xd6,0xf9,0xb7,0xf2,0xe5,0xe5,0xf8,0xe5,0xb7,0xf8,0xf4,0xf4,0xe2,0xe5,0xe5,0xf2,0xf3,0xbb,0xb7,0xe7,0xfb,0xf2,0xf6,0xe4,0xf2,0xb7,0xe3,0xe5,0xee,0xb7,0xf6,0xf0,0xf6,0xfe,0xf9]

private func periodicTable(a num: UInt8) -> UInt8 {
    return num ^ 151
}

/*: "imgUrl" :*/
fileprivate let show_shareMessage:String = "imgUradd make user text touch"
fileprivate let show_retToText:String = "count"

/*: "videoUrl" :*/
fileprivate let user_titleLabMessage:String = "videoUrlview value"

/*: "Video cover submitted successfully" :*/
fileprivate let user_bottomCellSuperStr:[UInt8] = [0x7b,0x44,0x49,0x48,0x42,0xd,0x4e,0x42,0x5b,0x48,0x5f,0xd,0x5e,0x58,0x4f,0x40,0x44,0x59,0x59,0x48,0x49,0xd,0x5e,0x58,0x4e,0x4e,0x48,0x5e,0x5e,0x4b,0x58,0x41,0x41,0x54]

private func appEqual(send num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "SpeedDotThen" :*/
fileprivate let main_tableValue:String = "view viewTalk"
fileprivate let const_arrayPath:String = "ditVidall menu response name image"

/*: "video_url" :*/
fileprivate let mainStatuteLabelMsg:String = "video_than title center"
fileprivate let dataToValue:String = "urequal"

/*: "img_url" :*/
fileprivate let noti_yearModelValue:String = "icon guard index gift ifimg_url"

/*: "status" :*/
fileprivate let main_centerMessage:String = "request"
fileprivate let kLabName:String = "tatmodes"

/*: "PathPopReactiveCompatible" :*/
fileprivate let data_modelFormat:String = "Talktrue user"
fileprivate let data_progressKey:String = "ditPhview extension"
fileprivate let showDocumentName:String = "direction"
fileprivate let kMinUrl:[Character] = ["t","o","C","e","l","l"]

/*: "ConnectViewDelegate" :*/
fileprivate let main_centerStr:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","B","i","r","t","h","d","a","y","C","e","l"]
fileprivate let data_addMessage:String = "tag"

/*: "AcadiaNationalParkViewCell" :*/
fileprivate let user_appEqualValue:String = "view return cellTalking"
fileprivate let kBlackMenuPath:[Character] = ["E","d"]
fileprivate let appCountimateData:String = "itAboubottom source"

/*: "UITableViewCell" :*/
fileprivate let data_aName:String = "kit name cell from atUITa"
fileprivate let mainBelowId:String = "ewCelreturn female add view var"
fileprivate let dataRowName:String = "equal"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SubViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class SubViewController: ScoreViewController {
	var rejectQuantity: Double = -16.8
	var bindKindArray: [AnyHashable] = []

    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [ViewTransformable] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [ViewTransformable] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [PopModelType] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (String(data_topText))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.objectRed()
        //: self.title = "Edit profiles".localized
        self.title = (String(appResponseKey.prefix(6)) + "rofile" + userSendData.lowercased()).localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(show_toContent)).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (showFemaleFormat.capitalized + kUseName.capitalized)), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.pingfangCenter(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(infoView), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = RegularBeginDecisionMakerAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = RegularBeginDecisionMakerAppManager.share.loginUserMode.aboutMe!
        //: seleteInters = RegularBeginDecisionMakerAppManager.share.loginUserMode.interest!
        seleteInters = RegularBeginDecisionMakerAppManager.share.loginUserMode.interest!
        //: designView()
        constraintView()
        //: GetGallery()
        underGet()
        //: getVideoInfo()
        overObject()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(someDelete(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    
            if (self.edgesForExtendedLayout == .left) && (self.tabBarItem.badgeValue == " ") {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let likeTotal = ScrubController()
            likeTotal.countName = signStr
            
            likeTotal.emptySum = { [self] userInterval in
            self.rejectQuantity = userInterval
            
                self.rejectQuantity -= 1
                if self.rejectQuantity != 65 {
                    self.rejectQuantity = self.rejectQuantity + 1
                }
            return self.rejectQuantity
            }
                self.navigationController?.present(likeTotal.self, animated: true) { [self] in
            self.bindKindArray = []
                }
            }

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        DescriptionProgressHUD.actionOf()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension SubViewController {
    //: func GetGallery() {
    func underGet() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
        dict[(String(k_loadId))] = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        MeButtonThen.drawUp(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<PopModelType>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [PopModelType])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func appFailure() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(data_liveKey.suffix(4)) + "ingEd" + String(userMoreId.prefix(5)) + String(const_blockValue.suffix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: ActionReactiveCompatible = i as! ActionReactiveCompatible
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(show_toData) + String(k_sizeBeginPath.prefix(4)) + "nCell") {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: SignViewDelegate = i as! SignViewDelegate
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&RegularBeginDecisionMakerAppManager.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && RegularBeginDecisionMakerAppManager.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && RegularBeginDecisionMakerAppManager.share.loginUserMode.birthday != self.birthday) || (signStr != (String(data_topText)) && RegularBeginDecisionMakerAppManager.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = PlayColorAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            ButtonTargetReactiveCompatible.equalAlert(message: (String(showMakeWhoData) + String(noti_colorMessage) + String(mainIconMakeName.suffix(5))).localized, leftBtnTitle: (String(kBackgroundId)).localized, rightBtnTitle: (String(constPhotoMsg.suffix(4))).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                ButtonTargetReactiveCompatible.obnubilateBlockAlert()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                ButtonTargetReactiveCompatible.obnubilateBlockAlert()
                //: self.uploadTool(isBack: true)
                self.colorPair(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func infoView() {
        //: uploadTool(isBack: false)
        colorPair(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func colorPair(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(data_liveKey.suffix(4)) + "ingEd" + String(userMoreId.prefix(5)) + String(const_blockValue.suffix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: ActionReactiveCompatible = i as! ActionReactiveCompatible
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(show_toData) + String(k_sizeBeginPath.prefix(4)) + "nCell") {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: SignViewDelegate = i as! SignViewDelegate
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(String(data_topMessage))] = nameStr
        }
        //: if birthday.count > 0 && RegularBeginDecisionMakerAppManager.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, RegularBeginDecisionMakerAppManager.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(String(appViewMsg) + dataGreenEqualChangeStr.replacingOccurrences(of: "location", with: "a"))] = birthday
        }
        //: if signStr != "nil" && RegularBeginDecisionMakerAppManager.share.loginUserMode.signature != signStr {
        if signStr != (String(data_topText)), RegularBeginDecisionMakerAppManager.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(app_homeFormat.lowercased())] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        suite(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.female.rawValue {
            //: uploadVideo()
            writeDown()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        DescriptionProgressHUD.toChange()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        MeButtonThen.bottomCompletion(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            DescriptionProgressHUD.actionOf()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.clickFill(showMsg: (String(app_responseMessage) + String(kPointUrl)).localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname = self.nameStr
                    RegularBeginDecisionMakerAppManager.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: RegularBeginDecisionMakerAppManager.share.loginUserMode.birthday = self.birthday
                    RegularBeginDecisionMakerAppManager.share.loginUserMode.birthday = self.birthday
                }
                //: RegularBeginDecisionMakerAppManager.share.loginUserMode.signature = self.signStr
                RegularBeginDecisionMakerAppManager.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.statusTo(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func suite(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            save()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                weddingPictureQuery()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            nameType(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            nameType(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func weddingPictureQuery() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            //: dict["pic"] = resultData
            dict[(showCenterVersionPath.replacingOccurrences(of: "corner", with: "c"))] = resultData
            //: ProgressHUD.show()
            DescriptionProgressHUD.toChange()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            MeButtonThen.withCompletion(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                DescriptionProgressHUD.actionOf()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.clickFill(showMsg: (String(app_responseMessage) + String(kPointUrl)).localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.underGet()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func save() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(main_tableMsg.lowercased())] = str
        //: ProgressHUD.show()
        DescriptionProgressHUD.toChange()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        MeButtonThen.valueFileCompletion(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.weddingPictureQuery()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                DescriptionProgressHUD.actionOf()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.clickFill(showMsg: (String(app_responseMessage) + String(kPointUrl)).localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.underGet()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func nameType(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [ViewTransformable] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (main_modeKey.replacingOccurrences(of: "count", with: "ab") + String(user_ofTitle.prefix(5)))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (String(user_smallIndexUrl) + String(noti_userFormat))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(showFrameValue))] = str
        //: dict["category"] = category
        dict[(String(app_namePriceMagnitudeMessage) + showEndValue.replacingOccurrences(of: "corner", with: "y"))] = category
        //: ProgressHUD.show()
        DescriptionProgressHUD.toChange()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        MeButtonThen.re(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            DescriptionProgressHUD.actionOf()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.clickFill(showMsg: (String(app_responseMessage) + String(kPointUrl)).localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: RegularBeginDecisionMakerAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    RegularBeginDecisionMakerAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: RegularBeginDecisionMakerAppManager.share.loginUserMode.interest = self.seleteInters
                    RegularBeginDecisionMakerAppManager.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func writeDown() {
        //: ProgressHUD.show()
        DescriptionProgressHUD.toChange()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        ViewSaveListener.shared.share(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                DescriptionProgressHUD.actionOf()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.statusTo(showMsg: String(bytes: data_modelText.map{periodicTable(a: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(show_shareMessage.prefix(5)) + show_retToText.replacingOccurrences(of: "count", with: "l"))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(String(user_titleLabMessage.prefix(8)))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            MeButtonThen.upload(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                DescriptionProgressHUD.actionOf()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.statusTo(showMsg: String(bytes: data_modelText.map{periodicTable(a: $0)}, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.clickFill(showMsg: String(bytes: user_bottomCellSuperStr.map{appEqual(send: $0)}, encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(main_tableValue.suffix(4)) + "ingE" + String(const_arrayPath.prefix(6)) + "eoCell") {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: SpeedDotThen = i as! SpeedDotThen
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.destroyDown(videoPlayPath: dict[(String(user_titleLabMessage.prefix(8)))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.barExecute(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func overObject() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        MeButtonThen.titleCompletion { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(String(mainStatuteLabelMsg.prefix(6)) + dataToValue.replacingOccurrences(of: "equal", with: "l"))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(noti_yearModelValue.suffix(7)))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(main_centerMessage.replacingOccurrences(of: "request", with: "s") + kLabName.replacingOccurrences(of: "mode", with: "u"))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension SubViewController {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func someDelete(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(data_liveKey.suffix(4)) + "ingEd" + String(userMoreId.prefix(5)) + String(const_blockValue.suffix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: ActionReactiveCompatible = i as! ActionReactiveCompatible
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(show_toData) + String(k_sizeBeginPath.prefix(4)) + "nCell") {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: SignViewDelegate = i as! SignViewDelegate
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension SubViewController: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if RegularBeginDecisionMakerAppManager.share.loginUserMode.sex == ScoreScalarLiteral.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((notiEnabledRecordUrl - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.along(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.along(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(main_tableValue.suffix(4)) + "ingE" + String(const_arrayPath.prefix(6)) + "eoCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? SpeedDotThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = SpeedDotThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.barExecute(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.gossipmongering(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.destroyDown(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(data_modelFormat.prefix(4)) + "ingE" + String(data_progressKey.prefix(5)) + showDocumentName.replacingOccurrences(of: "direction", with: "o") + String(kMinUrl))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? PathPopReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = PathPopReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.fromSelete(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.rubric()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(data_liveKey.suffix(4)) + "ingEd" + String(userMoreId.prefix(5)) + String(const_blockValue.suffix(5)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? ActionReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = ActionReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(main_centerStr) + data_addMessage.replacingOccurrences(of: "tag", with: "l"))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? ConnectViewDelegate
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = ConnectViewDelegate(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(show_toData) + String(k_sizeBeginPath.prefix(4)) + "nCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? SignViewDelegate
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = SignViewDelegate(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(user_appEqualValue.suffix(7)) + String(kBlackMenuPath) + String(appCountimateData.prefix(6)) + "tMeCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AcadiaNationalParkViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = AcadiaNationalParkViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.cell()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.firstFor(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.firstFor(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = CorrelationTableViewController()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.deadlineCurrent(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.deadlineCurrent(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.deadlineCurrent(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.deadlineCurrent(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(data_aName.suffix(4)) + "bleVi" + String(mainBelowId.prefix(5)) + dataRowName.replacingOccurrences(of: "equal", with: "l"))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func along(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [ViewTransformable]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if PathAtAddrTool.share.interfaceLang == ClickConvertible.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if PathAtAddrTool.share.interfaceLang == ClickConvertible.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if PathAtAddrTool.share.interfaceLang == ClickConvertible.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangCenter(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > notiEnabledRecordUrl - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - CleanThen

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension SubViewController: CleanThen {
    //: func freshSeleteTag() {
    func dogTag() {
        //: seleteAboutme =  RegularBeginDecisionMakerAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = RegularBeginDecisionMakerAppManager.share.loginUserMode.aboutMe!
        //: seleteInters =  RegularBeginDecisionMakerAppManager.share.loginUserMode.interest!
        seleteInters = RegularBeginDecisionMakerAppManager.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - InsertViewPhotoDelegate

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension SubViewController: InsertViewPhotoDelegate {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func fill(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = PopModelType()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func user(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func redirect(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension SubViewController {
    //: private func designView() {
    private func constraintView() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(SpeedDotThen.self, forCellReuseIdentifier: (String(main_tableValue.suffix(4)) + "ingE" + String(const_arrayPath.prefix(6)) + "eoCell"))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(PathPopReactiveCompatible.self, forCellReuseIdentifier: (String(data_modelFormat.prefix(4)) + "ingE" + String(data_progressKey.prefix(5)) + showDocumentName.replacingOccurrences(of: "direction", with: "o") + String(kMinUrl)))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(ActionReactiveCompatible.self, forCellReuseIdentifier: (String(data_liveKey.suffix(4)) + "ingEd" + String(userMoreId.prefix(5)) + String(const_blockValue.suffix(5))))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(ConnectViewDelegate.self, forCellReuseIdentifier: (String(main_centerStr) + data_addMessage.replacingOccurrences(of: "tag", with: "l")))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(SignViewDelegate.self, forCellReuseIdentifier: (String(show_toData) + String(k_sizeBeginPath.prefix(4)) + "nCell"))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(AcadiaNationalParkViewCell.self, forCellReuseIdentifier: (String(user_appEqualValue.suffix(7)) + String(kBlackMenuPath) + String(appCountimateData.prefix(6)) + "tMeCell"))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
