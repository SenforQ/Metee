
//: Declare String Begin

/*: "cmd" :*/
fileprivate let app_toMsg:[UInt8] = [0x7a,0x74,0x7d]

/*: "cancelCall" :*/
fileprivate let kBeautyFormat:String = "rowncel"

/*: "data" :*/
fileprivate let mainMakeSkinName:[UInt8] = [0x47,0x42,0x57,0x42]

private func deliverTheGoods(frame num: UInt8) -> UInt8 {
    return num ^ 35
}

/*: "logId" :*/
fileprivate let show_indexName:String = "logIdshow result"

/*: "#322870" :*/
fileprivate let user_byUrl:[Character] = ["#","3"]
fileprivate let kViewMessage:String = "22870"

/*: "Tap to start video chatting" :*/
fileprivate let userTitleName:String = "Tap tshared fatal item bind item"
fileprivate let data_equalStr:String = "model report toart "
fileprivate let dataGiftId:String = "pathhatting"

/*: "icon_close_nor" :*/
fileprivate let main_toUrl:[Character] = ["i","c","o","n","_","c"]
fileprivate let notiButtonSizeFormat:String = "lofail"

/*: "icon_views_number" :*/
fileprivate let mainRankId:String = "ime"
fileprivate let noti_pathData:[Character] = ["o","n","_","v","i","e","w","s","_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let main_collectionUrl:String = "%@ cleading center key true"
fileprivate let dataProfileKey:String = "iquitg"

/*: "icon_lounge" :*/
fileprivate let show_mTitle:[Character] = ["i","c","o","n","_","l","o","u","n","g","e"]

/*: "icon_rank_coin" :*/
fileprivate let app_countTitleValue:String = "make groupicon_ra"
fileprivate let showDelayKey:String = "nk_coindata to recording"

/*: "vip_btn_top" :*/
fileprivate let k_viewGiftUrl:String = "m shared false typevip_"
fileprivate let data_styleActualName:String = "equal"

/*: "btn_nearby_settings_nor" :*/
fileprivate let dataModelRangeMessage:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t","i","n","g","s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let app_resultData:String = "%@time"
fileprivate let user_centerFieldData:String = "Coinsbefore load"

/*: "get json error" :*/
fileprivate let dataPopName:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r"]
fileprivate let noti_airName:[Character] = ["r","o","r"]

/*: "matchId" :*/
fileprivate let userDataPath:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let show_statusStr:[UInt8] = [0xc2,0xd5,0xd5,0xc9,0xc8]

private func pointTitle(style num: UInt8) -> UInt8 {
    return num ^ 167
}

/*: "VIPFee" :*/
fileprivate let mainModelId:String = "cell user on open toVIPFee"

/*: "Matching now" :*/
fileprivate let notiPartyId:String = "Matchmic button user"
fileprivate let noti_allPath:String = "iequal"
fileprivate let user_frameMakeMessage:String = "type window sizeg now"

/*: "userTouch" :*/
fileprivate let constImageName:String = "screen"
fileprivate let k_inputMessage:[Character] = ["s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let showLayerActionId:String = "mf/frebottom try"
fileprivate let userReasonFormat:String = "l/getVi pic top self"

/*: "fromType" :*/
fileprivate let app_errorFormat:[UInt8] = [0xea,0xfe,0xe3,0xe1,0xd8,0xf5,0xfc,0xe9]

private func cuttingEdge(equal num: UInt8) -> UInt8 {
    return num ^ 140
}

/*: "You've been barred from receiving calls" :*/
fileprivate let kWhiteName:[UInt8] = [0xd9,0xef,0xf5,0xa7,0xf6,0xe5,0xa0,0xe2,0xe5,0xe5,0xee,0xa0,0xe2,0xe1,0xf2,0xf2,0xe5,0xe4,0xa0,0xe6,0xf2,0xef,0xed,0xa0,0xf2,0xe5,0xe3,0xe5,0xe9,0xf6,0xe9,0xee,0xe7,0xa0,0xe3,0xe1,0xec,0xec,0xf3]

private func pointSize(main num: UInt8) -> UInt8 {
    return num ^ 128
}

/*: "source" :*/
fileprivate let dataSucceedKey:[UInt8] = [0xf9,0xe5,0xff,0xf8,0xe9,0xef]

private func userShared(image num: UInt8) -> UInt8 {
    return num ^ 138
}

/*: "type" :*/
fileprivate let main_frameStr:[UInt8] = [0x11,0x1c,0x15,0x0]

/*: "uid" :*/
fileprivate let userProgressContent:[UInt8] = [0x27,0x3b,0x36]

private func byProduct(crush num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "fromFreeCall" :*/
fileprivate let const_sexPicName:String = "fmakem"
fileprivate let notiSourceValue:[Character] = ["a","l","l"]

/*: "requestCall" :*/
fileprivate let show_videoText:[Character] = ["r","e","q","u","e","s","t","C","a"]
fileprivate let appShareId:String = "last"

/*: "onVideoMatch" :*/
fileprivate let userImageStr:String = "labeln"
fileprivate let dataFrameManagerKey:String = "VideoMsend top component add"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let dataTitleFormat:[UInt8] = [0xcb,0xd1,0xe5,0xe4,0xff,0xfd,0xf1,0xe4,0xf9,0xf3,0xb0,0xe6,0xf9,0xf4,0xf5,0xff,0xcd,0xb0,0xc2,0xf5,0xf3,0xf5,0xf9,0xe6,0xf5,0xb0,0xe6,0xf9,0xf4,0xf5,0xff,0xb0,0xf3,0xf1,0xfc,0xfc,0xb0,0xf9,0xfe,0xf3,0xff,0xfd,0xf9,0xfe,0xf7]

private func textUser(container num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "refuseCall" :*/
fileprivate let show_inputStr:String = "reobjectuse"
fileprivate let user_resultKey:[Character] = ["C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let showActionKey:[Character] = ["o","n","C","a","n","c","e","l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let userBackId:[Character] = ["r","e","t","r","y","A","f","t","e"]
fileprivate let constImageMessage:[Character] = ["r"]

/*: "onRequestCall" :*/
fileprivate let app_contentName:[Character] = ["o","n"]
fileprivate let showLayerTitle:String = "Requespath if max app"

/*: "failType" :*/
fileprivate let data_hiddenValue:String = "status index height view guidefailT"
fileprivate let mainSizePlayerEqualStr:[Character] = ["y","p","e"]

/*: "callWaitingTime" :*/
fileprivate let data_userFilterValue:String = "var info name value letcallW"
fileprivate let mainFitText:String = "handleme"

/*: "toUid" :*/
fileprivate let appTitleFormat:String = "toUidself point add view view"

/*: "Free 1 min" :*/
fileprivate let appAcceptName:[Character] = ["F","r","e","e"," ","1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let showQuickKey:String = "%"
fileprivate let constLengthMessage:String = "@/minmodel file false manager"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EffectTotalViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class EffectTotalViewController: ScoreViewController {
	var lipOff: Bool = true
	var loadCount: Int = 72
	var everyArray: [AnyHashable] = []
	var blockDictionary: [AnyHashable: String] = [:]
	var accountingDataOpen: Bool = true
	var toolScheduleTotal: Int = 65
	var viewArray: [AnyHashable] = []
	var mainRowDictionary: [AnyHashable: String] = [:]

    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.parallelizeData()
            }
        }
        //: self.resetCamera()
        self.loadCamera()
    
            if (freeVideoAlert.layer.contentsRect.size.width != 1) && (freeVideoAlert.convert(CGRect(x: 0, y: 0, width: 0, height: CGFloat(67)), from: freeVideoAlert.superview).origin.x == 45.76) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let maxLegal = ScrubView()
            maxLegal.laboratoryOff = isFirstRandow
            maxLegal.enableInputInterval = chattingNum
            maxLegal.indexDoing = { [self] upwardOpen in
            self.accountingDataOpen = upwardOpen
            
            var maxLegal = animated
            maxLegal = false
            if maxLegal != self.accountingDataOpen {
                self.accountingDataOpen = maxLegal
            }
            
            return self.accountingDataOpen
            }
            maxLegal.contextCount = { [self] playerManagerCheckMagnitude in
            self.toolScheduleTotal = playerManagerCheckMagnitude
            
            var maxLegal = vipFee
                maxLegal += 1
                if maxLegal >= 0 {
                    maxLegal = maxLegal - 1
                }
            if maxLegal < self.toolScheduleTotal {
                self.toolScheduleTotal = maxLegal
            }
            
            return self.toolScheduleTotal
            }
            maxLegal.viewArray = { [self] beautyEventArray in
            self.viewArray = beautyEventArray
            
            guard var value = self.viewArray as? [String] else {
                return nil
            }
            return value
            }
            maxLegal.winnowDictionary = { [self] wayDictionary in
            self.mainRowDictionary = wayDictionary
            
            guard var value = self.mainRowDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                freeVideoAlert.addSubview(maxLegal)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        sizeDown()
        //: setupSubViewsConstraint()
        server()
        //: bindInteraction()
        totaleractionClick()
        //: videoManager.startPreview(isMatch: true)
        videoManager.formatBefore(isMatch: true)
        //: setPlayer(isRandon: false)
        addDoing(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            auspicateVideo()
        }
    
		if var topValue = videoManager.infoModel?.fee { 
	            if (!priceStackView.transform.isIdentity) && (priceStackView.layoutMargins.top == 14.82) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let receiveLet = ScrubView(frame: priceStackView.bounds.intersection(CGRect(x: CGFloat(0), y: CGFloat(0), width: CGFloat(223.16), height: CGFloat(76))))
	            receiveLet.laboratoryOff = isFirstRandow
	            receiveLet.enableInputInterval = chattingNum
	            receiveLet.indexDoing = { [self] upwardOpen in
	            self.lipOff = upwardOpen
	            
	            var receiveLet = isFirstRandow
	            receiveLet = true
	            if receiveLet != self.lipOff {
	                self.lipOff = receiveLet
	            }
	            
	            return self.lipOff
	            }
	            receiveLet.contextCount = { [self] playerManagerCheckMagnitude in
	            self.loadCount = playerManagerCheckMagnitude
	            
	            var receiveLet = topValue
	            receiveLet &+= 1
	            if receiveLet < self.loadCount {
	                self.loadCount = receiveLet
	            }
	            
	            return self.loadCount
	            }
	            receiveLet.viewArray = { [self] beautyEventArray in
	            self.everyArray = beautyEventArray
	            
	            guard var value = self.everyArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            receiveLet.winnowDictionary = { [self] wayDictionary in
	            self.blockDictionary = wayDictionary
	            
	            guard var value = self.blockDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                priceStackView.addSubview(receiveLet)
	            }
	
		}
	}

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            ActionSocketDelegate.shared.tabularArray(info: [String(bytes: app_toMsg.map{$0^25}, encoding: .utf8)!: (kBeautyFormat.replacingOccurrences(of: "row", with: "ca") + "Call"), String(bytes: mainMakeSkinName.map{deliverTheGoods(frame: $0)}, encoding: .utf8)!: [(String(show_indexName.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        showTimer()
        //: if TalkingSocketManager.shared.isShared == false {
        if ActionSocketDelegate.shared.isShared == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ActionSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ActionSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ActionSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ActionSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: PrepareReactiveCompatible = {
        //: let m = TalkingVideoChatManager()
        let m = PrepareReactiveCompatible()
        //: let info = TalkingVideoChatModel()
        let info = SubChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .census()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(auspicateVideo), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: notiEnabledRecordUrl + 1, height: main_versionEnterTimeStr + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(user_byUrl) + kViewMessage.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(userTitleName.prefix(5)) + "o st" + String(data_equalStr.suffix(4)) + "video " + dataGiftId.replacingOccurrences(of: "path", with: "c")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.addBar(name: (String(main_toUrl) + notiButtonSizeFormat.replacingOccurrences(of: "fail", with: "se") + "_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(holdfastDetail), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: QuantityervalFirstButton = {
        //: let btn = TalkingButton()
        let btn = QuantityervalFirstButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.addBar(name: (mainRankId.replacingOccurrences(of: "me", with: "c") + String(noti_pathData))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.addBar(name: (mainRankId.replacingOccurrences(of: "me", with: "c") + String(noti_pathData))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.pingfangCenter(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(main_collectionUrl.prefix(4)) + "hatt" + dataProfileKey.replacingOccurrences(of: "quit", with: "n")).fast(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.addBar(name: (String(show_mTitle)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.addBar(name: (String(app_countTitleValue.suffix(7)) + String(showDelayKey.prefix(7))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.pingfangCenter(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.addBar(name: (String(k_viewGiftUrl.suffix(4)) + "btn_to" + data_styleActualName.replacingOccurrences(of: "equal", with: "p")))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: QuantityervalFirstButton = {
        //: let btn = TalkingButton()
        let btn = QuantityervalFirstButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(embed), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.addBar(name: (String(dataModelRangeMessage))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.addBar(name: (String(app_countTitleValue.suffix(7)) + String(showDelayKey.prefix(7))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .colorFor()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (app_resultData.replacingOccurrences(of: "time", with: " ") + String(user_centerFieldData.prefix(5)) + "/min").fast(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.pingfangCenter(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: SizeDataReactiveCompatible = //: return SizeDataReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension EffectTotalViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func addDoing(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = ShadowEffectTool.default.varietyButton(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(dataPopName) + String(noti_airName)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func viewErrno(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(userDataPath)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: app_toMsg.map{$0^25}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: show_statusStr.map{pointTitle(style: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        BeginVideoManager.addCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.loadAdd()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(userDataPath))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func mintageMake() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        BeginVideoManager.mailCompletion { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(mainModelId.suffix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.parallelizeData()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension EffectTotalViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func embed() {
        //: ManageressThen.share.func__pushToSubscribeAlert()
        ManageressThen.share.sendUp()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func holdfastDetail() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        book()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        app_serverName.successOrMeanSun(eventID: kVideoData)

        //: if RegularBeginDecisionMakerAppManager.share.appConfigMode.displayMatchV4PopUp, RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes > 0,
        if RegularBeginDecisionMakerAppManager.share.appConfigMode.displayMatchV4PopUp, RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.titleFromSelect()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        firmness()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func auspicateVideo() {
        //: guard TalkingSocketManager.shared.isShared == false else {
        guard ActionSocketDelegate.shared.isShared == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.statusTo(showMsg: show_sessionName)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        addCenterOff()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(notiPartyId.prefix(5)) + noti_allPath.replacingOccurrences(of: "equal", with: "n") + String(user_frameMakeMessage.suffix(5))).localized
        //: setPlayer(isRandon: true)
        addDoing(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.randomVideoType == 2 {
        if RegularBeginDecisionMakerAppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            app_serverName.successOrMeanSun(eventID: userScreenTitle)
            //: req_getVideoInfo()
            scheme()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            app_serverName.successOrMeanSun(eventID: const_ageStr)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            ActionSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            ActionSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            ActionSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            ActionSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            tagErrno()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func tagErrno(cmd: String = (constImageName.replacingOccurrences(of: "screen", with: "u") + String(k_inputMessage)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        showTimer()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        viewErrno(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func loadAdd() {
        // 停止定时器
        //: destroyWaitingTimer()
        showTimer()
        //: showCloseBtn()
        book()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(userTitleName.prefix(5)) + "o st" + String(data_equalStr.suffix(4)) + "video " + dataGiftId.replacingOccurrences(of: "path", with: "c")).localized
        //: setPlayer(isRandon: false)
        addDoing(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isShared == false {
        if ActionSocketDelegate.shared.isShared == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ActionSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ActionSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ActionSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ActionSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        parallelizeData()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func showTimer() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func scheme() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = AddRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(showLayerActionId.prefix(6)) + "eCal" + String(userReasonFormat.prefix(6)) + "ideoInfo")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: app_errorFormat.map{cuttingEdge(equal: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        app_sizeUrl.dataChoice(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.loadAdd()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: RegularBeginDecisionMakerAppManager.share.loginUserMode.randomVideoType = 1
                    RegularBeginDecisionMakerAppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.auspicateVideo()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.statusTo(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = ActionHandyJSON.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = ControlTableReactiveCompatible()
                //: player.setMute(bEnable: true)
                player.cease(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.trademark(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                AllianceManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                PenumbraReactiveCompatible.behindModel { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.filenameThrough()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func filenameThrough() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        app_serverName.successOrMeanSun(eventID: user_keyId)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard AllianceManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.statusTo(showMsg: String(bytes: kWhiteName.map{pointSize(main: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            AllianceManager.shared.allCircle(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.loadAdd()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = ClickVideoUserDataBaptiseVc()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.firmness()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.loadAdd()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func addCenterOff() {
        //: if RegularBeginDecisionMakerAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if RegularBeginDecisionMakerAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(RegularBeginDecisionMakerAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(RegularBeginDecisionMakerAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func book() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func loadCamera() {
        //: guard TalkingSocketManager.shared.isShared == false else {
        guard ActionSocketDelegate.shared.isShared == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.formatBefore()
        }
    }
}

// MARK: - ViewObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension EffectTotalViewController: ViewObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func cleanData(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = VoiceHandyJSON.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(userDataPath)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: dataSucceedKey.map{userShared(image: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: main_frameStr.map{$0^101}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: userProgressContent.map{byProduct(crush: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = TopNumeric.randow
        //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes > 0 && RegularBeginDecisionMakerAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes > 0 && RegularBeginDecisionMakerAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (const_sexPicName.replacingOccurrences(of: "make", with: "ro") + "FreeC" + String(notiSourceValue)))
            //: TalkingSocketManager.shared.isFreeCall = true
            ActionSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: app_toMsg.map{$0^25}, encoding: .utf8)!: (String(show_videoText) + appShareId.replacingOccurrences(of: "last", with: "ll")), String(bytes: mainMakeSkinName.map{deliverTheGoods(frame: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        ActionSocketDelegate.shared.tabularArray(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            ActionSocketDelegate.shared.tabularArray(info: [String(bytes: app_toMsg.map{$0^25}, encoding: .utf8)!: (kBeautyFormat.replacingOccurrences(of: "row", with: "ca") + "Call"), String(bytes: mainMakeSkinName.map{deliverTheGoods(frame: $0)}, encoding: .utf8)!: [(String(show_indexName.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.tagErrno(cmd: (userImageStr.replacingOccurrences(of: "label", with: "o") + String(dataFrameManagerKey.prefix(6)) + "atch"))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func startLocation(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(show_indexName.prefix(5)))].intValue
    }
}

// MARK: - EvaluationObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension EffectTotalViewController: EvaluationObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func endSchedule(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func running(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func dataText(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func pullUpShort(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            BeginBaseDelegate.removeName(body: String(bytes: dataTitleFormat.map{textUser(container: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = VoiceHandyJSON.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            sleepingCapsuleUid(toUid: String(uid))
        }
        //: stop_randomVideo()
        loadAdd()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.makeFullView()
        //: RegularBeginDecisionMakerAppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        RegularBeginDecisionMakerAppManager.share.elementSituation(info: data, situation: .Calling)
    }
}

// MARK: - PathSuiteObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension EffectTotalViewController: PathSuiteObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func pointData(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        tagErrno(cmd: (show_inputStr.replacingOccurrences(of: "object", with: "f") + String(user_resultKey)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func placeToData(data _: [String: Any]) {}
}

// MARK: - ConnectAtErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension EffectTotalViewController: ConnectAtErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func heed(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(showActionKey)) { // 取消通话异常
            //: stop_randomVideo()
            loadAdd()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (userImageStr.replacingOccurrences(of: "label", with: "o") + String(dataFrameManagerKey.prefix(6)) + "atch"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            loadAdd()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(userDataPath))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.statusTo(showMsg: msg)
            //: uploadFailTime(type: 2)
            imageType(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (userImageStr.replacingOccurrences(of: "label", with: "o") + String(dataFrameManagerKey.prefix(6)) + "atch"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(userBackId) + String(constImageMessage))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(userDataPath))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.tagErrno(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(app_contentName) + String(showLayerTitle.prefix(6)) + "tCall"), errorNo == GlobalNumberLiteral.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            loadAdd()
            //: guard RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.normal.rawValue else { return }
            //: ManageressThen.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            ManageressThen.share.themeParams(clickEvent: constUserUrl, sufficient: false)
            //: uploadFailTime(type: 1)
            imageType(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        tagErrno(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension EffectTotalViewController {
    //: func uploadFailTime(type: Int) {
    func imageType(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(data_hiddenValue.suffix(5)) + String(mainSizePlayerEqualStr))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(data_userFilterValue.suffix(5)) + "aitingT" + mainFitText.replacingOccurrences(of: "handle", with: "i"))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        app_serverName.recordEqual(eventID: noti_routeFormat, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func sleepingCapsuleUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(data_userFilterValue.suffix(5)) + "aitingT" + mainFitText.replacingOccurrences(of: "handle", with: "i"))] = time
        //: parm["toUid"] = toUid
        parm[(String(appTitleFormat.prefix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        app_serverName.recordEqual(eventID: mainErrorValue, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension EffectTotalViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func parallelizeData() {
        //: guard RegularBeginDecisionMakerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard RegularBeginDecisionMakerAppManager.share.appStatus == MomentNumberLiteral.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = TopNumeric.randow
        //: if RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes > 0 && RegularBeginDecisionMakerAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if RegularBeginDecisionMakerAppManager.share.loginUserMode.freeCallTimes > 0, RegularBeginDecisionMakerAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if RegularBeginDecisionMakerAppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if RegularBeginDecisionMakerAppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(appAcceptName)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (showQuickKey.capitalized + String(constLengthMessage.prefix(5))).fast(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (showQuickKey.capitalized + String(constLengthMessage.prefix(5))).fast(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.mediumPingfangStatusSizeFont(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func sizeDown() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func server() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(showPlainValue + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func totaleractionClick() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(parallelizeData),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: const_faceMessage,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(mintageMake),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: user_errorValue,
                                               //: object: nil)
                                               object: nil)
    }
}
