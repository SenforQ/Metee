
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showTingTranslateContent:[UInt8] = [0x39,0x3e,0x39,0x44,0xf8,0x33,0x3f,0x34,0x35,0x42,0xa,0xf9,0xf0,0x38,0x31,0x43,0xf0,0x3e,0x3f,0x44,0xf0,0x32,0x35,0x35,0x3e,0xf0,0x39,0x3d,0x40,0x3c,0x35,0x3d,0x35,0x3e,0x44,0x35,0x34]

fileprivate func iconItem(sex num: UInt8) -> UInt8 {
    let value = Int(num) - 208
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Load..." :*/
fileprivate let notiWithId:String = "Load..block hidden path content"
fileprivate let show_dipValue:String = "."

/*: "Skip" :*/
fileprivate let k_appValue:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let mainModelValue:[UInt8] = [0xf1,0x1d,0x1d,0x19,0xfd,0x18,0x18,0x15,0xd7,0xf7,0xa,0x1d,0x12,0x1f,0xe,0xfd,0x18,0xf3,0x1c,0xd1,0xd0,0x1b,0xe,0xc,0x11,0xa,0x1b,0x10,0xe,0xd0,0xd2]

fileprivate func viewEffect(user num: UInt8) -> UInt8 {
    let value = Int(num) - 169
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let k_pathKey:[UInt8] = [0x58,0x4a,0x4f,0x45,0x50,0x58,0xf,0x50,0x4f,0x31,0x42,0x48,0x46,0x34,0x49,0x50,0x58,0x7,0x7,0x50,0x4f,0x31,0x42,0x48,0x46,0x34,0x49,0x50,0x58,0x9,0xa,0x1c]

fileprivate func actionName(current num: UInt8) -> UInt8 {
    let value = Int(num) - 225
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let notiTableName:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e","S","h","o","w",")"]
fileprivate let app_colorEveryValue:[Character] = [":"," "]

/*: --- :*/
fileprivate let notiDataUrl:String = "wrapwrapwrap"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let main_tagMsg:[UInt8] = [0x14,0xa,0xd,0x7,0xc,0x14,0x4d,0xc,0xd,0x33,0x2,0x4,0x6,0x2b,0xa,0x7,0x6,0x45,0x45,0xc,0xd,0x33,0x2,0x4,0x6,0x2b,0xa,0x7,0x6,0x4b,0x4a,0x58]

private func beautyUser(share num: UInt8) -> UInt8 {
    return num ^ 99
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let data_scaleJumpTitle:[Character] = ["j","s","E","v","e","n"]
fileprivate let const_brightValue:String = "t(onPamodel show kind ting"
fileprivate let constUserLeadingDirectionKey:[Character] = ["g","e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let kVersionInfoFormat:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let kEditPath:String = "canGoview kit cover make color"
fileprivate let constRecordSoundStr:String = "info to data color pushBack"

/*: "PHPSESSID= :*/
fileprivate let k_pathName:[Character] = ["P","H"]
fileprivate let const_lineWindowVarData:[Character] = ["P","S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let data_statusTimeContent:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let showToTitle:[Character] = ["C","o","o","k","i"]
fileprivate let appResultPath:String = "remove"

/*: "nav_back_black_nor" :*/
fileprivate let appChooseName:[Character] = ["n","a","v","_","b","a","c","k","_","b","l"]
fileprivate let dataTitleNameMsg:String = "ack_noron suite show"

/*: "ico_intimate_list_sm" :*/
fileprivate let data_tableUrl:[Character] = ["i","c","o","_","i","n","t","i","m","a","t","e","_","l","i","s","t"]
fileprivate let notiBorderName:[Character] = ["_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let constErrorUserVideoKey:[UInt8] = [0xe9,0xe2,0xee,0xf8,0xe0,0xe8,0xe3,0xf9,0xa3,0xea,0xe8,0xf9,0xc8,0xe1,0xe8,0xe0,0xe8,0xe3,0xf9,0xfe,0xcf,0xf4,0xd9,0xec,0xea,0xc3,0xec,0xe0,0xe8,0xa5,0xaa,0xef,0xe2,0xe9,0xf4,0xaa,0xa4,0xd6,0xbd,0xd0,0xa3,0xfe,0xf9,0xf4,0xe1,0xe8,0xa3,0xef,0xec,0xee,0xe6,0xea,0xff,0xe2,0xf8,0xe3,0xe9,0xb0,0xaa,0xff,0xea,0xef,0xec,0xa5,0xbd,0xa1,0xbd,0xa1,0xbd,0xa1,0xbd,0xa4,0xaa]

private func toolModel(change num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "Alert" :*/
fileprivate let mainFrontTitle:[Character] = ["A","l","e","r","t"]

/*: "OK" :*/
fileprivate let dataSkinStr:String = "ok"

/*: "Cancle" :*/
fileprivate let notiSizePath:String = "screen view let let norCancle"

/*: "Done" :*/
fileprivate let k_edgeValue:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let kTopId:String = "add fill image user featurejs c"
fileprivate let k_femaleContent:String = "eareaod"

/*: , message =  :*/
fileprivate let k_blockMessage:[Character] = [","," ","m","e","s","s","a","g","e"]
fileprivate let notiCrushText:[Character] = [" ","="," "]

/*: "http://" :*/
fileprivate let mainPagePath:String = "view equal normal equal modelhttp://"

/*: "https://" :*/
fileprivate let const_fatalData:String = "content private let bag userhttps:"
fileprivate let userSourceFormat:[Character] = ["/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let noti_titleText:[UInt8] = [0xe2,0xf5,0xe6,0xb4,0xf7,0xfb,0xfb,0xff,0xfd,0xf1,0xda,0xf5,0xf9,0xf1,0xe7,0xb4,0xa9,0xb4,0xf0,0xfb,0xf7,0xe1,0xf9,0xf1,0xfa,0xe0,0xba,0xf7,0xfb,0xfb,0xff,0xfd,0xf1,0xba,0xe7,0xe4,0xf8,0xfd,0xe0,0xbc,0xb3,0xaf,0xb4,0xb3,0xbd,0xba,0xf9,0xf5,0xe4,0xbc,0xf2,0xe1,0xfa,0xf7,0xe0,0xfd,0xfb,0xfa,0xbc,0xf7,0xfb,0xfb,0xff,0xfd,0xf1,0xbd,0xb4,0xef,0xb4,0xe6,0xf1,0xe0,0xe1,0xe6,0xfa,0xb4,0xf7,0xfb,0xfb,0xff,0xfd,0xf1,0xba,0xe7,0xe4,0xf8,0xfd,0xe0,0xbc,0xb3,0xa9,0xb3,0xbd,0xcf,0xa4,0xc9,0xb4,0xe9,0xb4,0xbd,0xaf,0x9e]

/*: "uid= :*/
fileprivate let noti_toMsg:String = "uid=info selected fill info"

/*: ;domain= :*/
fileprivate let appContentMsg:[Character] = [";","d","o","m","a","i","n"]
fileprivate let constResumeLayerKey:String = "="

/*: ;path=/" :*/
fileprivate let k_numberPath:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let show_titleThanUrl:String = "docrawe"
fileprivate let constBottomTitle:String = "ookie=\'"

/*: ';\n" :*/
fileprivate let dataRenderFormat:[Character] = ["\'",";","\n"]

/*: ;domin= :*/
fileprivate let noti_indexData:String = "pair"
fileprivate let notiTableSectionTitle:String = "line item false pricedomin="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let notiTurnKey:[UInt8] = [0xbb,0xb8,0x72,0x7a,0xb5,0xc1,0xc1,0xbd,0xbb,0xb7,0xa0,0xb3,0xbf,0xb7,0xc5,0x80,0xbb,0xc0,0xb6,0xb7,0xca,0xa1,0xb8,0x7a,0x79,0xa2,0x9a,0xa2,0xa5,0x97,0xa5,0xa5,0x9b,0x96,0x79,0x7b,0x72,0x8f,0x8f,0x72,0x7f,0x83,0x7b,0x72,0xcd,0x72,0xb6,0xc1,0xb5,0xc7,0xbf,0xb7,0xc0,0xc6,0x80,0xb5,0xc1,0xc1,0xbd,0xbb,0xb7,0x8f,0x79]

fileprivate func changeRead(remove num: UInt8) -> UInt8 {
    let value = Int(num) - 82
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: '; };\n" :*/
fileprivate let data_viewKickPath:String = "\'; };\n"

/*: '" :*/
fileprivate let noti_userKey:[Character] = ["\'"]

/*: "h5Domain =  :*/
fileprivate let notiTimeKey:[Character] = ["h","5","D","o","m"]
fileprivate let appInfoConversationMessage:String = "ain = current to mode let"

/*: "getAllCookies =  :*/
fileprivate let const_backStr:[Character] = ["g","e","t","A","l","l"]
fileprivate let main_makeData:String = "Coostatus player intimate tag"
fileprivate let showInfoUrl:String = "to color selfkies = "

/*: "PHPSESSID" :*/
fileprivate let constTableLocationMessage:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "uid" :*/
fileprivate let k_imageContent:String = "viewd"

/*: "appCookies =  :*/
fileprivate let userMinName:[Character] = ["a","p","p","C","o","o","k","i","e","s"," ","="," "]

/*: "UID" :*/
fileprivate let kExtraStr:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let data_originalUrl:[Character] = ["n","a","v","i","g","a","t","o","r",".","u","s","e","r","A"]
fileprivate let userKeyPath:String = "gefirstt"

/*: "xunwu_iphone" :*/
fileprivate let constPlacePath:[Character] = ["x","u","n","w","u","_","i","p","h","o","n"]
fileprivate let const_addId:String = "control"

/*: "  :*/
fileprivate let mainSharedPath:String = " "

/*: " xunwu_device_id/ :*/
fileprivate let show_fromDownName:[Character] = [" ","x","u","n","w","u","_","d","e","v","i","c","e","_","i","d","/"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let constReValue:[Character] = ["x","u","n","w","u","_","i","p"]
fileprivate let k_titleText:[Character] = ["h","o","n","e"," ","x","u","n","w","u","_","v","e"]
fileprivate let const_appPositionName:String = "push equal modelrsion/"

/*:  xunwu_package/ :*/
fileprivate let app_progressData:[Character] = [" ","x","u","n","w","u","_","p","a","c","k","a","g","e"]
fileprivate let notiLabelFormat:String = "/"

/*:  xunwu_bundleId/ :*/
fileprivate let mainMakeMsg:[Character] = [" ","x","u","n","w","u","_","b"]
fileprivate let showLabSizePath:String = "style titleundleId/"

/*:   webkit/1" :*/
fileprivate let data_errorPath:[Character] = [" "," ","w","e","b","k","i"]
fileprivate let userTapPath:String = "white name as heightt/1"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EffectNavigationDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class EffectNavigationDelegate: EstablishDescriptionThen {
	var loadDoing: Bool = false
	var nameMagnitude: Int = 19
	var listGiftText: String = "gift"
	var styleArray: [AnyHashable] = []
	var forefrontCount: Int = 3
	var bottomTitle: String = "half"
	var screenSum: Int = 65
	var dayOfRemembranceTitle: String = "conversation"

    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: FrameControlConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: ViewSendable?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: ViewSendable?, webConfig: FrameControlConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showTingTranslateContent.map{iconItem(sex: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        atAdd()
    
		if var cookieModelValue = self.urlStr { 
			if var nameValue = urlStr { 
		            if (self.prefersStatusBarHidden) && (self.transitioningDelegate != nil) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let colorReject = MenuController()
		            colorReject.actionTitle = nameValue
		            
		            colorReject.totalOn = { [self] emptyDoing in
		            self.loadDoing = emptyDoing
		            
		            var colorReject = isRechargeOrSubscribeChangeMatch
		            colorReject = !colorReject
		            if colorReject != self.loadDoing {
		                self.loadDoing = colorReject
		            }
		            
		            return self.loadDoing
		            }
		            colorReject.frameQuantity = { [self] commentTotal in
		            self.nameMagnitude = commentTotal
		            
		            return self.nameMagnitude
		            }
		            colorReject.meshTitle = { [self] valueContent in
		            self.listGiftText = valueContent
		            
		            var colorReject = cookieModelValue
		            colorReject.removeLast(colorReject.count)
		            if colorReject.contains(self.listGiftText) {
		                self.listGiftText = colorReject
		            }
		            
		            var personalty = self.listGiftText.startIndex
		            self.listGiftText.formIndex(after: &personalty)
		            self.listGiftText.append(self.listGiftText[personalty])
		            return self.listGiftText
		            }
		            colorReject.quoteArray = { [self] socialArray in
		            self.styleArray = socialArray
		            
		            guard var value = self.styleArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		                self.navigationController?.pushViewController(colorReject.self, animated: true)
		            }
		
			}
		}
	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        nameTheoryFollow()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    
		if var playerValue = urlStr { 
			if var gestureDisplayValue = config?.widthHeight { 
		            if (webView.bounds.origin.y == 31.43) && (webView.sizeThatFits(.zero).width == 139.81) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let worldView = MenuView(frame: webView.frame.offsetBy(dx: CGFloat(218.72), dy: CGFloat(562.00)))
		
		            worldView.totalryQuantity = gestureDisplayValue
		            
		            
		            worldView.styleExhibitModelCount = { [self] commentTotal in
		            self.screenSum = commentTotal
		            
		            return self.screenSum
		            }
		            worldView.stopText = { [self] valueContent in
		            self.dayOfRemembranceTitle = valueContent
		            
		            var worldView = playerValue
		            var pop = worldView.startIndex
		            worldView.formIndex(after: &pop)
		            worldView.append(worldView[pop])
		            if worldView.contains(self.dayOfRemembranceTitle) {
		                self.dayOfRemembranceTitle = worldView
		            }
		            
		            if self.dayOfRemembranceTitle[self.dayOfRemembranceTitle.startIndex].isASCII {
		                self.dayOfRemembranceTitle = ""
		            }
		            return self.dayOfRemembranceTitle
		            }
		                webView.addSubview(worldView)
		            }
		
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(notiWithId.prefix(6)) + show_dipValue.capitalized)
        //: addNotification()
        targetNotification()
        //: func__checkConfig()
        addEqual()
        //: func__setUpWebView()
        userSetUp()
        //: func__registerObserver()
        funcRegisterSeer()
        //: func__beginStartRequest()
        requestCapacity()
        //: func_checkAppleIAP()
        overObserver()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: noti_frameKey, object: nil)
            }
        }
    
		if var barValue = urlStr { 
			if var airValue = self.tranConfig?.duration { 
		            if (webView.bounds.origin.y == 31.43) && (webView.sizeThatFits(.zero).width == 139.81) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let worldView = MenuView(frame: webView.frame.offsetBy(dx: CGFloat(218.72), dy: CGFloat(562.00)))
		
		            worldView.totalryQuantity = airValue
		            
		            
		            worldView.styleExhibitModelCount = { [self] commentTotal in
		            self.forefrontCount = commentTotal
		            
		            return self.forefrontCount
		            }
		            worldView.stopText = { [self] valueContent in
		            self.bottomTitle = valueContent
		            
		            var worldView = barValue
		            var pop = worldView.startIndex
		            worldView.formIndex(after: &pop)
		            worldView.append(worldView[pop])
		            if worldView.contains(self.bottomTitle) {
		                self.bottomTitle = worldView
		            }
		            
		            if self.bottomTitle[self.bottomTitle.startIndex].isASCII {
		                self.bottomTitle = ""
		            }
		            return self.bottomTitle
		            }
		                webView.addSubview(worldView)
		            }
		
			}
		}
	}

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            buildConfig(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.outsideBecomeColor(), .font: UIFont.pingfangCenter(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(k_appValue)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension EffectNavigationDelegate {
    /// 添加通知
    //: private func addNotification() {
    private func targetNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(solution),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: data_clickPageMessage,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(atAdd),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func solution() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: mainModelValue.map{viewEffect(user: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func atAdd() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: k_pathKey.map{actionName(current: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(notiTableName) + String(app_colorEveryValue)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func nameTheoryFollow() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: main_tagMsg.map{beautyUser(share: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(data_scaleJumpTitle) + String(const_brightValue.prefix(6)) + String(constUserLeadingDirectionKey)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension EffectNavigationDelegate {
    //: func func__registerObserver() {
    func funcRegisterSeer() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(kVersionInfoFormat)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(kEditPath.prefix(5)) + String(constRecordSoundStr.suffix(4))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(enableWithForefrontDismiss), name: appListMessage, object: nil)
    }

    //: func func__setUpWebView() {
    func userSetUp() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        motor()
        //: func__addBridgeMethod()
        beatBackMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.objectRed()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            DescriptionProgressHUD.successActiveView(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(showPlainValue)
            }
        }
    }

    //: func func__beginStartRequest() {
    func requestCapacity() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.comparable()
            }
            //: } else {
        } else {
            //: func__startRequest()
            comparable()
        }
    }

    //: func func__startRequest() {
    func comparable() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = RegularBeginDecisionMakerAppManager.share.loginUid
        let uid = RegularBeginDecisionMakerAppManager.share.loginUid
        //: let ssid = RegularBeginDecisionMakerAppManager.share.loginSessionId
        let ssid = RegularBeginDecisionMakerAppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(k_pathName) + String(const_lineWindowVarData)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(data_statusTimeContent)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(showToTitle) + appResultPath.replacingOccurrences(of: "remove", with: "e")))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        getCracking()
        //: func__initRightButton()
        equalStatus()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func overObserver() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            InitialReactiveCompatible.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.beyondVariation(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            InitialReactiveCompatible.shared.stopInfo()
        }
    }

    //: func func__initLeftButton() {
    func discredit() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.addBar(name: (String(appChooseName) + String(dataTitleNameMsg.prefix(7)))), style: .plain, target: self, action: #selector(societal(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func equalStatus() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(ViewSendable.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.addBar(name: (String(data_tableUrl) + String(notiBorderName))), style: .plain, target: self, action: #selector(showSumimate))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func societal(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        makeMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.firmness() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: EffectNavigationDelegate.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! EffectNavigationDelegate).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! EffectNavigationDelegate).atAdd()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func addEqual() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func actionView(sucess: Bool) {
        //: ProgressHUD.dismiss()
        DescriptionProgressHUD.actionOf()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func getCracking() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: constErrorUserVideoKey.map{toolModel(change: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func unbolt(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: ManageressThen.share.func__pushToWebVC(urlStr: url)
            ManageressThen.share.barbLab(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func monthBy(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func enableWithForefrontDismiss() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: appListMessage, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func showSumimate() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = DepictionDataSource(frame: CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr))
        //: view.show()
        view.titleShow()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension EffectNavigationDelegate: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            DescriptionProgressHUD.successActiveView(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            actionView(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        discredit()
        //: func__clearJSBgColor()
        getCracking()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            actionView(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            actionView(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(mainFrontTitle)), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(notiSizePath.suffix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(k_edgeValue)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension EffectNavigationDelegate: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(kTopId.suffix(4)) + "all m" + k_femaleContent.replacingOccurrences(of: "area", with: "th") + " name = ") + "\(message.name)" + (String(k_blockMessage) + String(notiCrushText)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.zone(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func beatBackMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ActionDataTitle.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func makeMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ActionDataTitle.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension EffectNavigationDelegate {
    //: func func__addUserScript() {
    func stairs() {
        //: var htmlDomain: String = RegularBeginDecisionMakerAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = RegularBeginDecisionMakerAppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(mainPagePath.suffix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(mainPagePath.suffix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(const_fatalData.suffix(6)) + String(userSourceFormat))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(const_fatalData.suffix(6)) + String(userSourceFormat)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
        let tempUid: String = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
        //: let tempSessionID: String = RegularBeginDecisionMakerAppManager.share.loginSessionId
        let tempSessionID: String = RegularBeginDecisionMakerAppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: noti_titleText.map{$0^148}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_toMsg.prefix(4))) + "\(tempUid)" + (String(appContentMsg) + constResumeLayerKey.capitalized) + "\(htmlDomain)" + (String(k_numberPath))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((show_titleThanUrl.replacingOccurrences(of: "raw", with: "um") + "nt.c" + constBottomTitle) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(k_pathName) + String(const_lineWindowVarData)) + "\(tempSessionID)" + (noti_indexData.replacingOccurrences(of: "pair", with: ";") + String(notiTableSectionTitle.suffix(6))) + "\(htmlDomain)" + (String(k_numberPath))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((show_titleThanUrl.replacingOccurrences(of: "raw", with: "um") + "nt.c" + constBottomTitle) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: notiTurnKey.map{changeRead(remove: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_toMsg.prefix(4))) + "\(tempUid)" + (String(appContentMsg) + constResumeLayerKey.capitalized) + "\(htmlDomain)" + (String(k_numberPath))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((show_titleThanUrl.replacingOccurrences(of: "raw", with: "um") + "nt.c" + constBottomTitle) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_toMsg.prefix(4))) + "\(tempSessionID)" + (String(appContentMsg) + constResumeLayerKey.capitalized) + "\(htmlDomain)" + (String(k_numberPath))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((show_titleThanUrl.replacingOccurrences(of: "raw", with: "um") + "nt.c" + constBottomTitle) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func buildConfig(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = RegularBeginDecisionMakerAppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = RegularBeginDecisionMakerAppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(notiTimeKey) + String(appInfoConversationMessage.prefix(6))) + "\(htmlDomain)")
            //: let userID: String = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
            let userID: String = RegularBeginDecisionMakerAppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(const_backStr) + String(main_makeData.prefix(3)) + String(showInfoUrl.suffix(7))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(constTableLocationMessage)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (k_imageContent.replacingOccurrences(of: "view", with: "ui")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.requestCapacity()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.detect(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func detect(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = RegularBeginDecisionMakerAppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = RegularBeginDecisionMakerAppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(notiTimeKey) + String(appInfoConversationMessage.prefix(6))) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: PathAtAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(userMinName)) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(kExtraStr)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (k_imageContent.replacingOccurrences(of: "view", with: "ui"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(constTableLocationMessage)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(constTableLocationMessage))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.requestCapacity()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func motor() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(data_originalUrl) + userKeyPath.replacingOccurrences(of: "first", with: "n"))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(data_originalUrl) + userKeyPath.replacingOccurrences(of: "first", with: "n")))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(constPlacePath) + const_addId.replacingOccurrences(of: "control", with: "e"))) {
                //: let str = getUserAgentString()
                let str = twineVectorGetSomebody()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.getActionInkpadFullPhaseOfTheMoonErbium { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(show_fromDownName)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func twineVectorGetSomebody() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(constReValue) + String(k_titleText) + String(const_appPositionName.suffix(6))) + "\(kKeyPath)" + (String(app_progressData) + notiLabelFormat.capitalized) + "\(user_environmentMsg)" + (String(mainMakeMsg) + String(showLabSizePath.suffix(8))) + "\(userSuccessFormat)" + (String(data_errorPath) + String(userTapPath.suffix(3)))
    }
}
