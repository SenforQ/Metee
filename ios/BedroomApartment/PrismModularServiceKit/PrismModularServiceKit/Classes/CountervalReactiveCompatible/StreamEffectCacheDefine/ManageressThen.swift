
//: Declare String Begin

/*: "fee" :*/
fileprivate let k_cellOfStr:String = "ffirstfirst"

/*: "VIPFee" :*/
fileprivate let const_emptyViewText:[Character] = ["V","I","P","F","e","e"]

/*: "chattingNum" :*/
fileprivate let app_originButtonPath:[Character] = ["c","h","a","t","t","i","n","g","N"]
fileprivate let notiAtInputData:String = "uhidden"

/*: "You're already in her live room" :*/
fileprivate let noti_styleId:[UInt8] = [0x9e,0xa8,0xb2,0xe0,0xb5,0xa2,0xe7,0xa6,0xab,0xb5,0xa2,0xa6,0xa3,0xbe,0xe7,0xae,0xa9,0xe7,0xaf,0xa2,0xb5,0xe7,0xab,0xae,0xb1,0xa2,0xe7,0xb5,0xa8,0xa8,0xaa]

private func textPush(plot num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "toUid" :*/
fileprivate let userVoiceStr:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "momentId" :*/
fileprivate let k_willIndexMsg:[UInt8] = [0x2b,0x29,0x2b,0x23,0x28,0x32,0xf,0x22]

/*: "extra" :*/
fileprivate let noti_tableUrl:[UInt8] = [0x61,0x72,0x74,0x78,0x65]

/*: "uid" :*/
fileprivate let app_imageMsg:[UInt8] = [0x96,0x8a,0x87]

private func lifeStyle(fatal num: UInt8) -> UInt8 {
    return num ^ 227
}

/*: "onlineStatus" :*/
fileprivate let k_colorMsg:[UInt8] = [0x8d,0x8c,0x8a,0x87,0x8c,0x83,0x71,0x92,0x7f,0x92,0x93,0x91]

fileprivate func errorRead(photo num: UInt8) -> UInt8 {
    let value = Int(num) - 30
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isNewUser" :*/
fileprivate let data_insertUrl:[UInt8] = [0x37,0x2d,0x10,0x3b,0x29,0xb,0x2d,0x3b,0x2c]

/*: "userStatus" :*/
fileprivate let const_viewKey:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x72,0x65,0x73,0x75]

/*: "Account is restricted！" :*/
fileprivate let noti_statusContent:String = "member managerAcco"
fileprivate let main_equalData:String = "estrthem"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let const_itemUrl:[UInt8] = [0x72,0x8d,0x95,0x98,0x91,0x90,0x4c,0xa0,0x9b,0x4c,0x91,0x9a,0xa0,0x91,0x9e,0x4c,0xa0,0x94,0x91,0x4c,0x8f,0x94,0x8d,0xa0,0x4c,0x9e,0x9b,0x9b,0x99,0x5a,0x4c,0x7c,0x98,0x91,0x8d,0x9f,0x91,0x4c,0xa0,0x9e,0xa5,0x4c,0x8d,0x93,0x8d,0x95,0x9a,0x4c,0x98,0x8d,0xa0,0x91,0x9e]

fileprivate func viewClear(background num: UInt8) -> UInt8 {
    let value = Int(num) - 44
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "url" :*/
fileprivate let show_dataToTitle:String = "urframe"

/*: "userDetail" :*/
fileprivate let noti_actualValue:String = "US"
fileprivate let notiAddWithData:String = "erDemy view top var target"

/*: "&type=7" :*/
fileprivate let mainPleaseKey:String = "interaction corner process view component&type"
fileprivate let showMakeMsg:String = "=icon"

/*: "amount" :*/
fileprivate let user_frameValue:[UInt8] = [0xec,0xe0,0xe2,0xf8,0xe3,0xf9]

private func leadTransform(bar num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "transparency" :*/
fileprivate let kSoundStr:[Character] = ["t","r","a","n","s","p","a","r"]
fileprivate let notiSystemName:String = "eactualcy"

/*: "`%^{}" :*/
fileprivate let user_rowFormat:[Character] = ["`","%","^","{","}"]

/*: "[]|\\<>" :*/
fileprivate let kTitleContent:[Character] = ["[","]","|","\\","<",">"]

/*: "version= :*/
fileprivate let app_equalId:String = "vecolorsi"
fileprivate let app_inputTitle:String = "pull in super a borderon="

/*: &packageId= :*/
fileprivate let appMakeStr:[Character] = ["&","p","a","c","k"]
fileprivate let appFromFormat:String = "lab var button height equalageId="

/*: &bundleId= :*/
fileprivate let userLiveFormat:[Character] = ["&","b","u","n","d","l","e"]
fileprivate let const_valueKey:[Character] = ["I","d","="]

/*: & :*/
fileprivate let showMakeData:String = "&"

/*: ? :*/
fileprivate let user_chooseId:[Character] = ["?"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ManageressThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class ManageressThen: NSObject {
public class ManageressThen: NSObject {
    //: @objc static public let share = ManageressThen()
    @objc public static let share = ManageressThen()
    //: private override init() {}
    override private init() {}
}

//: extension ManageressThen {
extension ManageressThen {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func isNext(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        app_serverName.successOrMeanSun(eventID: dataButtonTextNoKey)
        //: guard TalkingSocketManager.shared.isShared == false else {
        guard ActionSocketDelegate.shared.isShared == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.statusTo(showMsg: show_sessionName)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        PenumbraReactiveCompatible.behindModel { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            BeginVideoManager.mailCompletion { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = EffectTotalViewController()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(k_cellOfStr.replacingOccurrences(of: "first", with: "e"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(const_emptyViewText))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(String(app_originButtonPath) + notiAtInputData.replacingOccurrences(of: "hidden", with: "m"))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.firmness()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func showVc() {
        //: RegularBeginDecisionMakerAppManager.share.userFillInfoMode = UserFillInfoModel.init()
        RegularBeginDecisionMakerAppManager.share.userFillInfoMode = ButtonInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = AddMagnitudeReactiveCompatible()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = afootVc()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func asMessage(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = afootVc()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: ReplyViewController.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: FromThen.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = ReplyViewController()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func bigness(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.firmness() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? TotalervalReadManagerDelegate, SearchPushListener.biologyLab().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.sight()

            //: } else if currentVC.isKind(of: TalkingVoiceRoomViewController.self), TalkingVoiceRoomManager.shared().isParty {
        } else if currentVC.isKind(of: FirstViewDelegate.self), ParlourEventHandler.tarotCard().isParty {
            //: TalkingVoiceRoomManager.shared().voiceRoom_mini()
            ParlourEventHandler.tarotCard().bringHomeSize()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? DetailViewDelegate {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.allSearch()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = DetailViewDelegate(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        pullIn()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func byType(uid: String, enterType: RegularStyleEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard PenumbraReactiveCompatible.dataLive() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(SearchPushListener.biologyLab().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.statusTo(showMsg: String(bytes: noti_styleId.map{textPush(plot: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        MagnitudeervalReactiveCompatible().sharedPush(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = WinterizeModel.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.afootVc()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: PopViewDelegate.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! PopViewDelegate).exceptGift()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            SearchPushListener.biologyLab().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = PopViewDelegate()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                app_serverName.successOrMeanSun(eventID: kTextKey, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                app_serverName.successOrMeanSun(eventID: constFrameContent, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    /// 进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func func_pushToVoiceRoomVC(roomId: String? = nil) {
    func sharedWithWaitingArea(roomId: String? = nil) {
        //: if TalkingLiveManager.shared().isLive == true {
        if SearchPushListener.biologyLab().isLive == true { // 直播中
            //: func__showStatusBarErrorMsg(showMsg: kMessage_live_limit)
            statusTo(showMsg: noti_videoValue)
            //: return
            return
        }
        //: if roomId == TalkingVoiceRoomManager.shared().partyModel.roomId {
        if roomId == ParlourEventHandler.tarotCard().partyModel.roomId { // 返回语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_goback()
            ParlourEventHandler.tarotCard().appGoback()
            //: return
            return
        }
        //: if TalkingVoiceRoomManager.shared().isParty == true {
        if ParlourEventHandler.tarotCard().isParty == true { // 切换语聊房
            //: if roomId != nil {
            if roomId != nil {
                //: TalkingVoiceRoomManager.shared().voiceRoom_switch(roomId: roomId!, beforeRoomId: TalkingVoiceRoomManager.shared().partyModel.roomId)
                ParlourEventHandler.tarotCard().frontTurnId(roomId: roomId!, beforeRoomId: ParlourEventHandler.tarotCard().partyModel.roomId)
            }
            //: return
            return
        }
        //: TalkingVoiceRoomManager.shared().voiceRoom_checkAndTurnOn(roomId: roomId)
        ParlourEventHandler.tarotCard().swerve(roomId: roomId)
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func hockeyPlayer(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = AddManagerDelegate(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = afootVc()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func discount(chatID: String, isFrom: StyleFromEnum = .Normal, completion: ((_ vc: ChatButtonViewController) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != ColorDelimitateMacroDefine.getXiaoMiID() {
        if chatID != ColorDelimitateMacroDefine.television() { // 系统消息
            //: ProgressHUD.show()
            DescriptionProgressHUD.toChange()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: userVoiceStr.reversed(), encoding: .utf8)!: chatID, String(bytes: k_willIndexMsg.map{$0^70}, encoding: .utf8)!: "0", String(bytes: noti_tableUrl.reversed(), encoding: .utf8)!: "1"]
            //: PictureRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            PictureRequestTool.starCompletion(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                DescriptionProgressHUD.actionOf()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = SpeedModelType.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = SpeedModelType.quantityerchangeKey(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                InfoDetailThen.playAddExecute(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: app_imageMsg.map{lifeStyle(fatal: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: k_colorMsg.map{errorRead(photo: $0)}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: data_insertUrl.map{$0^94}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: const_viewKey.reversed(), encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: noti_noUrl,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.statusTo(showMsg: (String(noti_statusContent.suffix(4)) + "unt is r" + main_equalData.replacingOccurrences(of: "them", with: "i") + "cted！").localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.redGiantStar(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: SpeedModelType())
                let chatVC = self.redGiantStar(chatID: chatID, isFrom: isFrom, userWrap: SpeedModelType())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: SpeedModelType ) -> TalkingPrivateChatController {
    func redGiantStar(chatID: String, isFrom: StyleFromEnum = .Normal, userWrap: SpeedModelType) -> ChatButtonViewController {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = afootVc()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: ChatButtonViewController.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! ChatButtonViewController
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = ChatButtonViewController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func cameraId(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        DescriptionProgressHUD.toChange()
        //: PictureRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        PictureRequestTool.mpSet(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            DescriptionProgressHUD.actionOf()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.statusTo(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if ColorDelimitateMacroDefine.isGroupChat(groupId) {
            if ColorDelimitateMacroDefine.statuteContent(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.ream(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = PositionChatController(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.afootVc()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.ream(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = PositionChatController(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.afootVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.statusTo(showMsg: String(bytes: const_itemUrl.map{viewClear(background: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func ream(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.afootVc()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: PositionChatController.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! PositionChatController
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func barDown(toast: String?) {
        //: let tpAuth = TPUserAuth(rawValue: RegularBeginDecisionMakerAppManager.share.loginUserMode.isTPAuth)
        let tpAuth = SawLogSchemaConvertible(rawValue: RegularBeginDecisionMakerAppManager.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingFaceVerificationVC()
            let vc = FacePopUserDataViewController()
            //: vc.toastStr = toast ?? ""
            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = afootVc()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingFinalVerificationVC()
            let vc = BeautyRecognizerDelegate()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = afootVc()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func allowKey(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func faceEqual() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = afootVc()
        //: let vc = TalkingFeedbackVC.init()
        let vc = TidewaterRiverFieldDelegate()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func contextModel(jumpModel: PopMeasurable) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (show_dataToTitle.replacingOccurrences(of: "frame", with: "l")) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            barbLab(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (noti_actualValue.lowercased() + String(notiAddWithData.prefix(4)) + "tail") {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func infoFunc() {
    @objc public func infoFunc() {
        //: ManageressThen.share.func__pushToWebVC(webViewType: .SubscribePage)
        ManageressThen.share.digitiserChangeType(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func cueFromRefreshing(type: ClickFromTypeMirrorPath, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = NewSelectViewController()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        afootVc()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension ManageressThen {
extension ManageressThen {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func digitiserChangeType(webViewType: ViewSendable) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        soarList(webViewType: webViewType, webConfig: FrameControlConfig())
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func themeParams(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(mainPleaseKey.suffix(5)) + showMakeMsg.replacingOccurrences(of: "icon", with: "7"))) {
        //: let payWinType = RegularBeginDecisionMakerAppManager.share.appUserConfigMode.payWinType
        let payWinType = RegularBeginDecisionMakerAppManager.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)
        //: if payWinType == 1 || (RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus && sufficient == false) {
        if payWinType == 1 || (RegularBeginDecisionMakerAppManager.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
            //: func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
            everyEqual(webViewType: .RechargeHalfPage, clickEvent: clickEvent)

            //: } else if payWinType == 2 {
        } else if payWinType == 2 { // 会员订阅弹窗
            //: func__pushToSubscribeAlert(appendParams: appendParams)
            sendUp(appendParams: appendParams)
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func sendUp(appendParams: String = (String(mainPleaseKey.suffix(5)) + showMakeMsg.replacingOccurrences(of: "icon", with: "7"))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = BeginManager.taphouse(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: ManageressThen.share.func__pushToWebVC(urlStr: urlStr)
        ManageressThen.share.barbLab(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        app_serverName.recordEqual(eventID: notiViewText, parameterStr: [String(bytes: user_frameValue.map{leadTransform(bar: $0)}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func everyEqual(webViewType: ViewSendable, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = FrameControlConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        soarList(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            dataTimeMessage = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                app_serverName.recordEqual(eventID: clickEvent, parameterStr: [String(bytes: user_frameValue.map{leadTransform(bar: $0)}, encoding: .utf8)!: 0])
            }
        }
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func soarList(webViewType: ViewSendable, webConfig: FrameControlConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = BeginManager.taphouse(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        imageShared(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func barbLab(urlStr: String?, webConfig: FrameControlConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? FrameControlConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        imageShared(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func imageShared(urlStr: String?, webViewType: ViewSendable?, webConfig: FrameControlConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(String(kSoundStr) + notiSystemName.replacingOccurrences(of: "actual", with: "n"))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (app_equalId.replacingOccurrences(of: "color", with: "r") + String(app_inputTitle.suffix(3))) + "\(kKeyPath)" + (String(appMakeStr) + String(appFromFormat.suffix(6))) + "\(user_environmentMsg)" + (String(userLiveFormat) + String(const_valueKey)) + "\(userSuccessFormat)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = EffectNavigationDelegate(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = afootVc() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: EffectNavigationDelegate.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! EffectNavigationDelegate).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.sumegrationBottom(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: notiEnabledRecordUrl / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: main_versionEnterTimeStr)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension ManageressThen {
public extension ManageressThen {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func afootVc() -> UIViewController? {
        //: return currentViewController()
        return firmness()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct PopMeasurable: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
