
//: Declare String Begin

/*: / :*/
fileprivate let noti_norData:[Character] = ["/"]

/*:  ( :*/
fileprivate let user_barName:String = " ("

/*: ; build: :*/
fileprivate let k_emptyFormat:[Character] = [";"," ","b","u","i"]
fileprivate let userEqualData:[Character] = ["l","d",":"]

/*: ; iOS  :*/
fileprivate let user_renderValue:String = "; iOS user as let"

/*: ;  :*/
fileprivate let constLabStr:String = "key "

/*: )" :*/
fileprivate let k_upLargeFormat:String = ")"

/*: "audio" :*/
fileprivate let app_requestData:String = "rankdio"

/*: + :*/
fileprivate let kIconTitle:[Character] = ["+"]

/*: .wav" :*/
fileprivate let show_viewStr:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let noti_cellId:[Character] = ["a","u","d","i","o","/","w","a","v"]

/*: .jpg" :*/
fileprivate let kBeMainWhiteText:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let app_makeInfoPath:String = "self bar elseimage/"

/*: "://app." :*/
fileprivate let notiClickPath:[Character] = [":","/","/","a","p","p","."]

/*: "data" :*/
fileprivate let data_showPath:String = "dpusha"

/*: ":null" :*/
fileprivate let appPopContent:[Character] = [":","n","u","l","l"]

/*: ":{}" :*/
fileprivate let appViewUrl:[Character] = [":","{","}"]

/*: "Request failed, Try again later" :*/
fileprivate let userFileFormat:[UInt8] = [0xa1,0x96,0x82,0x86,0x96,0x80,0x87,0xd3,0x95,0x92,0x9a,0x9f,0x96,0x97,0xdf,0xd3,0xa7,0x81,0x8a,0xd3,0x92,0x94,0x92,0x9a,0x9d,0xd3,0x9f,0x92,0x87,0x96,0x81]

private func rowTop(voice num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "json error" :*/
fileprivate let notiPicColorMessage:[Character] = ["j","s","o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let noti_toBarImagePath:[Character] = ["p","l"]
fileprivate let data_directionText:String = "atforeplacement"
fileprivate let data_constraintMessage:String = "honimage"

/*: &packageId= :*/
fileprivate let main_timeCenterFormat:String = "any price m title record&pac"
fileprivate let showToFormat:[Character] = ["k","a","g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let main_labFormat:String = "&bundleview info else value"
fileprivate let data_sendValue:String = "return extension path partyId="

/*: &lang= :*/
fileprivate let user_progressFormat:String = "equal"
fileprivate let dataLabUrl:[Character] = ["l","a","n","g","="]

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let userElementTentFormat:[Character] = ["?","!","@","#","$","^"]
fileprivate let data_managerMessage:String = "&%*+"
fileprivate let noti_showId:[Character] = [",",":",";","=","\'"]

/*: "`<>()[]{}/\\| " :*/
fileprivate let noti_addToVarData:[Character] = ["`","<",">","(",")","[","]","{","}","/","\\","|"," "]

/*: "/route/slowindex" :*/
fileprivate let dataViewMessage:[Character] = ["/","r","o","u"]
fileprivate let mainBirthId:[Character] = ["t","e","/","s","l","o","w","i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let show_managerLayerData:String = "/routemodel frame equal add"

/*: "?path= :*/
fileprivate let kSizeModelName:String = "make image view shared make?path="

/*: "&GJNonceStr= :*/
fileprivate let userStyleRangePath:String = "&GJNocenter color"

/*: "& :*/
fileprivate let noti_actionTitle:[Character] = ["&"]

/*: = :*/
fileprivate let main_centerData:String = "background"

/*: "&params= :*/
fileprivate let notiPlayerText:String = "data at info app&param"
fileprivate let const_withoutMessage:[Character] = ["s","="]

/*: "/ :*/
fileprivate let app_colorValue:String = "pop"

/*: "? :*/
fileprivate let main_sharedPlayMessage:[Character] = ["?"]

/*: "token" :*/
fileprivate let show_versionActionText:String = "toposeen"

/*: "请求成功 :*/
fileprivate let showToResultValue:String = "请each成功"

/*: "AAAAAAAA" :*/
fileprivate let userPlayerMessage:[Character] = ["A","A","A","A","A","A","A","A"]

/*: "AA" :*/
fileprivate let user_valueMsg:[Character] = ["A","A"]

/*: "BB" :*/
fileprivate let showKeyOutsideMessage:String = "barbar"

/*: "下载完成 -  :*/
fileprivate let kProgressCookieData:[Character] = ["下","载","完","\u{6210}"," ","-"," "]

/*: "/Documents/" :*/
fileprivate let user_targetUrl:String = "/Docuany path minimize text action"

/*: ".mp4" :*/
fileprivate let mainTextStyleValue:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let kGestureData:String = "移\u{52a8}\u{5730}址:"

/*: "后台任务下载回来" :*/
fileprivate let showMakeStr:String = "player任务下载"
fileprivate let userManagerMsg:[Character] = ["\u{56de}","来"]

/*: "下载成功 : :*/
fileprivate let noti_shareKey:[Character] = ["下","载","成","功"," "]
fileprivate let mainConstraintMsg:[Character] = [":"]

/*: "Net Error" :*/
fileprivate let noti_styleUrl:String = "send"
fileprivate let notiFrameBubbleKey:[Character] = ["e","t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let noti_indexValue:String = "下minimum败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PathRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let k_tagKey = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let noti_timeStr = "\(appErrTablePath)/\(app_domainId) (\(userSuccessFormat)" + (String(k_emptyFormat) + String(userEqualData)) + "\(showMaxName)" + (String(user_renderValue.prefix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: PopErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let app_sizeUrl = PathRequestTool()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class PathRequestTool: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func dataChoice(model: AddRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.whetConstraint() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.lipogram(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.comeDoingCircle(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func lipogram(model: AddRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.jumpModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.executeText(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    objectPlayer(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PopErrorResponse(errorCode: ActionDotTransformable.NetError.rawValue, errorMsg: constInputText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        statusTo(showMsg: constInputText)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (app_requestData.replacingOccurrences(of: "rank", with: "au")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.imageStamp())+\(user_environmentMsg)" + (String(show_viewStr)), mimeType: (String(noti_cellId)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(kBeMainWhiteText)), mimeType: (String(app_makeInfoPath.suffix(6)) + "jpeg"))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    objectPlayer(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PopErrorResponse(errorCode: ActionDotTransformable.NetError.rawValue, errorMsg: constInputText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        statusTo(showMsg: constInputText)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func comeDoingCircle(model: AddRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.jumpModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.executeText(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: const_barStr ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = k_tagKey }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    objectPlayer(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PopErrorResponse(errorCode: ActionDotTransformable.NetError.rawValue, errorMsg: constInputText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        statusTo(showMsg: constInputText)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = k_tagKey }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    objectPlayer(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PopErrorResponse(errorCode: ActionDotTransformable.NetError.rawValue, errorMsg: constInputText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        statusTo(showMsg: constInputText)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func objectPlayer(model: AddRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            PathAtAddrTool.share.researcherWith(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if const_barStr, model.requestServer.contains((String(notiClickPath))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.toTalk(key: PathAtAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (data_showPath.replacingOccurrences(of: "push", with: "at")) + "\"" + (String(appPopContent)), with: "" + "\"" + (data_showPath.replacingOccurrences(of: "push", with: "at")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !const_barStr {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(appPopContent)), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<InfoMeasurable>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == ActionDotTransformable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, PopErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    statusTo(showMsg: responseModel.msg ?? String(bytes: userFileFormat.map{rowTop(voice: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case ActionDotTransformable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kSoundStatusValue, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, PopErrorResponse(errorCode: ActionDotTransformable.NetError.rawValue, errorMsg: (String(notiPicColorMessage)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func jumpModel(model: AddRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(noti_toBarImagePath) + data_directionText.replacingOccurrences(of: "replacement", with: "r") + "m=ip" + data_constraintMessage.replacingOccurrences(of: "image", with: "e") + "&version=") + "\(kKeyPath)" + (String(main_timeCenterFormat.suffix(4)) + String(showToFormat)) + "\(user_environmentMsg)" + (String(main_labFormat.prefix(7)) + String(data_sendValue.suffix(3))) + "\(userSuccessFormat)" + (user_progressFormat.replacingOccurrences(of: "equal", with: "&") + String(dataLabUrl)) + "\(PathAtAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if const_barStr, serverUrl.contains((String(notiClickPath))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if PathAtAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(dataViewMessage) + String(mainBirthId)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(show_managerLayerData.prefix(6)) + "/index")) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(kSizeModelName.suffix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(userStyleRangePath.prefix(5)) + "nceStr=") + "\(PathAtAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.single(key: PathAtAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(notiPlayerText.suffix(6)) + String(const_withoutMessage)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func executeText(model: AddRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(noti_timeStr)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (show_versionActionText.replacingOccurrences(of: "pose", with: "k")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func beautyAfter(model: AddRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.jumpModel(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (showToResultValue.replacingOccurrences(of: "each", with: "求")) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(userPlayerMessage))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (String(kProgressCookieData)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(user_targetUrl.prefix(5)) + "ments/") + NSDate.imageStamp() + (String(mainTextStyleValue))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (kGestureData) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (showMakeStr.replacingOccurrences(of: "player", with: "后台") + String(userManagerMsg)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = BeginBaseDelegate.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func freebee(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: showManagerMsg.frequenter6decrypt(), with: kErrId)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(noti_shareKey) + String(mainConstraintMsg)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, PopErrorResponse(errorCode: ActionDotTransformable.Normal.rawValue, errorMsg: (noti_styleUrl.replacingOccurrences(of: "send", with: "N") + String(notiFrameBubbleKey)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, PopErrorResponse(errorCode: ActionDotTransformable.NetError.rawValue, errorMsg: (noti_styleUrl.replacingOccurrences(of: "send", with: "N") + String(notiFrameBubbleKey)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (noti_indexValue.replacingOccurrences(of: "minimum", with: "载失")) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func nameStart(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: showManagerMsg.frequenter6decrypt(), with: kErrId)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(noti_shareKey) + String(mainConstraintMsg)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, PopErrorResponse(errorCode: ActionDotTransformable.Normal.rawValue, errorMsg: (noti_styleUrl.replacingOccurrences(of: "send", with: "N") + String(notiFrameBubbleKey)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, PopErrorResponse(errorCode: ActionDotTransformable.NetError.rawValue, errorMsg: (noti_styleUrl.replacingOccurrences(of: "send", with: "N") + String(notiFrameBubbleKey)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (noti_indexValue.replacingOccurrences(of: "minimum", with: "载失")) + "\(errMsg)")
            }
        }
    }
}
