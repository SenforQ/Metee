
//: Declare String Begin

/*: "TalkingAreaCodeChoiceCellID" :*/
fileprivate let kTotaloName:String = "price point raw if rightTalki"
fileprivate let main_afterUrl:String = "return center now elsengArea"
fileprivate let app_frameBlockStr:[Character] = ["C","o","d","e","C","h","o","i","c","e","C","e","l","l","I","D"]

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainGoingName:[UInt8] = [0x22,0x27,0x22,0x2d,0xe1,0x1c,0x28,0x1d,0x1e,0x2b,0xf3,0xe2,0xd9,0x21,0x1a,0x2c,0xd9,0x27,0x28,0x2d,0xd9,0x1b,0x1e,0x1e,0x27,0xd9,0x22,0x26,0x29,0x25,0x1e,0x26,0x1e,0x27,0x2d,0x1e,0x1d]

fileprivate func bottomMain(empty num: UInt8) -> UInt8 {
    let value = Int(num) - 185
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Select Country Code" :*/
fileprivate let mainLayerContent:String = "Selemake progress type info"
fileprivate let main_liveText:String = "nlabry"
fileprivate let data_colorValue:[Character] = ["e"]

/*: "#F4F6FA" :*/
fileprivate let notiNeverVersionMessage:String = "false total class add#F4F6FA"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MetadataThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import SwiftUI
import SwiftUI
//: import UIKit
import UIKit

//: class TalkingAreaCodeChoiceVC: TalkingBaseViewController {
class MetadataThen: ScoreViewController {
	var tweedledeeAndTweedledumEnable: Bool = true
	var capNumber: Int = 85
	var headSum: Double = 80.5
	var progressText: String = "status"
	var liveDoing: Bool = false
	var noseCount: Int = 55
	var exhibitQueryedMagnitude: Double = 46.0
	var announcementTitle: String = "row"

    //: public var backCountryCode: ((TalkingAreaModel) -> Void)?
    public var backCountryCode: ((NoManSLandAreaModel) -> Void)?

    //: private let TalkingAreaCodeChoiceCellID = "TalkingAreaCodeChoiceCellID"
    private let TalkingAreaCodeChoiceCellID = (String(kTotaloName.suffix(5)) + String(main_afterUrl.suffix(6)) + String(app_frameBlockStr))

    //: private var sectionTitlesArray = [String]()
    private var sectionTitlesArray = [String]()
    //: private var sectionArray = [Array<Any>]()
    private var sectionArray = [[Any]]()

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainGoingName.map{bottomMain(empty: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            if (self.modalPresentationCapturesStatusBarAppearance) && (self.inputView != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let tableArrayLet = FinishController()
            tableArrayLet.endDoing = self.hideNavi

            tableArrayLet.backMonthUserSwitch = { [self] atOn in
            self.liveDoing = atOn
            
            var tableArrayLet = self.hideNavi
            tableArrayLet = true
            if tableArrayLet != self.liveDoing {
                self.liveDoing = tableArrayLet
            }
            
            self.liveDoing = false
            return self.liveDoing
            }
            tableArrayLet.pointMagnitude = { [self] conferenceTotal in
            self.noseCount = conferenceTotal
            
            return self.noseCount
            }
            tableArrayLet.appSum = { [self] sizeEqualMagnitude in
            self.exhibitQueryedMagnitude = sizeEqualMagnitude
            
                self.exhibitQueryedMagnitude += 1
                if self.exhibitQueryedMagnitude >= 0 {
                    self.exhibitQueryedMagnitude = self.exhibitQueryedMagnitude - 1
                }
            return self.exhibitQueryedMagnitude
            }
            tableArrayLet.frameName = { [self] cullOutText in
            self.announcementTitle = cullOutText
            
            return self.announcementTitle
            }
                self.navigationController?.pushViewController(tableArrayLet.self, animated: true)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Select Country Code".localized
        self.title = (String(mainLayerContent.prefix(4)) + "ct Cou" + main_liveText.replacingOccurrences(of: "lab", with: "t") + " Cod" + String(data_colorValue)).localized

        //: createDefaultData()
        translate()
        //: self.setupSubviews()
        self.medium()
        //: self.setupSubViewsConstraint()
        self.weltGenerate()
        //: self.bindInteraction()
        self.lag()
    
            if ((self.inputAssistantItem.allowsHidingShortcuts != true) && (self.inputAssistantItem.leadingBarButtonGroups.count == 9)) && (self.childForStatusBarStyle != nil && self.childForStatusBarStyle!.edgesForExtendedLayout == .left) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let toolLet = FinishController()
            toolLet.endDoing = self.hideNavi

            toolLet.backMonthUserSwitch = { [self] atOn in
            self.tweedledeeAndTweedledumEnable = atOn
            
            var toolLet = self.hideNavi
                toolLet = true
                toolLet = !toolLet
            if toolLet != self.tweedledeeAndTweedledumEnable {
                self.tweedledeeAndTweedledumEnable = toolLet
            }
            
            self.tweedledeeAndTweedledumEnable = true
            return self.tweedledeeAndTweedledumEnable
            }
            toolLet.pointMagnitude = { [self] conferenceTotal in
            self.capNumber = conferenceTotal
            
            return self.capNumber
            }
            toolLet.appSum = { [self] sizeEqualMagnitude in
            self.headSum = sizeEqualMagnitude
            
            self.headSum = 0
            return self.headSum
            }
            toolLet.frameName = { [self] cullOutText in
            self.progressText = cullOutText
            
            return self.progressText
            }
                self.navigationController?.present(toolLet.self, animated: true) { [self] in
            do {
                self.progressText = try String(contentsOfFile: self.progressText.lowercased() + "strength")
            } catch {
                self.progressText = error.localizedDescription
            }
                }
            }

	}

    //: deinit {
    deinit {}

    //: lazy var tableView: UITableView = {
    lazy var tableView: UITableView = {
        //: let tableView = UITableView.init(frame: .zero, style: .plain)
        let tableView = UITableView(frame: .zero, style: .plain)
        //: tableView.delegate = self
        tableView.delegate = self
        //: tableView.dataSource = self
        tableView.dataSource = self
        //: tableView.register(TalkingAreaCodeChoiceCell.classForCoder(), forCellReuseIdentifier: TalkingAreaCodeChoiceCellID)
        tableView.register(ChoiceThen.classForCoder(), forCellReuseIdentifier: TalkingAreaCodeChoiceCellID)
        //: tableView.sectionIndexColor = .appValueColor()
        tableView.sectionIndexColor = .oversee()
        //: tableView.backgroundColor  = UIColor.init(hex: "#F4F6FA")
        tableView.backgroundColor = UIColor(hex: (String(notiNeverVersionMessage.suffix(7))))
        //: return tableView
        return tableView
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingAreaCodeChoiceVC {
extension MetadataThen {
    //: private func createDefaultData() {
    private func translate() {
        //: let arr = TalkingLocationTool.share.areaArr
        let arr = ComeBackLocationTool.share.areaArr

        //: let collation = UILocalizedIndexedCollation.current()
        let collation = UILocalizedIndexedCollation.current()

        //: let sectionNumb = collation.sectionTitles.count
        let sectionNumb = collation.sectionTitles.count

        //: var sectionTempArray = [Array<Any>]()
        var sectionTempArray = [[Any]]()

        //: for _ in 0..<sectionNumb {
        for _ in 0 ..< sectionNumb {
            //: sectionTempArray.append([])
            sectionTempArray.append([])
        }

        //: for areaModel: TalkingAreaModel in arr {
        for areaModel: NoManSLandAreaModel in arr {
            //: let sectionIndex =  collation.section(for: areaModel, collationStringSelector: #selector(getter: areaModel.areaName))
            let sectionIndex = collation.section(for: areaModel, collationStringSelector: #selector(getter: areaModel.areaName))
            //: sectionTempArray[sectionIndex].append(areaModel)
            sectionTempArray[sectionIndex].append(areaModel)
        }

        //: for index in 0..<sectionNumb {
        for index in 0 ..< sectionNumb {
            //: let tempArr = sectionTempArray[index]
            let tempArr = sectionTempArray[index]
            //: let sortArr = collation.sortedArray(from: tempArr, collationStringSelector: #selector(getter: TalkingAreaModel().areaName))
            let sortArr = collation.sortedArray(from: tempArr, collationStringSelector: #selector(getter: NoManSLandAreaModel().areaName))
            //: sectionTempArray[index] = sortArr
            sectionTempArray[index] = sortArr
        }

        //: for (index, value) in sectionTempArray.enumerated() {
        for (index, value) in sectionTempArray.enumerated() {
            //: if value.count > 0 {
            if value.count > 0 {
                //: let titleStr = collation.sectionTitles[index]
                let titleStr = collation.sectionTitles[index]
                //: sectionTitlesArray.append(titleStr)
                sectionTitlesArray.append(titleStr)
                //: sectionArray.append(value)
                sectionArray.append(value)
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingAreaCodeChoiceVC {
extension MetadataThen {}

// MARK: - Delegate

//: extension TalkingAreaCodeChoiceVC: UITableViewDelegate, UITableViewDataSource {
extension MetadataThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return sectionTitlesArray.count
        return sectionTitlesArray.count
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: if sectionArray.count > section {
        if sectionArray.count > section {
            //: let array: [TalkingAreaModel] = sectionArray[section] as! [TalkingAreaModel]
            let array: [NoManSLandAreaModel] = sectionArray[section] as! [NoManSLandAreaModel]
            //: return array.count
            return array.count
        }
        //: return 0
        return 0
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell: TalkingAreaCodeChoiceCell = tableView.dequeueReusableCell(withIdentifier: TalkingAreaCodeChoiceCellID, for: indexPath) as! TalkingAreaCodeChoiceCell
        let cell: ChoiceThen = tableView.dequeueReusableCell(withIdentifier: TalkingAreaCodeChoiceCellID, for: indexPath) as! ChoiceThen
        //: let array: [TalkingAreaModel] = sectionArray[indexPath.section] as! [TalkingAreaModel]
        let array: [NoManSLandAreaModel] = sectionArray[indexPath.section] as! [NoManSLandAreaModel]
        //: cell.func__updateUIWithModel(areaModel: array[indexPath.row])
        cell.isServerProgress(areaModel: array[indexPath.row])

        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 50
        return 50
    }

    //: func sectionIndexTitles(for tableView: UITableView) -> [String]? {
    func sectionIndexTitles(for _: UITableView) -> [String]? {
        //: return sectionTitlesArray
        return sectionTitlesArray
    }

    //: func tableView(_ tableView: UITableView, viewForHeaderInSection section: Int) -> UIView? {
    func tableView(_: UITableView, viewForHeaderInSection section: Int) -> UIView? {
        //: let headView = UIView.init()
        let headView = UIView()
        //: headView.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: 30)
        headView.frame = CGRect(x: 0, y: 0, width: notiEnabledRecordUrl, height: 30)
        //: headView.backgroundColor = .white
        headView.backgroundColor = .white

        //: let label = UILabel.init()
        let label = UILabel()
        //: label.frame = CGRect(x: 15, y: 0, width: ScreenWidth-30, height: 30)
        label.frame = CGRect(x: 15, y: 0, width: notiEnabledRecordUrl - 30, height: 30)
        //: label.text = sectionTitlesArray[section]
        label.text = sectionTitlesArray[section]
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17)
        label.font = .pingfangCenter(type: .Semibold, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .census()
        //: headView.addSubview(label)
        headView.addSubview(label)
        //: return headView
        return headView
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 30
        return 30
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: let array: [TalkingAreaModel] = sectionArray[indexPath.section] as! [TalkingAreaModel]
        let array: [NoManSLandAreaModel] = sectionArray[indexPath.section] as! [NoManSLandAreaModel]
        //: let areaModel = array[indexPath.row]
        let areaModel = array[indexPath.row]
        //: if self.backCountryCode != nil {
        if self.backCountryCode != nil {
            //: self.backCountryCode!(areaModel)
            self.backCountryCode!(areaModel)
        }
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }
}

// MARK: - Layout

//: extension TalkingAreaCodeChoiceVC {
extension MetadataThen {
    // 添加视图
    //: private func setupSubviews() {
    private func medium() {
        //: view.addSubview(tableView)
        view.addSubview(tableView)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func weltGenerate() {
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalToSuperview()
            make.leading.trailing.top.bottom.equalToSuperview()
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func lag() {}
}
